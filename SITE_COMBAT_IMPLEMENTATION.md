# Site 交戰實作進度與驗證

2026-09-14 短招／接續增量完成：原攻擊從揮擊段播 0.85 秒、受擊 0.45 秒、擊退 0.75 秒、平手 0.3 秒；站定有末姿，移動只留攻擊起算最初 0.1 秒，連箭同級反應不反覆重啟。原傷害、硬直、交鋒節奏、疲勞、遠程、包圍與保存不變，2,520 次修改前後固定步狀態完全一致；12 組原角色近景通過。200 人／60 秒平均 254.97 FPS、P95 17.673 ms，原門檻通過，但比本輪基準 282.39 FPS／14.884 ms 慢，不能宣稱無效能退步。[規則、動畫播放、完整數據與全部失敗](output/site_exchange_animation_20260914/README.md)。下方保留此前歷史。

2026-09-14 接戰包圍增量完成：原接敵／有效追擊命令下，前排牽制、空閒近戰兵沿原格位預約繞侧；射手、玩家控制、工作及撤退限制保留並補齊無接觸時的補位分支。200 人含 40 射手／60 秒、218 步、三方向夾擊，平均 305.02 FPS／P95 14.142 ms；[本輪規則與完整驗收](output/site_encirclement_20260914/README.md)。下方為此前完成歷史。

2026-09-14 最新增量：正式近戰已改為相鄰能力交鋒；後續授權的落點遠程亦完成，含真弓弩／彈藥、單次到期、閃避／誤傷、疲勞／技能／設施、兩種普通兵完整圖集及原营地製作／領取／配裝。200 人含 40 射手、60 秒／800 發／原實物守恆與流暢門檻通過。當前可調數值見 [交鋒與遠程 v1](SITE_COMBAT_EXCHANGE_V1.md)，定向測試、實際 FPS、全部失敗與範圍見 [遠程證據](output/site_ranged_20260914/README.md)；[上一輪近戰證據](output/site_exchange_20260914/README.md) 保留，不以較早完成狀態代替本輪驗收。

## 2026-09-13 08:03 完成狀態：已授權的單一 Site 首版計畫與功能驗收完成

此段取代下方各時點的即時狀態；舊 FAIL、未完成結論與當時限制完整保留。**本次已授權首版的功能、操作入口、保存、正式缺裝素材及要求的功能驗收已完成**，涵蓋原交戰／指揮名冊、共用疲勞與多人工作、供糧／士氣／訓練、死亡／昏迷搜刮、拘押／原家人接續及真實配裝。這不是任意武器／騎兵／九千人實戰、完整平衡或即時 200 人效能達標的宣告。

最後完整交戰 `20260913_073548_879`：GPU helper **PASS、退出碼 0、1,379.043 秒**，實際 **60.091667 連續交戰秒、184 有效命中、29 人受傷、25 人自然戰死**。200 原人物、1,018 原物品／貨物及引用守恆；24 包、另 1 人仍待原倒地收束，不能冒稱 25 包全結算；4,311 Nodes 前後不變，交戰保存 BUSY 保留原安全檔。未觀測自然 KO，其原 200 人卸裝／自然甦醒另外驗收。兩張完整前後圖已檢視。

保留該次原始 `goal_met=false`／PILOT_INCOMPLETE：測試初始化 false 後以不覆寫的 Dictionary.merge 寫入最終結果，造成**報告誤標，不是遊戲門檻失敗**。只修報告最後旗標／reason，16 組正反案例及唯讀原結果覆核 `080110_099` headless PASS（1.572 秒），另存 `report_validation.json`，原 JSON／日誌與圖片不改，沒有宣稱修報告後又重跑一次長測。原門檻不變，較早未滿時的 pilot 仍未完成；詳見 [完整耐久與報告診斷](output/site_army_combat_longrun/README.md)。

**效能限制未掩蓋**：完整步進 CPU 1,338,155.163 ms，其中精確接觸 1,196,112.486 ms；這次約 23 分鐘才跑完 60 秒交戰模擬。功能耐久成立，但目前不是流暢即時 200 人作戰。遺物採稀疏批次資料、每包新增 Node 為零，仍有保存／顯示／換裝成本，不能據此宣稱零負擔。

最後完整匯入 `20260913_073310_177`：原執行器以 `--headless --editor --import` **PASS、退出碼 0、107.389 秒、未逾時**；1,230 項重新匯入、後處理及 editor layout 均真正完成，沒有用 `--quit-after` 提早截斷。保留原 Windows 憑證提示及既有 `output/verification-probe` 巢狀專案忽略訊息；不改正式玩家存檔。完整四份原始紀錄已逐檔 SHA-256 封存。

07:26 補記：固定模型搜尋重用的六組 GPU 對照全部 PASS（`071340_041`／`071350_550`／`071400_776`／`071410_169`／`071419_182`／`071430_017`，10.248／10.029／9.221／8.850／10.652／11.301 秒）。包含原 32 配方、八武器／徒手映射及明光卸裝／倒地／起身，共 56 姿態、224 原護甲點；新增 1,604 個護甲表面逐頂點、完整 pose Dictionary 在 cache 開／關時完全相同，無容差，原 HumanEditor 基準比對仍保留。搜尋重疊前綴、空前綴、輸入 Array／回傳 Array 被修改、換模型與显式改階層後失效均通過。新共用來源 SHA-256 `625a53788ce92e66c189e9c8623f4a099a0c4120a45f601890fab4142be385c0`；六個正式資產來源未改。

200 人原 KO→兩人各 5 秒卸甲／盾→原 30 動作秒自然醒／2.2 秒起身的 GPU 完整對照已 PASS：取物 `071555_517`（37.755 秒）、不取物 `071655_101`（36.133 秒）。兩組均原 200 隊員＋2 Actors、3,960 個原 120 Hz 小步、33 動作秒，第 3,601 步開始醒；跨原 10 秒交戰／23 秒和平，Site 共 1,390 秒，沒有假延長交戰或加速 KO 動畫。全原人物、HP、cargo、物品引用守恆，無新增包，未取的 198 人不變、兩取物目標醒來不補裝。兩組 Node 前後／峰值皆 4,311；取物後貼圖增加 53,477,376 bytes，baseline 不增加。各 134 个實際畫面間隔，每張最多 30 模擬步，平均 114.159／108.208 ms、P95 153.595／124.582 ms，共用步進 CPU 9,545.899／8,818.330 ms；這是分批壓力對照而非即時戰鬥 FPS。四張圖已逐張檢視，右 UI 遮部分右隊，不冒充每人放大外觀驗收。完整分段／記憶體及失敗歷史見 `output/site_loot_200_knockout_20260913/README.md`。

07:13 歷史補記：`070239_453` 十分鐘有界耐久正常退出（595.138 秒），仍 **PILOT_INCOMPLETE**：35.825 連續交戰秒、150 次有效命中、25 人受傷、20 人自然戰死並留下 20 包，1,018 原物品與 200 原列完整守恆，Nodes 4,311 不變；CPU 565,281.987 ms／接觸 505,544.693 ms。來源舊版 SHA-256 `621e676452f67384d3465ee46fa45ec224e19978c5676bedca21ae4ca7fe5938`，原四份輸出先逐檔 SHA 核對封存於 `output/site_army_combat_longrun/mixed/endurance_before_lookup_cache/`，最終圖已檢視。死亡後戰線重排時大量重複搜尋固定模型部件；僅在原單一共用查詢 editor 初始化完成後重用原節點搜尋結果，保留原順序／Node、回傳新 Array、模型換代清空，不改原六個資產來源、骨架／動作／幾何／取樣精度。當時新增六組對照尚在驗證，其後結果見上段。

07:29 補記：查詢重用後的同一 600 秒耐久 `071849_787` 正常退出（594.993 秒），仍 **PILOT_INCOMPLETE**：40.291667 連續交戰秒、156 有效命中、25 人受傷／20 自然戰死、20 包／1,018 件守恆、4,311 Nodes 前後不變；CPU 562,815.505 ms／接觸 491,809.984 ms，共用固定節點查詢只有 13 個鍵，static memory 696,040,883→700,858,127 bytes。比舊試跑推進更多但依然遠非即時交戰，不能當成 FPS 提升百分比。新增獨立 `--full-endurance`（內部 1,800 秒／helper 1,820 秒）供完整 60 秒正確性驗收，保留原 100／600 秒模式、原輸出及所有門檻，測滿即退出；不再以短測估計完整傷亡後的時間成本。

### 本輪完成的剩餘實作

- 原玩家、NPC、隊員沿同一人物、實際行囊、持物者及 Site 時鐘完成入退隊、拆併、多人工隊、訓練、疲勞與供養；原 NPC 運補期間不再被自動工作或自動救助搶走任務。取消／完成後恢復原工作入口，沒有另造代理人物。
- 營地出庫、兩隊交糧、私人交糧、原地面包拾取接上同一作業；明確指派普通／專職搬運者提供 3／6 日份載量，超量降職拒絕；潰退、退卻與失能後無法攜走的糧留在實際現場。開封糧保留精確份量（含兩份 0.75 合為 1.5），不取整、不變成新 grain，首次供養與轉軍籍不重吃或洗飢餓。
- 死亡只將原剩餘貨物、裝備及私人開封糧轉入一次遺物；昏迷搜身由原計時作業取原物，醒來不補裝。本人／原隊員可撿開封餘糧，容量、等值換引用、換控制者、受擊、取消及版本上界皆拒絕非法提交。舊檔已結算死者的遺漏開封糧沿原殘存包或原合法格補遷移，不重生人物或裝備、不改舊檔；清隊完整預檢後保留物資，不丟掉其他人物的餐份歷史。
- 私人鬆散裝備可從原「個人裝備／領裝」面板明確入營地庫存，再由有權者命令原 NPC 完成 5 秒領裝；多選原件、容量、持有人／庫存版本与舊視窗守衛皆接原原子移轉。不是自動卸装、免費生裝或授予國家職位。
- 拘束、封存、唯一有效看守、押送、贖回／逃脫／解綁、原家人死亡接續与異地 SiteStore 守衛已串接。獨立隊員不再被前隊長當成有權看守；贖回子視窗保留原付款人／控制者／場景，換人或換場景後不能開作業或扣糧。家族、兵種標準／替代裝備、名冊多選與個人裝備六張 GPU 面板已實際檢視。

### 正式缺裝資產與公平判定

標準普通男兵五槽的 **32 組全部配方、160 頁、17,152 幀已完整發布**（`20260913_061749_202`，7.668 秒），不是部分 catalog。481 檔共 294,842,772 bytes；六個原模型／動作／烘焙來源指紋不變，每頁原像素／PNG 解碼／RES 解碼雜湊核對，160 頁已逐頁檢視。解碼 RGBA 頁合計 1,622,736,896 bytes 是所有頁相加，**不是同時 GPU 常駐**；原讀取器保留 64 MiB 強引用頁預算與 Sprite 正在使用的原共享引用，不逐兵复制。

- 正式 32 配方 GPU 初次 `062704_608` PASS（29.516 秒），最終來源再驗 `073229_487` **PASS（27.849 秒）**：真實原名冊 32 種實裝、Store/save/load/bind、96 個 idle／guard／attack Sprite 姿態、64 次精確查詢，四張最新圖重新檢視，包含無甲／無盾／無武器；不以帳面卸裝代替畫面。516 件原物守恆，32 配方的 17,152 個幀鍵齊全，3,493 Nodes；不外推完整全武器呈現。
- 同一步只重用原人物的世界投影、批次結束即清；生效窗外不再計算原本丟棄的武器投影。專項初次 `064949_142` GPU PASS（9.155 秒），最終 `073004_586` **PASS（8.856 秒）**，含原女性、不同攻擊者、guard／移動／卸裝及下一步失效，逐頂點與完整接觸結果嚴格相同，沒有改候選、120 Hz、生效窗、傷害或護甲。
- 四方向 × 攻／守角色替換 × idle／guard／ally／moving **32 組最終全 PASS**：`073014_109`／`073049_724`／`073120_447`／`073158_203`，35.433／30.549／37.677／31.200 秒。32 份 JSON 與 32 張 PNG 全部和已封存的原批次 SHA-256 完全一致，覆蓋最後固定模型查詢重用改動；先前 `065506_657` 等批次保留。完整男性同實裝矩陣不等於所有缺裝／女性／武器組合；原男女八武器及馬上長劍／長槍驗收另見下方 21:22 批次。

### 最終功能回歸

27 項 headless 實際流程均 PASS（不是 check-only）：搜刮與開封餘糧、舊檔遷移／清隊保留、運補排他、四來源交糧、物流 UI、受控人物行為、私裝入庫／領裝 UI、押送、拘押供養與轉軍籍、共用時鐘／200 人制度慢幀、死亡／KO 搜刮生命週期、家族接續、正式領裝、物品保存、贖回視窗、有效段、共同移動、名冊、指揮保存、戰術、戰鬥慢幀、疲勞、工作隊伍、首次供養及保存大小。

執行 ID 為 `065256_110` 至 `065432_374` 的 16 項，以及 `065934_632`／`065940_068`／`065944_484`／`065949_644`／`065954_300`／`070103_139`／`070109_174`／`070127_417`／`070133_324`／`070139_400`／`070145_286`。日期前綴均為 `20260913_`，完整腳本名稱、實際秒數、退出碼與日誌保存於 `output/site_institutions_20260913_0330/verification/`。

保留 `065959_541` 的戰術測試 FAIL：舊 fixture 仍假定 0.24 秒走完，與已驗收的原人物共用 0.38 秒不符。只改測試為 0.3 秒仍在途、發新命令保留所有已提交格步、完成後不能續走舊退卻目的地；原規則不改，完整戰術回歸 `070103_139` PASS。`064855_021` 是新測試來源 editor 的型別推斷失敗，顯式 int 後檢查及 GPU 皆通過。不能把舊失敗刪掉或以印出 PASS 字樣取代 helper 結果。

### 已量測的物資負擔與長測歷史

沿用下方 200 人遺物開／關、1,000／9,000 容器資料及 GPU 指標。新雙人實際回收 `064208_556` GPU PASS（25.297 秒）：原 200 具遺物、1,018 件物品、前後 4,311 Nodes；兩名原 Actor 透過實際和平共用時鐘完成原木 2＋3 件回收，3 遊戲秒／6 個活動畫面間隔，CPU 268.383 ms，平均 97.255／P95 154.001 ms。這個樣本很短，不是稳定 FPS 或自動尋路回收；200 包仍有未取裝備而保留。保存 395,246 bytes，保存 59.662／載入 485.722 ms，圖已實際檢視。

原全配裝百秒 pilot `063100_281` 未達門檻；換成明確起始混合配裝（原 20 名前排的原 40 件盾／甲移到營地，其他 180 人不變）後，`065014_232` helper 正常退出，但仍為 **PILOT_INCOMPLETE**：9.275 連續交戰秒、60 次有效命中、20 人受傷、零 KO／死亡，人物／物品守恆；CPU 71,839.663 ms、其中接觸查詢 63,202.854 ms。這不是即時 FPS，也不是已完成 60 秒長測。其後兩次 600 秒結果見上方歷史補記；最終唯一完整耐久為 `073548_879` 的獨立 `--full-endurance`，已實測達原门檻且保留報告誤標及修正證據。原百秒／600 秒測試與輸出不覆蓋，200 人 KO 卸裝自然甦醒對照已另有實測。

### 操作規則與首版界線

完整可調數值與入口见 `SITE_COMBAT_INSTITUTIONS_V1.md`，不是平衡完成宣告。共同疲勞 0–100，30 以下不減速、100 時前後搖／工作耗時最多增加 30%；攻擊／跑步每遊戲秒 +0.1、格擋 +0.05，工作／訓練每遊戲小時 +10。脫離威脅連續安全休息 30 秒後，每遊戲小時回復 20；NPC 工作 80 停、50 續，玩家不強制停工，戰鬥生效段／傷害／護甲不被疲勞改寫。

仍保留單一 Site、兩隊合計 200 原名冊人物、8 MiB 保存上限、原政治／家族資料及未完成動作保存守衛；普通兵本版正式呈現為標準五槽 32 配方，不宣稱任意 tint、所有普通兵武器、騎兵／攻城或 9,000 人實戰。稀疏物資容器新增 Node 為零，不代表新增存檔與場景成本為零；所有效能限制照實保留。

本輪依 ponytail 沿既有 owner 實作，沒有新增第二人物世界／戰鬥時鐘／每兵骨架。沒有 Git 提交或推送、沒有改正式玩家存檔；無關工作樹變更保留。完整編輯器匯入已通過，所有本輪執行均已結束；背景監看依使用者授權採現有狀態／等待工具，專案不再硬性要求不存在的 schedule。

## 2026-09-13 05:55 歷史狀態：搜刮、拘押、領裝與原家人接續已串接，仍在完成剩餘驗收

本段取代下方 03:10 的即時狀態，保留所有舊失敗與其當時界線。**整份計畫尚未完成；下列已驗收切片不代表全部武器或大量實戰已通過。** 本輪依 ponytail 沿原人物、原 cargo、原工作／移動／命中及 SiteStore 擴充；沒有新增第二人物世界或逐兵骨架。

### 05:55 後續核對與正在完成的工作

- 標準男性同實裝四方向 × 攻擊者／受擊者替換 × idle／guard／ally／moving，**32 組全部 PASS**：`053224_253`／`053346_003`／`053526_411`／`053700_792`，36.497／32.165／42.490／31.730 秒。原玩家／NPC baseline 與普通隊員的完整 HP／封包／接觸 tick／實際側步末格相同；32 張原 Actor baseline 圖逐張已檢。不是 ordinary 缺裝 renderer 或全武器／女性矩陣，完整界線見 `.visual_captures/site_role_matrix_final_20260913/README.md`。
- 原名冊多選派工、停止、97+3 原人物拆分、實際接收指揮者一次確認／換人舊確認拒絕、再合併 100 人及 Store/load/bind：`053200_930` PASS，5.996 秒。保留 `053027_918` 的測試 UI typed Array 錯誤，沒有藉重建人物修正。
- 四名原俘虜與一名看守經真正共用步進押送，人物／物品引用不換、沿途有效看守，NPC 80 停工／50 恢復及受控玩家不強制停工：`053113_690` PASS，6.948 秒。80／50 此新測是邊界 fixture，完整自然恢復由既有疲勞回歸證明。
- 裝備圖集 108/160 批已產生且逐頁核對，仍待全部完成後一次發布。正式 ordinary 圖腳點發現漏乘原 map_scale，已作最小修正；新 32 實裝／Sprite 圖片驗收尚未跑。
- 逐條核對發現制度第 3 節仍缺營地出庫、兩队交糧、專職 6 日份搬運與潰退超量現場遺留，正在沿原 delivery 作業、原人、原庫存與共用容器補齊。開封餐份不可捨去或捏成 grain；新的可選 `open_rations` 容器欄與原 row `logistics` 角色沿 format 3 加嚴格校驗，不放寬 8 MiB。
- 首次啟用隊伍供養改走已有 captivity/membership 原子供養 owner，避免既有私人餐份被當新隊員重領；新增原 row／受養俘虜、延遲時鐘拒絕與開封餐份存讀測試，尚未執行。裝備／家族六張正式 UI 驗收和受控 row 退隊私人供餐延伸亦待本輪圖集後集中跑。

- 死亡經原 down 與已提交移動收束後，只把剩餘實物轉入一次遺物；昏迷搜身不延長昏迷，醒來與存讀不補裝。原人物／遺物清單、指定實物、5 秒卸裝及散裝 `2 + 0.2 × 件數` 作業已串原 Site 時鐘及接觸後提交。`034714_278` 完整 headless PASS，3.906 秒；清晰 GPU companion `040931_872` PASS，15.013 秒，已檢 `output/site_loot_lifecycle_20260913/` 兩圖：原甲盾移除、地面標記與剩餘實物清單。GPU companion 不代替 headless 全時鐘／存讀證明。
- 實裝推導來自唯一 holder／item record；原角色編輯器的玩家槽選擇改走合法自有物品的 5 秒換裝，不能靠選單生成裝備。普通兵共享單一幾何查詢，快取含實際配方；無盾持近戰武器才使用原武器格擋，遠程與徒手不冒充長劍。32 缺裝、8 原武器及再裝明光甲的幾何／護甲定向 GPU 6 組通過，224 護甲點、56 姿態，最大幾何誤差約 `0.000098465`；不是每種實裝的完整戰鬥矩陣。
- 實際拘束／封存、解綁、30 秒無看守且無威脅逃脫、100 日份實物贖回與同一步中斷已接原人物作業；供養變更搬移原飢餓和已吃餐份，不生成第二份糧。`040750_045` 供養整合 PASS，5.654 秒，含跨隊缺糧原死亡。押送沿原人物／Army 預約步進，失去看守停止而不解綁；`041756_314` PASS，3.344 秒，含停止、暫停、合法格位、拘押持物不變、原存讀與看守死亡。看守也使用原工作疲勞；最新 80／50 接線尚待最後回歸。
- 國家軍事首長才能更動未來標準；隊長不自動取得該職位。只有真庫存與明列同槽替代可领裝，缺貨／退庫超量／呈現未就緒均不部分扣物。`040843_179` 原領裝 provider PASS，1.225 秒；尚須最後受控身分回歸。首版沿實際資產原配色，非原生 tint 支援不足時回傳 UNSUPPORTED，不把自訂色忽略或改成白色。
- 家族須明確登錄既有人物，按 Site ID + 原 person ID 引用。死亡與遺物／指揮狀態收束後才暫停選擇；未登錄、名單未確認完整、異地檔缺失都不誤判 Game Over。接續原 NPC／Army row 不換或複製 body/cargo；未成年、拘押與原職位不變。異地先由原保存守衛歸檔死者 Site 再載原快照；BUSY 留在選擇，可明確等待原場景脫戰。`043217_673` 原 callback + SiteStore 全流程 PASS，15.045 秒；尚非完整鍵盤／GPU 验收。
- 繼承後交糧改用原受控 sender 的 body／holder／cargo 引用，等值換來源、換控制人物、同一步受擊都會拒絕提交。`043352_321` PASS，5.945 秒；原主角供餐流程回歸 `043405_407` PASS，7.701 秒。人物控制與家族存讀新增後仍需整體回歸。
- 原隊員軍籍与身體分開：`member=false` 仍保留原 row、位置、HP、碰撞、持物与生死，退出不變成 departed；隊形、指揮、供養與原隊訓練只認實際成員。`052247_500` membership PASS，4.756 秒；`052300_008` 原 full Lab 控制 PASS，8.792 秒（兩原 Actor 訓練來源隔離、NPC 入退隊／原存讀、Army 走跑／攻防／手採／本人交貨）；`052508_405` 原名冊拆併回歸 PASS，5.114 秒。先前 Store 子驗證誤要求其他切片的 worker 欄位已修正，完整存檔仍強制檢查。
- 俘虜／個人供養與軍籍分流已用同一原子交易 owner 串起：`052542_754` 新 roster／row 與 Actor membership 全流程 PASS，5.578 秒；`052555_897` 舊拘押供養回歸 PASS，5.769 秒。未啟用來源隊、既有個人餐份、俘虜換軍籍、臨時看守換隊及拘押者換隊／退隊均不重建餐份；原庫存／open 餘糧不隨信用搬動。生死 checkpoint 保持原 feeding cohort schema，未新增另一份人物生命。
- 多人工作已接原每分鐘切片、每個原隊員 work_task/cargo/疲勞及原逐格步進，沒有把單一工人產出乘人數。新地點尋路重入與 JSON 工作區格號比較已修正。`043826_719` helper PASS，4.484 秒；完整 Controller／原雙人 UI／正常慢幀／暫停／80→50／原貨物搬運／保存重綁續工 `045924_256` PASS，6.219 秒；新增軍籍過濾後 `052316_281` 回歸 PASS，5.649 秒。原 NPC／受控 Army row 手採沿本人原貨物，玩家不被 80 強制停工；訓練同樣按當前 controlled ID 而非殘留 is_player body 旗標，`045938_053` PASS，0.555 秒。
- 200 名原隊員死亡突發：倒地計時未滿前不再每個 120 Hz 小步重查全體持物者，完整 common-clock CPU 由 `044044_737` 的 5,140.767 ms 降至 `044848_005` 的 280.451 ms，仍只生成一次 200 包；同測試无遺物對照 `044156_374` 305.297 ms（跨次計時波動不能推論生成遺物比不生成更快）。enabled headless PASS 6.588 秒，存檔 384,791 bytes，場景節點前後均 1,095。GPU enabled `044221_383`／baseline `044911_382` PASS，靜止平均 43.891／42.812 ms，移鏡 42.908／42.179 ms，各 32 個真實間隔，節點均 4,309，texture monitor 均 908,275,712 bytes。這是原全場景死亡／遺物隔離比較，非持續交戰 FPS，完整數值在 `output/site_loot_200_20260913/`。
- 1,000／9,000 真實物品容器的資料＋原地圖標記 GPU 五次驗收全 PASS，四張圖片已檢視；檔案 279,788／2,578,326 bytes，保存 37.256／285.863 ms，讀取 752.241／1,014.288 ms。GPU 靜止平均 14.608／23.879 ms、P95 59.619／68.903 ms，移鏡平均 14.492／25.046 ms、P95 59.879／72.325 ms。兩者固定 137 Node／128 行，容器新增 Node 為零；9,000 包有明顯尖峰，**不宣稱穩定 60 FPS，也不是 9,000 士兵交戰**。精準合法 padding 的 8 MiB 可存，+1 byte 拒絕並保留舊檔與 9,000 包；padding 是邊界 fixture，不冒充自然容器檔案成長。見 `output/site_ground_loot_scale/README.md`。
- 32 組共享缺裝動畫分 160 個有界批次，沿原模型、AnimationPlayer、裁切與錨點；來源 6 個指紋鎖定，每批原像素／PNG 解碼／RES 解碼 SHA 相同。壓縮代表 128 幀 `041034_374` PASS：RES 由 9,126,354 降至 1,076,345 bytes，PNG 逐位元組不變；不能把這一組的 88.2% 當所有配方實測。已完成 68/160 批、7,352 幀，m00..m12 完整、m13 前 384 幀，**未發布部分正式 catalog**。累计 PNG 45,029,897 bytes／壓縮 RES 70,363,752 bytes，解碼頁合計 679,723,008 bytes 非同時常駐；逐頁已檢。實際資料 admission `041800_772` PASS，完整 m00 准入、缺幀 m01 拒絕。正式全套與卸裝後普通兵畫面仍待完成。

本輪 result/log（含 FAIL）已複製到 `output/site_institutions_20260913_0330/verification/` 並逐檔 SHA-256 比對；每個烘焙批次另保存自身 canonical verification，保留最新 100 批規則不變。`schedule` 缺席不再阻擋分工，仍只開一個 Godot 驗收程序。沒有 Git 提交／推送，也沒有更改正式玩家存檔。

剩餘：完成 32 配方及新實裝實景、營地／兩隊／地面口糧及超量遺留全流程、替代裝備／物流／家庭 UI 驗收、最新受控 row 私人供餐與首次啟用回歸、200 原人物交戰長跑、最終格式／UI／編輯器回歸。32 組同實裝攻守矩陣、名冊拆併 UI 與四俘虜押送已於上方新批通過。新長跑／多 case 矩陣沿原 helper 的公開 TimeoutSeconds 參數另設有界較長驗收（helper 預設 120 秒，既有 20／25 秒只是短測選值）；不修改 helper，不放寬或重標舊短測 FAIL。不能把正在產生的測試或 asset 當成已通過。

## 2026-09-13 03:10 最新收束：本輪定向驗收完成，整份計畫仍未完成

本段取代下方 02:40 的即時狀態，但不刪除當時失敗。證據已複製到 `output/site_phase_movement_20260913_0200/`，每份 `result.json`／`combined.log` 複製後比對 SHA-256；保留 PASS 與 FAIL，沒有提交或推送，也未改正式玩家存檔。

### 本輪已解決並驗證

- 原玩家／NPC／軍隊的新移動共用 0.38／0.18 秒和原二次 ease-out；四方向走／跑、移動中存讀及舊在途曲線通過。確切查到普通兵 float32 進度停在 `0.999999642372131`，比原人物晚結束；改用 float64 和同樣的一奈秒終點清理，不放寬接觸步誤差、不改動畫速度。
- 可變名冊移轉／拆併保留原人物、能力、HP／KO／疲勞、貨物與裝備引用；本 Site 仍是兩隊、總 NPC 不超過 200。原玩家入退隊、幹部順位、指揮與軍隊保存回歸通過。UI 已有合格玩家指揮者「拆出選取隊員」，完整補充／合併的操作面板仍不是全數完成。
- 兩位原女性的拆併圖像已逐張檢視。舊呈現器只有旗標改成戰鬥、同名 idle 未刷新持握，導致其中一位仍收盾；現由原裝備顯示擁有者更新持握／收納並清除相應幾何快取。最終圖前後先驗原 editor／原配裝／實際持盾，再截圖，最後才查幾何；普通男兵拆為首列仍是原男體。驗收圖統一白色濾色以排除原隊長金色高亮，沒有修改正式高亮規則。
- Site 工作／時鐘與人物改為同一 120 Hz 小步，不再先结算整個和平大幀。供餐／訓練接原資料，交糧期限到達後先結當步命中再檢查並扣原糧。新測試證明 0.1 現實秒大幀在中途 0.05 秒首次受擊時，只推進 `3.0 + 0.05` 遊戲秒；Site、隊伍、私人供養三者一致。工作、暫停、原疲勞 80／50 及保存回歸皆通過。
- 玩家入退隊不洗掉飢餓／已吃餐份、不自動搬走共有或私人糧。正式完整 save → load → bind → 繼續共同時間已通過。非空供養群組必須與保存 Site 時鐘同步（1e-5 秒容差），不能讀入永久落後的缺糧紀錄；空供養擁有者可保留開封食物並合法向前對齊，不能跳過有人的飢餓時間。
- 同一 Lab 的 200 名原 NPC，180 小幀與 3 大幀推進同一 90 遊戲秒、跨六小時餐期，完整人物／餐份／訓練／疲勞結果一致，含實際缺糧死亡、80 停訓輪休與兩種暫停。這是制度定向回歸，不是戰鬥長跑或 FPS。
- 物資資料層最終校驗通過：原物件移轉、唯一持有人／版本、容量、死者剩餘物資容器 API、空包清理、格式遷移、有人／空供養時鐘、歷史人物 ID 與 8 MiB 舊檔保護。原 StringName 資源鍵被誤拒的問題已修正，未放寬數量或所有權限制。

### 最終證據與驗收界線

| 範圍 | 最終／代表執行 ID | 結果 |
| --- | --- | --- |
| 原玩家／NPC 共同移動 | `024709_241` | headless PASS，1.665 秒 |
| 原人物對普通兵，四方向走／跑、舊新移動存讀 | `024432_052` | headless PASS，7.305 秒 |
| 名冊原人物移轉／拆併 | `024711_198` | headless PASS，4.658 秒 |
| 生效段前次幾何／環境損血／潰退入口 | `024716_126` | headless PASS，4.573 秒 |
| Controller 交糧、入退隊、同一步命中取消 | `024226_579` | headless PASS，6.035 秒；另有後續完整 Lab 測試 |
| 完整 Lab 時鐘／訓練／入退隊／保存讀回／首次受擊餘幀 | `030508_823` | headless PASS，6.419 秒 |
| 200 人制度正常幀／慢幀、餐期及缺糧死亡 | `025441_365` | headless PASS，7.614 秒 |
| 物資與供養最終保存界線 | `025144_360` | headless PASS，7.302 秒 |
| 原工人輪休／工作疲勞／最終保存大小 | `025835_248`／`025838_478`／`025841_309` | PASS，3.069／2.729／2.468 秒 |
| 原玩家入隊／幹部順位／指揮保存 | `030557_173`／`030603_246`／`030716_777` | PASS，5.763／4.564／5.377 秒 |
| 兩位原女性拆併、持盾與男兵首列 | `025858_919` | GPU PASS，13.281 秒，2 張圖已檢視 |
| 原工作輪休面板 | `025941_475` | GPU PASS，13.398 秒，1 張圖已檢視 |
| 原供糧面板／實際相鄰交付 | `030436_362 --panel-only` | GPU PASS，16.830 秒，1 張圖已檢視；不是完整存讀 GPU |
| 編輯器有界啟動檢查 | `030122_462` | PASS，9.783 秒、exit 0；有既有 nested project 與 quit-after 的 scan-thread-aborted 提示，不等於全部素材完整重匯入 |

表內短寫 ID 前綴均為 `20260913_`。原工人與軍隊移動保存斷言改為僅容許十進位 JSON 的 float64 尾差（小於 1e-15）；實際量得工作進度尾差約 `6.94e-18`，存讀後禁止增長、人物 ID、數量、占位及受擊先後仍嚴格驗證。不是容許丟進度、放寬遊戲計時或一幀誤差。

### 可操作入口與目前首版規則

右側「隊伍口糧／訓練（主動啟用）」選擇隊伍；玩家攜帶真實食物、停在選取的同陣營隊員相鄰可達格後交糧，每批 1–30 日份，作業 `2 + 0.2 × 日份` 有效遊戲秒，完成才扣料。收糧代表共用工作疲勞；玩家不被強制停工，NPC 到 80 後不接新工作、安全降到 50 再接。训练按原規則每遊戲小時基本 +1，再乘實際參訓比例、教練與疲勞效率；玩家下令仍須是原隊伍當場合格指揮者。

供餐基本每人每日 1 日份、每六小時一餐；缺糧超過 48 等效小時才逐步扣 HP，足食立即停止飢餓傷害但不回血。士氣、潰退與恢復公式、指定食物順序及所有其餘首版規則仍見 `SITE_COMBAT_INSTITUTIONS_V1.md`，沒有另改成自動徵糧、即時回血或強制接管玩家。

### 尚未完成與下一切片

死亡／昏迷搜刮仍只有已驗證資料 API，尚未接完整生命週期、原動作／地面選取 UI、逐槽卸裝外觀及防護同步；不能宣稱 NPC 已可搜刮。俘虜／封存／贖回／逃脫、家族接續、正式領裝制度、多人工隊伍、完整三者攻守／盾擋／友軍／移動目標矩陣及最終 200 人交戰長跑仍未完成。此輪無正式資產改動、無批量烘圖；32 組缺裝圖集的 3.127 GiB 是下方記錄的未壓縮算術估算，不是實測。

下一切片仍是「一名真實 NPC 失能 → 原物品轉移 → 對應缺裝畫面／碰撞 → 醒來／再次失能／存讀不補裝」，先量代表配方，沿用原動畫／圖集／單一共用查詢來源。下方矩陣缺口保留；早期四方向 idle 及部分 guard PASS 不能替新增後的完整矩陣背書。

## 2026-09-13 02:40 本輪中途進度（歷史保留）：名冊、共同移動與供養／物資基礎

**整份計畫仍未完成。** 本段按截至 02:40 已讀到的程式與既有 `result.json`／`combined.log` 更新；只說明已落地範圍及各次執行當時的證據，不把後續修改算進較早 PASS。最新定向重跑、完整 GPU、存檔整合及本輪證據歸檔仍由主線續補。下方 01:10 與更早段落完整保留為歷史批次，其「未實裝／待授權／沒有 schedule 因而不分工」不代表本段最新狀態。

使用者已同意無 `schedule` 時使用原生子代理狀態／等待；`AGENTS.md` 已明列不要求不存在的工具，不以週期自動化代替看門計時。Godot 仍用未修改的標準有界執行器並串行啟動，保留最新 100 次已完成紀錄及所有未完成紀錄；下列失敗不得刪除或改標 PASS。

### 已落地的實作，不等於完整玩法驗收

- **變動名冊與原人物引用**：`TerrainArmy` 新增 `transfer_members_to()`／`split_members_to()`／`merge_into()`，仍預設部署 100 人，實際列數可變，Site 總軍隊列上限維持 200。每列保存穩定 `person_id`，`combat_identity()`／`index_for_identity()` 對應原人物；玩家仍是原角色引用，專用 `PLAYER_MEMBER` 哨兵與實際人物 ID 分離。舊固定百人快照有明示遷移，新快照缺 ID、重複 ID／隊伍 ID 或總數超界拒絕。Site 分配器保留單調身分，不能靠清隊重用曾持有遺物的人物 ID。
- **拆併不生成人物**：移交原 combat row、HP／KO／疲勞、物資與實装引用、任職／能力及原格位；共同訓練按實際存活成員加權，死者不灌入新訓練權重。權限、停止狀態、陣營及容量等先驗證，供養移轉 hook 成功後才提交名冊。兩名原女性的既有呈現器可隨原人物移交，普通男性可成為第 0 列，不以變性、丟棄女性或每人新增骨架解決索引變動。這些 API 已有定向資料測試；最終 UI／供養／保存與兩女性拆併畫面仍待一起重驗。
- **共同移動時鐘**：原玩家／NPC 改由共同動作小步推進已提交移動，軍隊新步共用 `CharacterCombatTimings` 的 walk 0.38 秒、run 0.18 秒與原 QUAD EASE_OUT `t * (2 - t)`，畫面及接觸地面位置取同一進度。原快照缺少移動物件的角色保留舊剩餘段；軍隊舊 0.24 秒／smoothstep 在途步用明示 `move_curve` 標記保留原曲線及時長，到達後才採新規則，不在讀檔半途改速。原人物四方向定向測試已有 PASS，但普通兵對照最新在移動完成旗標斷言失敗，尚不可宣稱三者完整移動公平性完成。
- **生效段與環境損血**：普通兵首次有效武器掃掠不再帶入前搖的上一幀輪廓，生效窗外清空前次幾何，沿用原角色生命週期。環境缺糧封包仍走原死亡／取消動作入口，但非致命缺糧不製造戰鬥事件、有效武器受擊版本或救助中斷。潰退拒絕新的主動 ATTACK／PURSUE／MOVE；HOLD／RETREAT 及無退路守位自衛保留。定向生效窗測試已有 PASS，不代表完整盾擋／友軍第一接觸矩陣。
- **供餐與訓練 controller**：`SiteSustain` 是純公式，狀態仍置於原 `TerrainData.site.team_supply`／人物；`SiteController` 已接相鄰玩家向實際隊員交糧、每批 1–30 日份、完成才扣原行囊、取消／來源改變／有效受擊／容量／暫停守衛，不自取營地庫存。UI 已加入選隊、份量、交糧／取消、合格玩家指揮者訓練入口。實際供養被明確啟用後才產生餐份紀錄；共同訓練讀原列、實際武器、原疲勞及 80／50 輪休，缺糧／不安全／移動／失能等不得增長。原單一工作 NPC 的 80 停工、50 恢復亦已接原生產與保存；尚不是完整多人採集／建造工作队伍。
- **供養後續整合仍待驗收**：玩家原入退隊與 `person_supply` 的已吃餐份／飢餓歷史移交、拆併餐份不複製、收糧者不重複計訓練／休息、HOLD 潰退有界重查，以及交糧期限到達後等待同一步碰撞結算再原子提交，均已有接線，但晚於早期 controller PASS。交付進行中的保存明確 BUSY；清除測試隊伍保留實際共有糧，不能消失或轉給玩家。完整共同時鐘／保存讀回／UI 實景尚未以最終程式驗收。
- **新物資帳本僅是資料基礎**：`SiteRuntime` 已有共用 `item_definitions`、逐件 `item_records`、持有人 `item_state`、`transfer_items()`、`leave_ground_loot()`、空容器回收及格位索引。轉移共用原 cargo，檢查來源／接收版本、同件去重、原持有人及容量，提交時同步扣原物、改 holder 與實裝槽；初始化／舊檔空帳本遷移不由外觀查詢補物。`SiteStore` 已接物品唯一位置／裝備槽、人物身分及供養欄位校驗。早期資料測試通過不代表實際死亡會留下遺物、玩家能搜刮或畫面卸裝；最新新增孤立供養保存案例仍失敗，不能以舊 PASS 覆蓋。

### 截至本段已確認的執行證據

紀錄均位於 `.godot-temp/godot_verify/<run ID>/result.json` 及同目錄 `combined.log`。此表不是最終回歸清單；沒有列出的後續測試不得推定已跑。PASS 列均正常 exit 0，FAIL 列保留執行器原判定。

| 檢查／當時範圍 | 結果／秒數 | run ID |
| --- | --- | --- |
| `site_sustain_test.gd` 純供養／訓練公式 | PASS／0.568 | `20260913_021128_542` |
| `site_actor_movement_test.gd` 原玩家／NPC 四方向、暫停、曲線與舊段恢復 | PASS／1.670 | `20260913_021344_780` |
| `site_army_roster_test.gd` 當時名冊移轉／拆併資料 | PASS／5.761；早於後續供養 hook／新增跨隊保存斷言 | `20260913_021448_485` |
| `site_loot_storage_test.gd` 早期物品原子轉移／唯一性／資料容器 | PASS／6.677；不是最新供養整合結果 | `20260913_021535_196` |
| `site_work_rest_test.gd` 原單一工人 80／50、任務／貨物／保存保持 | PASS／3.078 | `20260913_021649_254` |
| `site_combat_role_matrix_test.gd --replace=target --direction=0 --mode=guard` | PASS／18.567；單一 GPU 組，不是完整矩陣或本段新增畫面審查 | `20260913_021714_290` |
| `site_controller.gd --check-only` | PASS／1.343；只有語法／載入，不是流程 | `20260913_022852_110` |
| `site_team_sustain_flow_test.gd` 原 controller 交糧／取消／受擊／容量／訓練／環境死亡／共有糧保留 | PASS／5.566；不涵蓋其後入退隊、同一步提交等新增斷言 | `20260913_022902_234` |
| `site_army_active_window_test.gd` 生效窗前次幾何、潰退命令與環境損血 | PASS／6.165 | `20260913_023036_567` |
| `site_army_movement_parity_test.gd` | FAIL／20.113；第 48 行移動完成斷言後逾時 | `20260913_023051_923` |
| `site_loot_storage_test.gd` 新增保存整合 | FAIL／20.109；第 97 行斷言後逾時 | `20260913_023211_532` |
| 同上，拆出具體診斷後 | FAIL／2.557，exit 1；孤立供養保存被「隊伍庫存／訓練指令」校驗拒絕 | `20260913_023857_263` |

另保留本輪較早失敗：`021155_967` 的 `valid_duration` 型別推斷錯誤及逾時；`021421_127` 雖腳本 exit 0，卻把 PASS 標記誤傳成預期檔案而被執行器判 FAIL；`021752_816` 的未完成 controller 缺 `_chosen_supply_team()`，雖 exit 0 仍有真實 parse error；`022638_120`／`022725_227` 的 `result`／`offset` 型別推斷錯誤。上述短寫 run ID 均加日期前綴 `20260913_`；不得把 exit 0、印出腳本 PASS 或寫出圖片單獨當成執行器通過。修正後的定向 PASS 只取代對應故障，不替後续新改動背書。

早期物資測試 `021535_196` 另實際量到純資料容器：1,000 個保存 162,055 bytes、保存 29.964 ms、讀回 531.796 ms；9,000 個保存 1,474,055 bytes、保存 359.119 ms、讀回 1,373.704 ms。這些是該早期 fixture 的帳本／保存量測，不含戰場碰撞、地面標記、角色缺裝、持續 FPS，也不是最新供養校驗後的回歸證據。

### 本輪仍未完成：搜刮生命週期、逐槽缺裝與人物制度

1. **死亡／昏迷搜刮尚未接成玩法**：仍須由原死亡／KO 入口、原動作及權限／距離／中斷守衛觸發真實轉移；死亡只轉一次，昏迷扣原持物，醒來／再次倒地／讀檔不補裝。尚缺地面容器可選與可操作 UI、搬運回收工作、實際缺裝同步、同一步死亡／醒來／搜刮競態及全程保存驗收。已有 `leave_ground_loot()` 不等於這些流程完成。
2. **普通兵逐槽缺裝呈現與判定尚未實作**：目前仍為整張合成圖集。標準配裝有武器、盾、甲、內襯、靴五個非空可取槽，任意獨立拿掉需 `2^5 = 32` 種有／無組合；頭盔／披風原本空。不能只做五張單件缺失圖就宣稱連續搜刮完成，更不能帳面卸甲而畫面仍全裝。昏迷者醒來還需相同缺裝的 idle、移動、攻擊／格擋、受擊、起身與救助，不是只烘倒地。
3. **缺裝資產的最小路線與負擔邊界**：沿用 `bake_terrain_army_soldier.gd`、原模型／AnimationPlayer／`CharacterRenderContract`、既有裁切／地面錨點／打包；配方必須蓋過目前 baker 每個 clip 強制選武器／盾的設定。现有圖集 4096 × 6404、1,080 幀，若直接複製 32 套即 34,560 次擷取，RGBA8 算術為 `4096 * 6404 * 4 * 32 / 1024^3 = 3.126953125 GiB`，約 **3.127 GiB**；這是未壓縮像素估算，**不是 GPU 記憶體、烘焙時間或 FPS 實測**。應先用無武器、無盾、無甲、全部缺失等代表配方做小樣，裁到目前真正可達的長劍／徒手動作並量測分頁及常駐預算，再決定完整 32 組，不能一次把全部完整圖集常駐。
4. **同份實裝資料驅動原攻守 owner**：從 `item_state.equipped` 推導呈現配方，不增加第二份物品所有權。`HumanCharacter3DEditor.select_part_by_id()`／`restore_appearance()` 已有原生隱藏／收納／身體與內襯更新；`TerrainWeaponCollision.weapon_shapes()` 支援 `attack_unarmed`，`shield_shapes()`／`armor_at()` 已看真實可見配裝。普通兵共用 `terrain_army_contact_source.gd` 的快取鍵仍缺配方、武器查詢仍固定 `walk_slash`，需加入配裝鍵並保留合法再裝備會用到的原部件；Army 動作／傷害 profile／格擋家族／圖集鍵必須一起讀同一實裝。仍保持一個共用非渲染查詢來源，不改成逐人活骨架。
5. **不另造未必要的 renderer**：現行架構已移除舊 Paper Doll，未找到可直接沿用的裝備顏色／深度合成器；benchmark 的 `SpecialRigidEquipmentBatchRenderer` 只處理部分剛性槽及另一套角色插槽，不能直接代替普通兵衣甲／靴。若有限配方實測仍超預算，再考慮各槽顏色＋深度＋完整身體底層；手臂交錯、前後盾牌、遮住的身體表面及收納武器都需要正確遮擋，單純平面圖層排序不足。本輪只有唯讀查核，沒有新增 renderer、重烘正式資產或這項視覺驗收。
6. **俘虜與人物接續仍未完成**：四秒拘束、唯一拘押／有效看守、俘虜處置與贖回、封存裝備包、家族继承及不複製人物的接續、正式國家領裝／編制仍只是已授權規則，不能由物品帳本或救助動作推定已實作。完整多人工作隊伍、跨角色實際攻守／友軍第一接觸矩陣、最終存讀檔和 200 人場景／更大資料標記效能亦仍需分段驗收。

最小下一步：先收束本輪移動完成旗標與物資／供養保存失敗，重跑最终共同時鐘／名冊／供養整合及兩女性拆併 GPU；再做一組真實 NPC 失能 → 原物品轉移 → 對應缺裝畫面與碰撞 → 醒來／再次失能／保存讀回的有界切片。不要在缺装資產仍不存在時，以禁止普通兵取裝、假裝卸甲或只完成資料層宣稱全部 NPC 搜刮完成。

## 01:10 批次摘要（歷史保留）

更新：2026-09-13 01:10。**本批完成固定長劍普通兵連續碰撞取樣、共用出招瞄準及凍結瞄準點保存；200 人慢幀與完整 GPU 場景正常退出，原共用疲勞及存檔回歸通過。整份計畫仍未完成，三者完整攻守／移動公平性、多人工作隊伍與輪休等仍待實作／驗收。原第 10 節制度已新獲授權，首版規則已告知並写入專文件，不再以缺少授權作阻擋，但尚未實装。**

設計依據：`SITE_COMBAT_DESIGN.md`。裝備與動作盤點：`SITE_COMBAT_3D_ASSET_GAPS.md`。
本輪使用 ponytail 原則沿用 TerrainLab／TerrainTestCharacter／TerrainWeaponCollision 及既有攜帶存貨；沒有恢復舊 World／Battle 系統，也沒有新增每兵 3D 節點。

## 2026-09-13 NPC 搜刮：僅補入計畫

- 使用者提出死亡／昏迷 NPC 可搜刮物資與裝備，並詢問效能。規則已寫入 `SITE_COMBAT_INSTITUTIONS_V1.md` 第 3.1 節及 `SITE_COMBAT_DESIGN.md` 第 6.1 節：死亡一次轉存，昏迷扣原持物；醒來／再倒下不補裝，實際卸甲失去防護；回收工作共用原疲勞、容量與中斷規則。
- 本次查核現況：角色與軍隊受擊入口已有 HP／KO 分流，但沒有遺物轉移；角色 appearance 與軍隊 `standard_soldier_v2` 固定配裝不是逐人物品庫存。`SiteResourceView` 有按列批次呈現可擴充，`SiteStore` 有原子保存及 8 MiB 讀檔上限，但尚無遺物／實裝所有權校驗。不能只加掉落清單就宣稱搜刮完成。
- 沿用原人物／TerrainData.site／SiteRuntime／SiteStore，規劃稀疏地面容器與共用物品轉移，不加逐件物理或每兵骨架；先完成實際配裝與缺裝呈現／判定，再接搜刮、封存包及繼承。效能驗收區分 200 人實際場景與 1,000／9,000 容器資料／標記壓力測試。
- **本次只有文件變更與接點核對，沒有新增搜刮 runtime、Godot 驗證或效能測量；整份計畫仍未完成。**

## 2026-09-13 連續碰撞與共用瞄準

- 普通兵碰撞改用原 AnimationPlayer 的精確動作時間，含當次訓練／疲勞映射與既有下向瞄準權重，不再從低頻圖集幀取得命中輪廓。畫面仍選原 1,080 幀圖集；沒有重烘焙或修改男女 GLB／Blender。女隊長也以精確時間同步自己的實際模型。
- 兩隊共用一個不可見的固定裝備查詢骨架，不是人物、没有 HP／位置所有權，也不渲染。兩個原女隊長仍即時呈現，99 個普通兵／隊仍共用圖集；因此現在是「兩個軍隊呈現骨架＋一個共享非渲染查詢骨架」，不能再說總共只有兩個。退出場景／清隊會釋放查詢來源。
- 瞄準點選擇抽自原玩家邏輯，普通兵按目標原身體與自身肩膀取得本次揮擊的固定點；每小步只更新原瞄準權重，不追蹤移動目標或導引飛行彈體。瞄準點放在原人物列保存，JSON 保留亞像素座標；舊快照缺項沿用無瞄準狀態，非法向量拒絕。
- 私有查詢動畫移除未穿裝備及不參與碰撞的披風 morph 軌道，不改活體角色或原資產。剛性單骨蒙皮與正交投影改用 Godot 原生批次運算，其他蒙皮／morph 保留原計算；34,839 頂點與原計算的頂點差為 0，投影最大差 0.000016852 地圖像素。
- 原生匯入動畫在非圖集時間有 0.016710708 像素差，已拒用；只有匯入網格雜湊與 raw GLB 相符，且有同體型原始動畫可私有複製時才用匯入網格加速。否則讀原 GLB。沒有把不相符的結果以放寬容差算通過。舊碰撞／護甲表保留為離線回歸參考，但不再於實際交戰載入無用的第二份全表。
- 四方向共 32 個非圖集姿態對獨立原角色，包含下向瞄準與 rescue→guard 歷史，body／weapon／shield 最大邊界差 0.001220703125 地圖像素，128 個護甲覆蓋點差 0；8 張原角色圖已檢視。另回歸原玩家／NPC 下向長劍實際互換，兩者同為 30 傷害／18 暈眩，不是完整三角色互換矩陣。
- 200 列實際命中／友軍阻擋／KO／死亡／凍結瞄準保存通過；疲勞 80 的慢幀比較含 180 小步與三個半秒幀，19.657 秒、18 次接觸；完整兩隊 GPU 24.637 秒、20 次接觸，四張部署／交戰／倒地／入隊圖已檢視。這些限時執行靠近上限，不是持續 FPS 或可任意擴到 9,000 人的證明。
- 保留本批所有失敗：推斷型別錯誤、測試把 JSON 座標當成位元完全相等、匯入動畫改變中間姿態，以及完整場景三次／慢幀一次超時。最後一次完整場景雖印出腳本 PASS 並寫圖，但未在期限內正常退出，仍列 FAIL。修正後才作定向驗證，未放寬 20／25 秒上限或改標準執行器。

本批證據見 [連續命中紀錄](output/site_continuous_contact_20260913_0100/README.md)。沒有 commit／push，沒有操作正式玩家存檔。依 ponytail 移除不再使用的執行期碰撞表載入、重用原動畫與命中所有者；未新增平行戰鬥框架。

## 新授權的制度與下一段

使用者已同意供糧／士氣、平時訓練、俘虜／繼承及配裝制度採可調首版，要求告知規則。[制度首版規則](SITE_COMBAT_INSTITUTIONS_V1.md) 已列出：每日一份口糧、六小時供餐、48 小時等效缺糧寬限；20／50 士氣門檻；原疲勞及工作 NPC 80／50 輪休；四秒拘束／救援、有效看守與實物贖回；不複製人物的繼承與原子領裝。**本段是新規則基準，並非上述制度的程式交付。**

下一段依該文件第 9 節：補三者實际攻守／友軍第一接觸與移動公平性，再接原名冊補員／拆併、多人工作／供養、後勤／士氣、訓練／輪休、拘押／人物接續與配裝。不可把只有公式、資料測試或文件的階段標為完成。下方歷史段落中的「待授權」已由本次新授權取代，其已驗證功能無需重做。

## 2026-09-13 持械移動與原素材保留

- 依 ponytail 沿用原 baker、角色編輯器與軍隊選幀入口，僅追加 combat_walk／combat_run（四方向各八幀）到候選區。發布前備份，發布後再驗證原 1,016 幀的像素、矩形、索引、metadata、碰撞／護甲資料全數保留；新幀時長及 sample_time 與原 walk／run 相同。正式圖集現為 1,080 幀、35 動作組、4096 × 6404，未改男女 GLB／Blender 素體。
- 交戰列仍存 idle／walk，畫面和碰撞共同映射持械變體；run 依既有保存的 RUN_DURATION 判斷，沒有新跑步命令、AI 政策或速度變更。移動時取消原 12 像素待機前傾位移，抵達回到持械待機。女隊長仍用原模型與動畫，不新增每兵 3D 節點；和平圖集及原收納規則保留。
- 四方向 16 姿態、32 組原玩家／NPC 對普通兵幾何比較，body／weapon／shield 邊界最大差 0.00013647876039613 地圖像素；128 個同點斬擊護甲覆蓋差為零。16 張並排圖與涵蓋新增 64 幀的兩張圖板已檢視。這些是同體型、同實裝、同原始取樣時間與地面原點的對照，不是三者連續移動／攻守公平閉合。
- 新測試以原軍隊 100 列中隊長與一名普通兵實際預約步進，驗證半途暫停、JSON 恢復、到達、疲勞 80 不被重置，以及退出交戰後原待機。GPU 7.144 秒，三張近景已檢視；保持女性隊長、99 個普通兵共用圖集。run 測試使用既有合法保存時長，不冒充新增了可操作跑步命令。
- 1,080 個取樣邊界與 1,189 組出步、圖集結構、指揮存讀檔、144 組疲勞時序及工作流程回歸皆通過。200 列起始疲勞 80 的慢幀回歸 17.511 秒、18 次接觸；完整兩隊 GPU 24.313 秒、20 次接觸，四張圖已檢視，仍僅兩個隊長即時模型。最後 editor 7.609 秒正常退出；不是長時間 FPS 或全裝備矩陣。
- 一次失敗保留：新測試將原生快照直接與 JSON 數值型別轉換後的快照比較，暫停斷言後逾時；改為原生前後比較，再獨立驗證 JSON 恢復後通過。沒有修改正式暫停規則或放寬 20／25 秒限時。標準執行器未修改，無 schedule 工具故不開背景或子代理。

本批 20 份執行紀錄（19 PASS、1 FAIL）、25 張驗收圖、4 份輪廓報告、四份原素材備份與雜湊見 [持械移動驗收紀錄](output/site_combat_move_20260913_0000/README.md)。未 commit／push，未動正式玩家存檔、LFS guard 或不相關工作樹改動。標準保留機制淘汰的舊暫存不在本批證據內，本批已複製歸檔；遇鎖定舊 shader_cache 則保留，不強制刪除。

下一個獨立切片：普通兵連續取樣與目標動態瞄準，再做三者完整攻守互換／友軍第一接觸和移動公平性；不能降低玩家精度來消除差異。疲勞已接原手工及一名 NPC 工人，但完整多人工作隊伍、輪休、供餐／士氣聯動和未定國家／俘虜／家族制度仍未交付。

## 23:35 共用人物疲勞：戰鬥與工作

- 設計已補入 `SITE_COMBAT_DESIGN.md` 第 5.6 節，資源工作計畫指向同一契約。依 ponytail 沿用原人物／軍隊資料與 Site 時間，只抽共用純公式；沒有第二份疲勞狀態、工作名冊、每兵節點或新時鐘。疲勞 >30 線性影響前後搖與作業耗時，上限 +30%；不改生效段、傷害、護甲、HP 或已累積訓練。
- 攻擊／格擋／戰鬥奔跑及真正有效工作按初始速率累積，停止作業後連續安全 30 遊戲秒才恢復；8 格合法敵方可達路程或原投影的在途彈體阻止休息。一次小步內只共用地形威脅查詢，沒有跨步快取。工作缺工具、貨滿、占地受阻、缺投入、缺水與完成貨物等候搬取不重收疲勞。預檢沿用原 harvest 守衛，材料／產物仍由原 Runtime 提交。
- 玩家／工人面板顯示疲勞和耗時增幅，軍隊原摘要顯示存活成員平均疲勞／疲憊人數。存檔保留疲勞、休息等待和當次出招倍率；任職、攻擊中途訓練改變與一般 reset_combat 不刷新疲勞。工作與人物均完成當次結算才自動保存，Site 暫停與 SceneTree 暫停都凍結。舊人物缺欄位採 0、新圖／沒有角色快照的 format 1 不繼承前圖疲勞，非法值拒絕。
- 144 組動作／訓練／疲勞時序、跨門檻工作積分、三種人物攻擊累積、休息中斷、路徑／彈體、暫停、工作轉戰鬥、人物／軍隊存讀檔與自動保存邊界通過。200 列全員起始疲勞 80，比較 180 小步與三個半秒幀，疲勞／休息／當次倍率及原傷害／指揮／去重／占格一致，18 次接觸，17.997 秒正常退出。
- GPU 工作流程 13.159 秒，兩張面板圖已看；疲勞 100／訓練 60 的長劍右向兩個延後時刻，以及原持械待機／防禦四姿態、兩名真實角色對普通兵輪廓通過，最大邊界差 0.00013647876039613 地圖像素，32 個護甲點差為零。這是取樣／時間映射，不是連續三者攻守公平閉合。另以注入的穿越／偏離彈道對實際身體投影驗證休息守衛，不冒充實際傷害。完整兩隊 GPU 回歸 24.077 秒、20 次接觸，四張圖已檢視；不是長時間 FPS。
- 四次失敗均保留：測試呼叫不存在方法、測試任務缺 message；逐人重算路徑導致慢幀逾時，改為同一步每陣營一次多來源查詢；下一次揭露測試重置軍隊卻沒有重置 Site 交戰時間，補正共同起點後通過。未放寬 20／25 秒限時或安全／碰撞規則。全工作樹差異檢查因不相關 Blender LFS 暫存寫入權限拒絕而停止；改做本輪文字程式／文件的範圍檢查通過，沒有改 LFS guard。

完整 22 份紀錄（18 PASS、4 FAIL）、10 張最新驗收圖與輪廓數據見 [疲勞驗收紀錄](output/site_fatigue_20260912_2330/README.md)。全程用未修改的標準 Godot 執行器；無 schedule 工具，遵守專案計時限制而未開背景或子代理。仍未 commit／push，沒有操作正式玩家存檔或重新輸出角色／軍隊資產。

23:35 當時的下一段是普通兵連續取樣／移動持械與動態瞄準；其中持械移動已由上方 2026-09-13 續作補齊。疲勞先接既有一名 NPC 工人與手工入口，完整多人工作隊伍、輪休、供餐和士氣聯動未交付。其餘未定國家、俘虜、家族與保存政策不因本次疲勞授權而自動定案。

## 22:23 持械待機接入與原素材保留

- 沿用原 baker 與角色編輯器，先從原 manifest 恢復實裝，僅生成持械 idle 的四方向各四張到暫存區。驗證原 1,000 幀的像素矩形、metadata、碰撞／護甲記錄完全相同後，備份並更新正式 PNG／RES／JSON／collision.bin。現在 1,016 幀、33 動作組、4096 × 6031，沒有重做男女 GLB／Blender 素體。
- 軍隊交戰列仍保存 idle，呈現與碰撞一同讀 combat_idle。兩個女隊長按 manifest 的 pose 映射回原 idle 動畫並拔出裝備。玩家／NPC 唯讀查詢現有 has_combat_armies，在軍隊交戰模式維持持械；離開該模式仍沿用原三／四格收納遲滯。未自動出招、舉盾或改生命值，未改存檔格式。
- 四方向 16 姿態／32 組即時角色對照，實際 body／weapon／shield 邊界最大差異 0.000172633489 地圖像素，128 次同點斬擊護甲查詢差異為零；包含持械待機進出、揮劍與防禦。原素材擴充回歸另有 52 姿態逐點比較，52 張新驗收圖逐張檢視；不是連續動作或全裝備驗收。
- 1,016 筆取樣時序（原 idle 按同時刻持械 alias 檢查）、1,189 組出步、6,362 次碰撞篩選對照、200 列慢幀、軍隊／人物存讀檔、舉盾救助、editor 全部正常退出 PASS。完整兩隊 GPU 24.468 秒、180 小步、20 次接觸，仍只有兩個隊長即時模型；不是 FPS。
- 選兵接觸 GPU 23.693 秒，劍命中身體扣 15 HP／12 stun；同陣營箭矢再扣 15 HP，僅耗一支箭，未轉打舊 NPC。待機盾由背負改為手持後，這個固定角度的結果與前批背負盾擋住不同，沒有改傷害公式。救助獨立 GPU 21.412 秒，明示注入 KO、拒絕敵軍救助後設同陣營、四秒甦醒／起身，不把注入失能算實戰傷害。
- 三次失敗保留：初次追加實裝比較拒絕（未改正式資產）；單一選兵全案例在完成斷言後退出逾時；救助拆分漏設原友軍前提，正式敵軍救助守衛正確拒絕。分別針對性修正，未放寬 25 秒 GPU 限時、碰撞或救助規則。標準執行器未修改，無 schedule 工具故未用背景程序或子代理。

23 份執行紀錄（20 PASS、3 FAIL）、52 張圖、4 份輪廓報告、素材備份／雜湊及重跑方式見 [本輪驗收紀錄](output/site_combat_idle_20260912_2210/README.md)。未 commit／push，未操作正式玩家存檔。**下一段仍須處理連續取樣／目標瞄準及移動持械，再進行三者完整攻守互換與友軍第一接觸；不能降低玩家精度或把本輪待機完成當整份計畫結案。**

## 21:50 普通士兵時序與同姿態對照

- 直接依原 manifest 的 `sample_time` 選擇當刻之前最後一張姿態，修正單次動作提前選幀及精確取樣時浮點落回前一幀；循環姿態由原末張時間判定，涵蓋 run／無盾防禦等原取樣。沒有修改正式素材、動作時長或碰撞範圍。
- 依 ponytail 將既有玩家平滑出步曲線原式移至 CharacterCombatTimings，普通士兵共用，保留原 31.5 像素與通行／占位守衛。訓練 0／60／10,000 的平地、前格合法長劍案例，1,189 次比較一致；不宣稱任意地形／目標距離已一致。
- 實際男性玩家／切換成同體型的 NPC／原烘焙普通士兵，依既有標準皮甲、內衣、皮靴、長劍與盾牌比對 idle 第 0 張、walk_slash 第 4／6 張、guard 第 1 張及四方向。32 組角色對照的輪廓最大差異 0.000172633489 地圖像素，128 個同點斬擊防護查詢完全相同。16 張並排圖逐張檢視；是同原始姿態，並非連續出招或全護甲矩陣。
- 原時序斷言確實抓出問題，修正後通過；初版輪廓測試把共線頂點差異誤當外形差異，改為雙向到實際多邊形邊界距離後通過。正式碰撞容差未改，失敗／逾時未混算 PASS。
- 最終取樣／出步、200 列接觸、200 列慢幀、指揮存讀檔、舉盾救助、既有規則、同步接觸及 editor 全部 PASS、exit 0。四方向 GPU 各 17.137–17.322 秒；完整兩隊 GPU 23.269 秒、20 次實際接觸；玩家選兵／劍／友軍箭矢／救助 GPU 24.145 秒。皆原技能前景執行，GPU 25 秒、headless 20 秒硬限時。
- 完整兩隊仍 200 列、兩個隊長即時模型，普通士兵未新增 3D 節點。這次取樣修正改變接觸時點，因此不以查詢數變化宣稱效能提升；不是 FPS。玩家劍／箭確有接觸，但目標盾甲使 HP 保留 100，不能視作無護具友軍傷害已驗收。救助測試仍含既有注入的失能狀態。

完整命令、14 份最終 PASS、失敗診斷及 23 張新圖见 [本輪驗收紀錄](output/site_soldier_parity_20260912_2150/README.md)。本輪不修改正式資產、LFS guard 或存檔格式，未 commit／push。

**21:50 當時的剩餘差異**：普通士兵持有離散原始姿態，玩家／NPC 在 120 Hz 即時取樣；向下近戰的隨目標手臂瞄準未共用；戰鬥待機的拔出裝備與普通 idle 收納圖集也不同（此待機項已由上方 22:23 續作補齊）。不能把同原始姿態 PASS 當成三者完整攻守命中／盾擋公平性已結案，也不能降低玩家取樣精度來消除差異。其餘未完成範圍維持下方清單。

## 21:22 武器／騎乘與攻守互換驗收

- 依 ponytail 沿用原 HumanCharacter3DEditor 動畫切換入口：單次攻擊關閉 Loop 後，idle／walk／run／guard／無盾持武器或長槍防禦／騎乘持續姿態恢復循環；攻擊、装填、舉收盾與破防、倒地、救助、起身仍維持單次。沒有改傷害、碰撞範圍、正式模型或動作時長。
- 真實男女模型各自同體型、同實裝及初始姿態，交換 TerrainTestCharacter／TerrainTestNPC 兩者攻擊身分；使用原 TerrainLab 120 Hz 結算、武器網格掃掠、骨骼肢體與臉部網格範圍。地面長劍／長槍／戰斧／戰鎚／匕首／弓／弩／徒手共 128 案例，馬上長劍／長槍對步行者另 32 案例；合計 80 對結果完全一致，實際唯一接觸與 HP 封包相符，無強制傷害。遠程每次只扣一彈、飛行終了不殘留，收招視覺回原格。
- 基準卸除頭盔、身甲、靴、披風與盾，保留基本內衣。不是全護甲、友軍阻擋或普通烘焙士兵驗收；騎乘不包含馬匹受傷／占地／推擠或騎兵衝鋒。
- 男性向下射箭最初出現 30／15 傷害差異；診斷證實相同射出位置，但攻擊歷史使一方 idle 循環、另一方停在末幀。修正後全部矩陣皆以最終正式程式補驗，不混用修正前結果。初次 NPC 體型設定及弓箭分組逾時屬 fixture 問題，分別透過原選單實際換體型與更小批次收束，未放寬守衛。
- 固定 Godot 4.6.2 mono 71f334935／D3D12／RTX 5060，未修改的技能驗證器，34 次 GPU 正常桌面核准前景執行，25 秒硬限時；全部 PASS、exit 0，12.722–21.542 秒。80 張新圖逐張檢視；截於生效起點後 0.1 秒，不保證就是碰撞瞬間，垂直方向有遮擋，不宣稱完整動作穿模驗收。
- `site_combat_rules_test.gd` 增加 110 個單次→持續姿態循環預設，連同既有傷害／動作鎖／救助／互擊規則 PASS、exit 0、1.699 秒（`20260912_211232_234`）。Editor PASS、exit 0、4.969 秒（`20260912_212121_886`）；headless 根憑證訊息為已知環境雜訊。

完整命令、34 組對照、4 份失敗診斷及驗證邊界見 [本輪驗收紀錄](output/site_combat_weapon_matrix_20260912_2122/README.md)。該目錄保存 40 份 result.json／combined.log、34 個案例報告及 80 張方向圖，圖檔複製逐檔雜湊一致，避免標準 100 次暫存保留規則淘汰證據。沒有背景程序或子代理（無 schedule），未動 LFS guard，未 commit／push。

下一個未完成的驗收切片仍是玩家／NPC／普通士兵同裝三者互換、護甲與友軍第一接觸，不能把這次兩人 160 案例當作全部 32 動作或完整交戰已完成。其餘名冊／制度邊界見下方剩餘範圍。

## 20:30 接觸修復與可操作交戰續作

使用者要求解決問題後繼續，不再於每次首次失敗後等待另一個「繼續」。本批每次失敗均先定位、修正程式或不符正式契約的 fixture，再採原有界驗證器檢查；未放寬碰撞、傷害、起身或逾時守衛。依 ponytail 延伸原擁有者，未建立第二套戰鬥／角色生命狀態，未動已補齊的正式模型、32 動作／1,000 幀圖集或碰撞資產。因無 schedule 工具，沒有派背景程序或子代理。

### 本批實作

- **接觸效能**：TerrainWeaponCollision 使用實際凸掃掠及身體多邊形的 AABB（保留 0.001 像素數值裕量）排除明確不相交者，再走原精確交集、八輪時間細分與命中點。軍隊另先檢查同幀 body／shield 實際範圍；女隊長仍取自身即時幾何。6,266 案例逐 Dictionary 比較原演算法，包括全部 1,000 姿態、邊界、盾牌、等時與長距離掃掠。沒有縮小武器、改格距命中或跳過動作步。
- **玩家入隊與指揮**：100 列 NPC 加對原玩家的名冊引用，明示 101 名成員但沒有第 101 列 NPC 或替身。保留位置、HP、裝備與自主操作；加權納入新兵訓練一次。同隊攻擊讀同一 training，玩家稀疏指揮能力存原人物。角色索引 100 可依相同在場／資格規則接任，不自動出招或搬動。正式與代理權限、五種命令、順位草稿上移／下移與整次套用重查已接入 UI。
- **任職先後**：`record_officer_service` 接受既有隊內人事事件，保存現任期間先後；同批按稳定人物 ID，未列順位者補尾，死亡／離任移除，重新任職補尾但保留能力，暫時 KO 不重設。舊快照以原順位作一次固定回退。沒有增加玩家任意任免或國家政治流程。
- **直接選兵**：點軍隊格位選中人物而不放置玩家，按 Space／按鈕用當前武器出招。攻擊目標以穩定 ID 保存；釋放箭矢時重新查原人物／军隊幾何，不回退瞄準舊 NPC，不複製目標狀態。實際命中仍經共同幾何、盾牌與護甲查詢。
- **舉盾與施救**：普通士兵使用正式 guard_raise／guard／guard_lower 動作與鎖；HOLD 自衛及無近敵時相鄰救助已接線。玩家／士兵均可救助原人物狀態，四秒後呼叫同一甦醒入口，保留 HP／被俘與起身鎖，原格被占用則不強行站起。人物與軍隊之間施救者唯一，受有效擊中、移動／新 NPC 命令或關係失效即中斷。倒數、關係與姿態可保存續接，JSON 整數與格位哨兵正規化；四秒浮點尾差以十億分之一秒裕量收束。

### 最新驗證

固定 Godot 4.6.2 stable mono `71f334935`，執行檔 `C:/Users/Nekki/Godot_v4.6.2-stable_mono_win64/Godot_v4.6.2-stable_mono_win64_console.exe`。沿用未修改的技能 `verify_godot.ps1`，命令形狀 `[--headless] --path <專案> --script res://scripts/tests/<測試>`；headless 硬限時 20 秒，GPU 正常桌面核准、25 秒。下表全部 PASS、exit 0；headless 根憑證訊息為已知環境雜訊。

| 測試 | 秒數 | 紀錄（`.godot-temp/godot_verify/` 下） |
| --- | ---: | --- |
| contact_bounds，6,266 個原演算法比對 | 2.896 | `20260912_202955_898/result.json` |
| contact_sampling，同步快照／慢幀 | 1.586 | `20260912_202959_815/result.json` |
| army_combat，真實圖集接觸與未篩選路徑比對 | 4.539 | `20260912_200502_082/result.json` |
| player_army，身分／接任／保存及真實順位對話框信號 | 3.909 | `20260912_202553_461/result.json` |
| player_soldier，直接目標／共用訓練／救助讀回 | 3.916 | `20260912_201331_966/result.json` |
| army_guard_rescue，四秒／唯一施救者／中斷／占位 | 2.792 | `20260912_202243_385/result.json` |
| officer_service，任職先後／重任／舊快照 | 3.014 | `20260912_202616_542/result.json` |
| army_command_save，指揮／RNG／移動一致 | 3.456 | `20260912_202951_404/result.json` |
| army_tactics，補位／追退 | 4.101 | `20260912_202252_155/result.json` |
| combat_save，人物存讀檔 | 2.609 | `20260912_201406_701/result.json` |
| combat_rules，傷害／擊暈／動作鎖／同時互換 | 1.587 | `20260912_202620_724/result.json` |
| combat_slow_frame，200 列實際圖集接觸 | 10.672 | `20260912_202837_277/result.json` |
| army_combat_visual，兩隊＋玩家入隊近景 | 23.582 | `20260912_202645_374/result.json` |
| player_soldier_visual，劍／箭／救助／盾／起身 | 24.026 | `20260912_202134_917/result.json` |
| editor，`--headless --editor --quit-after 2` | 5.896 | `20260912_202722_508/result.json` |

本輪 GPU 合計四次通過：完整兩隊三次（首次 `194035_534`、入隊後 `195310_791`、最終表列），另一次是選兵獨立 GPU，不混作兩隊持續交戰。最終 180 個小步、2,113 次查詢、20 次接觸；前 800 次軍隊查詢 521,633 微秒，原失敗紀錄同區段為 4,579,770 微秒，約少 88.6%。完整查詢軍隊 1,424,593 微秒、即時人物部分 1,275,711 微秒。這是特定 fixture 的 CPU 區段，不是 FPS、整體加速比例或長時間效能保證。

慢幀驗證以同一初始快照重建兩隊，比較 180 × 1/120 秒與 3 × 0.5 秒，逐兵 HP／暈眩／KO／姿態時間／命中去重／格位與預約一致，指揮及 RNG 相同；暫停凍結。它包含實際烘焙碰撞，不只是空轉計時，但 headless 不涵蓋即時女隊長渲染或全武器角色互換。

所有表列 GPU 預期檔均新寫出並逐張檢視：`.visual_captures/site_army_combat/01_deployed.png`、`02_contact.png`、`03_down.png`、`04_member_and_down_close.png`；以及 `.visual_captures/site_player_soldier/01_player_sword_contact.png`、`02_player_rescue_and_soldier_guard.png`、`03_soldier_get_up.png`。最後一張是起身動作的起始臥姿，不宣稱已觀察整段站起動畫。軍隊倒地及選兵救助使用明示 KO fixture；不是所有倒地都由該場短交戰自然產生。

選兵 GPU 最初誤把「HP 必須下降」當命中條件；診斷確認人物 1299 有真實盾接觸，傷害／暈眩均可合法為零。後改為核對實際結算封包、唯一目標、HP 與封包結果一致，沒有修改遊戲傷害規則。箭矢也命中盾，驗到同陣營仍進入實際接觸結算、扣一支箭及不再瞄準遠方 NPC，**不稱為穿盾扣血或裸露身體誤傷矩陣**。另修正 fixture 推進未達正式弓第 116 幀釋放事件，以及未下 HOLD 而被補位者占住甦醒格的問題，保留正式時長与占位守衛。

### 剩餘範圍（整份計畫尚未完成）

1. 21:22 已完成玩家／獨立 NPC 男女八種武器／徒手與馬上長劍／長槍對步行目標的 160 案例基準。仍欠普通士兵加入的三者同裝互換、全護甲／盾牌／無盾格擋與友軍第一接觸、移動／任意站位、跳躍重擊入口及其餘動作整合、長時間完整 GPU／FPS；目前普通軍隊仍是固定長劍切片，未宣稱全 32 動作完成。
2. 不固定人數的實際補員／拆分／合併名冊与共用訓練流程，以及人事事件上游來源；本批任職先後接點不是完整國家任免。保持原角色與資產，不以移除難接入內容結案。
3. 第 10 節仍未授權定案的後勤／攜糧與士氣數值、平時訓練資源／成長、拘押與俘虜處置、家族繼承、配裝／編制和保存政策。既有公平供養與不回血等已確認規則保留，沒有自行補上制度。

本批表列紀錄、原逾時基準與七張驗收圖另複製至 `output/site_combat_20260912_2032/`，各 run ID 子目錄保留 result.json／combined.log，避免日後標準 100 次保留規則淘汰這些證據；沒有停用清理。

所有變更仍未 commit／push。範圍內差異格式檢查通過，沒有留下 Godot 程序；標準保留清理仍只處理最舊已完成測試暫存，被占用者警告略過，未強制刪除素材或正式存檔。下列 19:34 及更早段落僅保留歷史證據，其「仍逾時／未入隊」不是目前狀態。

## 19:34 接觸取樣範圍內共用投影與效能定位（歷史）

使用者同意針對接觸判定修正並重新驗證。本批依 ponytail 留在原擁有者：`TerrainTestCharacter.incoming_geometry()` 提取原本的 body／shield／無盾格擋幾何，`incoming_contact()` 沿用原來的精確接觸、盾牌／格擋優先順序與命中點計算。`TerrainLab` 僅在所有角色前進完成後、軍隊同步收集接觸的區段，按實例共用各人物一次投影；在傷害結算前立即清除。沒有跨步／跨畫面保存，不入存檔，不取代護甲三角形查詢；獨立人物／投射物查詢仍讀取當前姿態，遠程不能套用近戰武器格擋。

新增 `scripts/tests/site_contact_sampling_test.gd`：headless 使用計數幾何驗證真實 TerrainLab 小步入口，8 次查詢只投影一次，移動／下一步／離開批次／慢幀多小步皆正確更新；盾牌、無盾武器格擋、破防及遠程結果不變。GPU 模式載入真正男性角色，先暖表面陣列，再對同一姿態與地形各查詢 12 次：未共用 14,511 微秒、批次共用 4,371 微秒（單次局部測量約少 70%）。逐個實際身體／盾牌多邊形與掃掠，比較直接與共用路徑的完整命中 Dictionary 相等。這不是女性／全動作矩陣、FPS 或整體戰鬥加速 70% 的證据。

固定 Godot 4.6.2 stable mono `71f334935`，`C:/Users/Nekki/Godot_v4.6.2-stable_mono_win64/Godot_v4.6.2-stable_mono_win64_console.exe`。直接使用未修改的 `godot-runtime-verify/scripts/verify_godot.ps1`，遵守已更新 AGENTS 的最新 100 次完成紀錄保留規則；不改 Git LFS guard。命令形狀為 `[--headless] --path <專案> --script res://scripts/tests/<檔名>`；headless 限時 20 秒，GPU 正常核准桌面使用者環境、限時 25 秒。

| 測試 | 結果 | 秒數 | 紀錄（`.godot-temp/godot_verify/` 下） |
| --- | --- | --- | --- |
| `site_contact_sampling_test.gd`／headless | PASS，exit 0 | 1.508 | `20260912_193211_327/result.json` |
| 同一腳本／GPU 真實模型幾何比較 | PASS，exit 0 | 5.202 | `20260912_193228_484/result.json` |
| `site_army_combat_test.gd`／headless | PASS，exit 0 | 3.674 | `20260912_193322_316/result.json` |
| `site_combat_rules_test.gd`／headless | PASS，exit 0 | 1.477 | `20260912_193339_985/result.json` |
| `site_combat_save_test.gd`／headless | PASS，exit 0 | 2.421 | `20260912_193342_695/result.json` |
| `site_army_combat_visual_test.gd`／GPU | **FAIL，逾時，無正常退出碼** | 25.160 | `20260912_193402_769/result.json` |

各 PASS 紀錄均有測試完成標記，headless 根憑證訊息為 exit 0 下的既知雜訊。GPU 局部幾何比較沒有截圖，不稱為視覺驗收。完整 GPU 無腳本／解析／斷言錯誤，但逾時仍是 FAIL；只有新部署圖 `01_deployed.png` 在 18.665 秒寫出並已檢視，`02_contact.png` 與 `03_down.png` 仍缺失。

完整 GPU fixture 的測試子類分別計時既有軍隊接觸查詢與剩餘即時人物查詢，未更換遊戲流程。tick 90 在 19.101 秒，尚未進入武器生效；tick 120 在 24.341 秒，已有 800 次查詢，軍隊接觸累積 **4,579,770 微秒**，其餘即時人物部分 **539,295 微秒**。前批沒有相同分項計時，不能拿局部加速數字當整體前後比較；目前可定位主要區段是 `_collect_army_contacts()`，其內投影、逐兵掃描與多邊形交集的比例尚未分開測量。

下一個有界修正是檢查既有軍隊候選排除及共享 `TerrainWeaponCollision.contact()`：後者目前對每個候選肢體直接進入多邊形交集，可用**實際掃掠／肢體多邊形**的保守包圍盒先排除明確不相交者，再走完全相同的精確交集與接觸先後計算。需以修正前演算法逐案例比對，包含長距離掃掠、邊界接觸、盾牌和等時命中；不能改成格距命中、缩小武器、假傷害或丟棄動作步。

依 `godot-runtime-verify` 首次失敗即停規則，完整 GPU 失敗後沒有修改遊戲程式或再啟動 Godot，只做唯讀檢查、圖片檢視與文件交接。程序已結束。本批未重跑 editor、軍隊指揮保存／補位追退測試或未完成的整體矩陣。標準保留清理舊測試暫存，個別 shader_cache 被占用時由驗證器警告並略過，未自行強制刪除；正式存檔、模型／烘焙與 LFS 設定未動，無 commit／push。

## 19:23 首次體型載入修正與部署畫面（歷史）

依 ponytail 只修改既有初始化接點：`HumanCharacter3DEditor._initialize_preview()` 使用 `visual_state.body_index`；`TerrainArmy._create_visual_source()` 在 open 前設定體型，移除 open 後固定再載入女性的呼叫。不重製素材，不移除後續選單換裝／體型切換；`_load_body_model()` 仍同步 UI 與狀態。GPU fixture 新增玩家／NPC／隊長初始體型斷言，並將原部署使用的冪等初始化分段記錄，沒有替換實際接觸流程。

本批只啟動一次 Godot。先前停用歷史清理的執行方式遭安全檢查拒絕，Godot 未啟動；確認標準清理範圍後，改用**未修改的**技能驗證器，正常核准桌面使用者環境。固定 Godot 4.6.2 stable mono `71f334935`，執行檔 `C:/Users/Nekki/Godot_v4.6.2-stable_mono_win64/Godot_v4.6.2-stable_mono_win64_console.exe`；命令 `--path <專案> --script res://scripts/tests/site_army_combat_visual_test.gd`，前景硬限時 25 秒。

- 結果 **FAIL**：25.174 秒逾時，沒有正常退出碼；無腳本／解析／斷言錯誤。證據 `.godot-temp/godot_verify/20260912_192333_416/result.json` 與 `combined.log`，沒有遺留 Godot 程序。
- 階段時間（從引擎啟動起）：開始建立場景 1.515 秒，ready 10.754 秒，綁定地形 11.208 秒，碰撞解壓 12.657 秒，兩隊視覺來源分別於 15.441／18.106 秒完成，部署完成 18.129 秒。
- `.visual_captures/site_army_combat/01_deployed.png` 於 18.501 秒寫出，已用圖片檢視器確認兩隊角色與隊長在 2D 地面上顯示，沒有黑色 viewport 方塊。體型與每隊單一隊長 3D 來源、100 個 Sprite2D 的斷言已越過。這張是部署畫面，不是傷害／倒地／效能驗收。
- 實際 120 Hz 戰鬥迴圈已進入，tick 0／30／60／90 分別在 18.617／18.697／18.848／18.984 秒；之後在 tick 120 標記前逾時。`02_contact.png`、`03_down.png` 未產生。武器生效起點為 20/24 秒，落在 tick 90–120 區段；只可定位為接觸區段耗時，尚無細分 profiler 證據，不能宣稱某個函式已是唯一瓶頸。

唯讀追查發現下一個具體重複工作：`TerrainLab._collect_unit_contacts()` 對玩家與 NPC 只排除地形不通，沒有距離／包圍盒排除；fixture 中兩人遠在 (50,50)／(52,50)，每次軍隊揮擊查詢仍呼叫 `incoming_contact()`，重做身體／盾牌的即時投影。`TerrainWeaponCollision.posed_points()` 再逐頂點投影；表面陣列快取並不快取同一動作步的已投影幾何。這是可消除的多餘查詢，但本批尚未量測它與多邊形窄相交／護甲查詢各占多少時間。

依 `godot-runtime-verify` 首次失敗即停規則，失敗後只做唯讀定位、檢視已產生圖片和更新文件，沒有繼續修改碰撞程式或重啟 Godot。下一個有界修改是先細分接觸階段耗時，再在現有共同接觸入口消除同一步重複投影／明確不相交目標；不得以格距取代實際命中、多給護甲覆蓋、丟棄動作時間或只加長逾時來取得 PASS。其他既有測試本批未重跑。

標準驗證器清理最舊的 `20260912_113424_358` 測試暫存時遇到 shader_cache 被占用警告；未自行重試刪除，該次 result.json 與 stdout／stderr／combined 紀錄仍在。未動正式存檔或素材，無 commit／push。玩家入隊、正式指揮／順位 UI、主動舉盾／施救與完整攻守／慢幀矩陣等缺口仍在，以下舊段落保留歷史證據。

## 19:14 補位／追退驗證與 GPU 定位（歷史）

本批依 ponytail 只修改既有 TerrainArmy 的命令接點，沿用合法局部路徑、原占格／預約與移動提交，不新增另一套導航或每兵 Node：

- 新攻擊令以当前已提交步的終點重新建立戰線，不延續舊機動／撤退目標；破防鎖不能被步行取消。
- 補位者再次倒下時移除失效指派，依目的格去重，避免兩個倒地人物共用同一戰線格而指派兩人搶格。
- 追擊者已進入合法近戰距離時算接敵，停步／收招不是「無路」；不試圖占用敵人格位。機動／撤退／回位找不到合法局部通路時就地自衛，不瞬移或穿人。
- GPU fixture 停止 SiteController 自動推進；倒地截圖前先下守位令，避免已新增的補位者合法占用倒地者原格而混淆該項斷言。

`scripts/tests/site_army_tactics_test.gd` 已通過：百人密集隊形合法平移、路徑成本／人物 ID 補位、指派保持、補位者再次倒下、破防移動鎖、已提交步收尾、新令取代、隔牆無路自衛，以及實際推進 20 秒的追擊上限、追擊倒數 JSON 恢復、距離限制、暫停、撤退優先與目標死亡收束。每小步檢查來源與目的預約不重疊；追擊計時切片保留 100 列但只有一名可行動追擊者，不冒充百人混戰效能測試。測試不產生武器傷害，實體命中仍由獨立近戰測試負責。

沿用 `C:/Users/Nekki/Godot_v4.6.2-stable_mono_win64/Godot_v4.6.2-stable_mono_win64_console.exe`，4.6.2 stable mono `71f334935`，同一技能安全執行器，僅略過舊紀錄清理、未修改技能檔。兩次均採前景硬限時；GPU 經正常核准以桌面使用者執行，沒有遺留 Godot 程序。

| 檢查 | 命令形狀／上限 | 結果 | 紀錄（`.godot-temp/godot_verify/` 下） |
| --- | --- | --- | --- |
| tactics | `--headless --path <專案> --script res://scripts/tests/site_army_tactics_test.gd`／20 秒 | PASS，exit 0，4.128 秒 | `20260912_191240_463/result.json` |
| visual | `--path <專案> --script res://scripts/tests/site_army_combat_visual_test.gd`／25 秒 | FAIL，25.174 秒逾時、无正常退出碼、三張輸出缺失 | `20260912_191356_647/result.json` |

GPU 階段紀錄：腳本開始 2.615 秒、開始建立 TerrainLab 2.627 秒、場景 ready 15.439 秒、fixture 地形綁定完成 15.898 秒。沒有抵達「bake loaded and two armies deployed」，也沒有進入截圖等待或戰鬥迴圈。故耗時區段已縮到地形綁定之後（含兩個 fixture 人物放置）至兩隊部署完成之前，尚未細分碰撞解壓、圖集 texture 或各隊長建立的實際耗時。不是已驗證的渲染錯誤，也不是素材缺失的證據。

唯讀追查確認一個具體的多餘載入：`HumanCharacter3DEditor._initialize_preview()` 固定 `_load_body_model(0)`；NPC 的 `initialize_visual()` 與 `TerrainArmy._create_visual_source(1, ...)` 隨後再載入女性。`_load_body_model()` 每次以新的 GLTFDocument 解析來源並建立模型，並非只切換已載入實體。女 NPC 與兩名女隊長因此各多建一次男性。這是可直接消除的重複工作，但尚未量測它占上述逾時區段多少比例。

依 `godot-runtime-verify` 首次失敗即停規則，本批 GPU 失敗後沒有再修改遊戲程式或啟動 Godot，只做唯讀定位與文件交接。下一個有界修正是讓預覽首次建立直接讀既有 `visual_state.body_index`，由軍隊來源在 open 前指定身體；避免先建男性再切女性，保留後續正常換裝／切換功能，並補部署內部耗時標記後驗證。不要只加長逾時或拿舊截圖當新結果。

本批未重跑修改後的其他近戰／存檔回歸或 editor；18:42 表格為前批結果。GPU 三個預期檔 `.visual_captures/site_army_combat/{01_deployed,02_contact,03_down}.png` 均未產生，無畫面可供驗收。玩家入隊、正式權限 UI、主動舉盾／施救等仍在未完成範圍；無 commit／push。

## 18:42 存讀檔一致性修復與回歸

本次只處理使用者確認的存讀檔失敗及相關回歸。原因是指揮能力原本為 int，Godot JSON 讀回為 float，恢復時直接複製 Dictionary 沒有還原三項能力的整數型別。現於 `TerrainArmy.restore_combat_state()` 將已由驗證器確認為 0–100 精確整數的戰術／統御／教練值轉回 int；不重新抽值，不改 HP 或倒數，也不放寬輸入檢查。

依 ponytail 採擁有者內的最小修正。原複合斷言拆成能力型別、能力內容、死者 HP 與 KO 倒數各自檢查，保留嚴格一致性比較；新增 -1、101、1.5、字串、null、NaN、Infinity 的拒絕斷言及重複恢復檢查。修正後已完整走完原先第 110 行之後的移動提交、來源／目的預約、重複軍隊拒絕與冪等恢復驗證。

固定執行檔 `C:/Users/Nekki/Godot_v4.6.2-stable_mono_win64/Godot_v4.6.2-stable_mono_win64_console.exe`，Godot 4.6.2 stable mono `71f334935`。沿用技能安全執行器，每次前景硬上限 20 秒，僅停用本次執行的舊紀錄清理呼叫，不修改技能檔。測試命令為 `--headless --path <專案> --script res://scripts/tests/<下列檔名>`；編輯器為 `--headless --editor --quit-after 2 --path <專案>`。

| 檢查 | 結果 | 秒數 | 紀錄（`.godot-temp/godot_verify/` 下） |
| --- | --- | --- | --- |
| `site_army_command_save_test.gd` | PASS／exit 0 | 3.741 | `20260912_184137_026/result.json` |
| `site_army_combat_test.gd` | PASS／exit 0 | 3.524 | `20260912_184159_430/result.json` |
| `site_combat_save_test.gd` | PASS／exit 0 | 2.782 | `20260912_184213_385/result.json` |
| `site_combat_rules_test.gd` | PASS／exit 0 | 1.485 | `20260912_184226_545/result.json` |
| editor | PASS／exit 0 | 4.311 | `20260912_184252_915/result.json` |

所有測試皆有各自 PASS 標記，無腳本／斷言錯誤或逾時。根憑證讀取訊息為 exit 0 下的環境雜訊；editor 另有既存 `output/verification-probe` 子專案略過及結束掃描警告，未移除該目錄。

測試保存僅使用 `.godot-temp/site_combat/` 測試檔，不操作正式存檔；沒有重製素材、commit 或 push。這次未開 GPU 視窗，也未把資料測試當成完整畫面／效能證據。18:26 的失敗由本段結果取代；下列內容保留歷史過程及其他未完成範圍。

## 18:26 指揮、命令與軍隊保存續作（歷史）

使用者要求繼續完成未完部分。本批改動留在原本擁有者，沒有重製／重烘焙使用者補齊的素材，也沒有另開背景工作；目前工具無 `schedule`，遵守計時規則採前景硬限時執行。

- TerrainArmy 新增正式／代理／當前指揮關係、順位、稀疏三項能力及完整 64 位亂數狀態。按傷亡後的在場資格處理代理、代理轉正、原正式隊長回歸及空缺；身分與物理格位不交換。能力只在真正任職且缺值時生成，並依合格能力持有人原值合計後遞減。
- 指揮在 TerrainLab 同一步接觸全部結算後處理；空缺停止新的主動進攻與追擊，保留既有撤退及已提交移動的安全收尾。恢復指揮保留須新進攻命令的鎖，不自動重播舊令。守位、攻擊、機動、撤退與具名追擊已接入命令入口，補位仍使用原合法尋路及占位／預約／完成移動資料。
- Site 面板新增明示為「測試指揮」的 NPC 隊長命令按鈕；**不是玩家已加入名冊或已取得指揮權**。一般玩家操作沒有被接管。順位 API 已有權限與原子替換檢查，但幹部任職流程、順位上移／下移完整 UI 尚未接入。
- SiteStore format 3／SiteController 捕捉與 TerrainLab 恢復軍隊列、HP／暈眩／KO、姿態／命中去重、移動來源及預約、指揮／能力／追擊倒數；唯讀圖集碰撞資料不入存檔。新部署使用地圖上的單調隊伍序號。交戰保存仍 BUSY，缺少軍隊快照仍拒絕保存；不再一律要求刪除軍隊才保存。
- 女隊長護甲查詢前同步 sprite 當前地面＋踏步偏移，避免同一量化動畫幀內護甲留在舊位置。人物已完成攻擊的保存不攜帶失效的舊去重名單；未完成攻擊仍完整保存。
- GPU fixture 補 `_initialize`、場景建立、地形、部署、戰鬥步與圖像寫出的時間標記。**本批未執行 GPU**，不能聲稱上次超時已修復。

### 本批驗證與停止點

同一 Godot 4.6.2 stable mono `71f334935`／console 路徑及技能安全執行器如下方歷史紀錄；前景上限各 20 秒，沒有留下背景程序。

| 檢查 | 命令形狀 | 結果 | 紀錄 |
| --- | --- | --- | --- |
| 編輯器載入 | `--headless --editor --quit-after 2 --path <專案>` | PASS，exit 0，9.239 秒 | `.godot-temp/godot_verify/20260912_182400_843/result.json` |
| 新指揮／軍隊存檔 | `--headless --path <專案> --script res://scripts/tests/site_army_command_save_test.gd` | FAIL，斷言後 20.106 秒硬逾時，無正常退出碼 | `.godot-temp/godot_verify/20260912_182421_940/result.json` |

第一個失敗為 `SCRIPT ERROR: Assertion failed.`，位置 `scripts/tests/site_army_command_save_test.gd:110`。該行合併比較讀回能力 Dictionary、死者 HP 及昏迷倒數；不能以單一複合斷言假稱已精確隔離全部差异。

執行已越過第 109 行，包含代理順位、恢復不擠掉現任、代理轉正、沒有指揮後等待新令、權限拒絕、保存及載入、格位／移動進度與 RNG 狀態相同。但**整個測試沒有 PASS**，後續占格提交、重複快照及重複恢復等斷言尚未執行完成。

唯讀定位：測試保存的三项能力為 JSON 浮點值；`restore_combat_state()` 目前以 `item["values"].duplicate()` 直接取回，未把已驗證的 0–100 整數欄位正規化成 int。保存檔死者 HP=0、另一人物 KO=29.9。能力型別是具體可疑點，仍須拆開該複合斷言，分別輸出能力值／型別及倒數差值，不能單靠修改斷言忽略實際狀態差異。

依 `godot-runtime-verify` 的首次失敗即停規則，失敗後只做唯讀定位及文件交接，沒有修正重跑或另跑 GPU。下一個有界修正：正規化通過驗證後的指揮能力整數，拆分狀態斷言，再執行同一測試；之後才跑補位／追退、既有近戰／人物存檔回歸與 GPU 階段定位。

### 現在仍缺少的閉環

1. 上述軍隊讀回一致性失敗與後續驗證；本批新補位、追擊、撤退尚無獨立執行通過證據。
2. 玩家正式加入既有名冊、同一人物參與接任而不產生第二個角色狀態、正式下令／順位 UI，以及幹部任職先後的整合。
3. 軍隊普通士兵主動舉盾、施救及玩家對選中軍隊成員直接出招的完整操作；目前仍是固定長劍切片。
4. GPU 實景、角色互換攻守、慢幀／遠近景、友軍阻擋／遠程第一接觸完整矩陣與效能實測。
5. 設計第 10 節的拘押制度、後勤／攜糧、士氣門檻／整補、家族繼承等未決制度沒有擅自定案。不得把這些或全部 32 動作組宣稱完成。

差異格式檢查僅對 scoped scripts／文件執行並通過；未變更 Git LFS 設定，沒有 commit／push。以下 17:27 與更早段落保留歷史證據，不代表最新程式已通過當時的測試。

## 17:27 普通士兵近戰續作

使用者補齊的正式角色、16 個新增動作、1,000 幀圖集及碰撞資料均保留，沒有重製、重烘焙或覆寫。本次也確認先前人物 JSON 讀回問題已有 16:38 的通過紀錄，不再重做該修正。

- `TerrainArmy.combat_units` 持有逐兵 HP、暈眩／衰減、昏迷、姿態時間、攻擊去重與結果；格位仍使用原本 cells／占用及預約資料。第二隊仍是同一 TerrainArmy 擁有者，不新增 Battle 系統或逐兵行為 Node。
- 普通士兵從同一圖集幀取得畫面與 body／weapon／shield／armor；護甲仍按實際投影三角形覆蓋點取值。兩隊共用一份唯讀碰撞資料，不逐兵複製整份圖集。即時 3D 女隊長保留原模型投影，不以男性圖集代替其身體。
- 軍隊與現有角色接入 TerrainLab 同一 120 Hz 動作步與命中佇列；軍隊戰鬥時間不走既有移動的慢幀丟棄路徑。玩家原本的手動操作保留，沒有替玩家自動出招。
- 新增實驗入口：Site 面板的「部署兩隊近戰測試（各 100 人）」。只尋找現成連通 20×10 空地，不削平地形、不刪物件、不移動玩家；已有隊伍或找不到空地就明確拒絕。兩隊固定長劍／既有皮甲與盾，以近距離接敵為本批切片，不是全武器軍隊配置系統。
- 近戰友軍實際接觸阻擋、每揮每人去重、同時命中互換傷害、死亡／昏迷釋放站立占位、起身前檢查占位與不回血已接線；普通士兵尚沒有主動施救、補位、隊長接任或追退。
- 「查看兩隊傷亡」顯示可戰／昏迷／死亡；「結束測試並清除兩隊」是明確的測試清理。軍隊快照未完成前，保存入口共用 `SiteStore` 的 BUSY 守衛，載入也拒絕直接丟棄測試隊伍。這不是正式遊戲的死亡或保存政策。

### 本次驗證

固定 Godot：`C:/Users/Nekki/Godot_v4.6.2-stable_mono_win64/Godot_v4.6.2-stable_mono_win64_console.exe`，4.6.2 stable mono `71f334935`。沿用 `godot-runtime-verify/scripts/verify_godot.ps1`；只停用本次執行的舊紀錄清理呼叫以保留證據，未改動技能檔、環境／逾時／錯誤守衛。

| 檢查 | 命令形狀／上限 | 實際結果 | 紀錄 |
| --- | --- | --- | --- |
| editor | `--headless --editor --quit-after 2 --path <專案>`／20 秒 | PASS，exit 0，4.533 秒 | `.godot-temp/godot_verify/20260912_172026_826/result.json` |
| 軍隊定向資料 | `--headless --path <專案> --script res://scripts/tests/site_army_combat_test.gd`／20 秒 | PASS，exit 0，3.158 秒 | `.godot-temp/godot_verify/20260912_172044_883/result.json` |
| 完整場景 GPU | `--path <專案> --script res://scripts/tests/site_army_combat_visual_test.gd`／25 秒 | FAIL，25.168 秒超時，無正常 exit code；三張輸出均未產生 | `.godot-temp/godot_verify/20260912_172243_325/result.json` |

資料測試建立兩隊共 200 列且沒有角色 Node，從真實圖集選取一組相鄰士兵揮擊驗證一次接觸、友軍阻擋，另驗 KO／占位／起身／死亡與同時致命交換。**不是 200 人持續混戰或 9,000 人效能通過**；也未重跑本次修改後的既有角色／存檔／隊形完整回歸。

GPU 日誌只有引擎與 D3D12／RTX 5060 初始化，沒有腳本錯誤或足以定位到哪個函式的階段訊息。第一張部署圖尚未寫出；因此目前只能確認逾時，不能斷言是模型載入、圖集解壓、GPU 準備或首幀等待中的哪一段。三個預期檔名為 `.visual_captures/site_army_combat/{01_deployed,02_contact,03_down}.png`；不存在，不作視覺宣稱。程序已由硬逾時終止，沒有留下待跑工作。

依 `godot-runtime-verify` 失敗即停規則，本次不重跑。下一個有界修正是替 GPU fixture 的場景建立、圖集載入、兩隊部署及首幀等待加入階段耗時，再分離一次性載入與戰鬥取樣；不是單純調高逾時。另需核對女隊長在同一圖集幀內踏步時，實際 sprite 投影與護甲查詢的位置同步，以及結束測試後人物攻擊去重身分的保存邊界。

差異格式檢查僅針對 scripts 範圍通過。全樹檢查遇既有 Git LFS 暫存位置存取拒絕，未更改 LFS 設定或以此清理使用者檔案；沒有 commit／push。

## 已寫入的第一批接線

以下是程式範圍，不表示已通過玩法與 GPU 驗收：

- 共用 `SiteCombatRules`：斬／刺／鈍、HP 與暈眩分開、實際部位倍率、零傷害／零暈眩可能、破防與訓練遞減換算。
- 玩家／NPC：浮點 HP、暈眩、昏迷倒數、自然甦醒、現場救助、死亡優先、非致命普通命中不取消攻擊收招。
- 命中：沿用模型投影，補實際盾輪廓、命中點護甲三角形查詢、按裝備項而非子網格重複累加；加入多目標候選與每揮每人去重、友軍近戰阻擋。
- 箭矢：改為在途彈體陣列，固定出手方向，途經人物也參與碰撞；從現有玩家／工人 cargo 扣除 arrow／bolt，不建立第二個彈藥存貨。
- 動作時間：TerrainLab 分小步先推進角色再結算接觸，不把攻擊倒數接到軍隊會丟棄慢幀累積量的移動路徑。
- 操作：取消玩家因接近 NPC 而自動轉身；實驗面板增加友軍切換及相鄰救助入口，顯示 HP／暈眩。
- 工作守衛：昏迷／死亡／被俘及施救期間不能繼續採集工作。

## 本次續作新增

- 修正資料測試的 Site 初始化缺漏，沿用正式 `SiteEnvironment.initialize()`，不再直接寫入空的 `static_blocked`。
- 救助期間的合法移動會先中止救助再移動；移動中止、有效受擊中止與自然／救助甦醒不回血均有資料測試。
- 接觸佇列依命中比例分組；同時合法攻擊能互換傷害，較早死亡阻止較晚近戰命中，已出手投射物不因射手死亡失效。這三項定向測試已通過，不等於所有 GPU 接觸時序已驗收。
- 補無盾時的實際武器對武器格擋接線，只適用近戰；不把刀劍接觸當成箭矢格擋。聚焦 GPU 測試已有側向劍招的真實 parry；其他測試角度部分先命中身體，不能由選 guard 保證所有攻向被擋。
- `SiteStore` 升為 format 2，新增玩家／NPC 快照驗證與恢復接線：HP、暈眩／昏迷倒數、實裝與外觀、格位與移動、攻擊去重、格擋、救助關係、在途彈體、NPC 命令。彈藥仍共用原 cargo，沒有新增平行存貨。
- 保留原本交戰期間不可保存的守衛；format 1 舊地圖的明示遷移已通過定向測試。軍隊／拘押關係等不在本批人物快照內。

先前合法 JSON 外觀讀回為浮點數卻遭拒絕的 `CORRUPT_SAVE` 已修正。最新 `site_combat_save_test.gd` 通過男女狀態恢復、不回血、既有保存守衛、攻擊去重、彈體／彈藥不重複、非法快照拒絕與舊地圖明示遷移。測試只寫入 `.godot-temp/site_combat/snapshot.json` 及其 legacy 測試檔，沒有操作正式 `user://sites/current.json`；這不代表軍隊或完整遊戲存檔已驗收。

角色補件已提升至男女正式 `.blend/.glb/.json`：16 個新增 clip、箭／弩矢及箭袋、共用事件時序、起身腳底校正，以及明光鎧四種低姿態的十個裙甲部件修形。護甲投影使用既有碰撞流程加上 Godot 原生 morph 結果；1,000 幀圖集與逐姿態碰撞已同步重烘焙。詳細來源保存、容差、四視圖、日誌與未涵蓋裝備範圍見 `SITE_COMBAT_3D_ASSET_GAPS.md`。

護甲初始係數是本輪可調的測試值，不是已經驗證的平衡數字：皮革 12/6/4、緩衝 4；鐵 25/18/10、緩衝 6；鋼／明光 35/25/15、緩衝 10；內衣 1/0/0、緩衝 2。前三項依序為斬／刺／鈍防護；只在該實裝覆蓋命中點時套用。

## 還未完成／不能宣稱的部分

1. 本次已有兩隊長劍近戰測試接線與逐兵狀態，但 GPU 實景尚未通過；沒有玩家入隊、隊長接替、補位、追擊或撤退閉環。軍隊接入與玩家／NPC 相同的命中佇列，不把每兵轉為 TerrainTestCharacter。
2. 普通士兵開始消費交戰圖集及碰撞；目前只有固定長劍切片，未把全部 32 動作組變成軍隊可用命令。主動舉盾／施救、長槍軍隊、完整跨角色互動仍待後續驗證。
3. 舉盾／放盾／破防與無盾劍／槍格擋已有正式動作、盾面移位及部分實際武器接觸證據；全攻向、全裝備組合未驗收。
4. 命中先後三項資料測試及雙角色慢幀、一次事件、幾何／縮放回歸通過；整個 D79 與多人友軍阻擋、箭矢第一接觸實景矩陣仍未完成。
5. 男女互換施救接觸、起身完整動作鎖、占位阻擋、暫停、存讀姿態與明光鎧低姿態通過聚焦回歸；不等於所有裝備／多人／騎乘組合通過。
6. 彈藥目前讀既有 cargo；未增加取得／補給流程或初始贈送箭矢。現有弓弩測試必須明確準備彈藥，不得用無限彈藥略過規則。
7. format 2 人物保存／恢復的定向測試已通過；軍隊／拘押仍未持久化，現行交戰保存守衛未放寬，未用直接序列化測試暗示允許戰鬥中保存。
8. 俘虜動作與唯一拘押關係、家族繼承、國家配裝、軍隊攜糧、士氣、潰退與整補尚未完成；設計第 10 節的未決制度沒有被本輪擅自定案。
9. 舊 combat／weapon-contact 測試仍包含 HP=96 的全身格擋舊斷言，尚未依實體盾／護甲規則改寫。本輪未執行那些舊驗收。

## 16:44 角色素材階段證據（早於本次軍隊修改）

Godot 4.6.2 stable mono console；使用既有安全執行器，退出碼均為 0，無腳本／斷言錯誤。以下紀錄均位於 `.godot-temp/godot_verify/<紀錄>/result.json`。

| 檢查 | 模式／秒數 | 紀錄 |
| --- | --- | --- |
| `site_combat_rules_test.gd` | headless／1.787 | `20260912_163846_956` |
| `site_combat_save_test.gd` | headless／2.714 | `20260912_163850_357` |
| `site_combat_character_assets_test.gd` | visual／21.672 | `20260912_164039_133`；含最新正式男女資產與明光鎧 Site 五姿態 |
| `bake_terrain_army_soldier.gd` | visual／50.809 | `20260912_163819_025`；1,000 幀重新烘焙 |
| `site_combat_atlas_test.gd` | visual／16.619 | `20260912_163947_543`；48 姿態逐點比對及護甲查詢 |
| `--editor --import` | headless／84.250 | `20260912_164050_299`；最新角色／圖集完整匯入 |

明光鎧男女完整取樣、正式包換裝重設、來源保留與 SHA-256 另列於素材文件。headless 執行有根憑證讀取雜訊；完整匯入另報既存 `output/verification-probe` 子專案略過警告，未移除該無關目錄。上述 PASS 不抹除下方的歷史失敗。

## 10:55 歷史診斷（已由上述續作結果取代）

固定執行檔：`C:/Users/Nekki/Godot_v4.6.2-stable_mono_win64/Godot_v4.6.2-stable_mono_win64_console.exe`。
版本：Godot 4.6.2 stable mono，build `71f334935`。
執行器：`C:/Users/Nekki/.codex/skills/godot-runtime-verify/scripts/verify_godot.ps1`。
各次均使用執行器前景限時 20 秒，沒有留下背景程序。下表區分前批與本次續作；較早 PASS 不覆蓋之後新增的存檔程式。

| 檢查 | 命令形狀 | 結果 |
| --- | --- | --- |
| 前批編輯器載入 | `--headless --editor --quit-after 2 --path <專案>` | PASS，exit 0，5.396 秒；不是最新存檔修改後的獨立 editor 掃描 |
| 前批資料／行為測試 | `--headless --path <專案> --script res://scripts/tests/site_combat_rules_test.gd` | FAIL，20.113 秒超時；fixture 初始化缺漏，本次已修正 |
| 本次初始化修正後 | 同上 | PASS，exit 0，2.415 秒 |
| 本次救助／命中先後修正後 | 同上 | PASS，exit 0，1.586 秒；護甲公式、浮點 HP、暈眩、死亡、甦醒／救助及中止、動作鎖、接觸先後、無幾何不捏造命中 |
| 本次新增人物存讀檔 | `--headless --path <專案> --script res://scripts/tests/site_combat_save_test.gd` | FAIL，exit 1，13.882 秒，未超時；首個錯誤如下 |
| GPU 場景／動作 | 未執行 | 不作外觀、接觸公平性或效能結論 |
| Git 差異格式 | `git diff --check` | 本輪檢查無輸出；不是玩法驗證 |

本次首個失敗：

```text
SCRIPT ERROR: Assertion failed: { "ok": false, "code": "CORRUPT_SAVE", "message": "CORRUPT_SAVE：人物狀態／實裝非法：player" }
at: run (res://scripts/tests/site_combat_save_test.gd:32)
```

失敗後唯讀追查已定位一處明確型別缺陷：`HumanCharacter3DEditor.valid_appearance()` 使用 `value.get("body") in [0, 1]`。Godot 的 JSON 數字讀回為浮點數，而 Array 的 `in`／`has` 區分整數與浮點數，因此合法的 `0.0`／`1.0` 也會被這個整數清單拒絕。依據：[Godot JSON 數字說明](https://docs.godotengine.org/en/stable/classes/class_json.html#class-json-method-stringify)、[Array.has 型別說明](https://docs.godotengine.org/en/stable/classes/class_array.html#class-array-method-has)。這是原始碼與官方語義交叉核對，尚未做修正後執行驗證，不能宣稱沒有其他讀回問題。

下一個有界修正：在共用外觀驗證入口先確認數值型別、有限值及精確的 0／1 範圍，再轉整數索引；不得用無條件 `int()` 把非法小數截斷為合法值。新增男女外觀 JSON 往返與非法值拒絕的定向斷言，再跑同一存讀檔測試。

依 `godot-runtime-verify` 的失敗即停規則，本次失敗後未再啟動 Godot，也未套用上述程式修正；已完成唯讀定位並等待針對性續作。資料測試 PASS 僅有系統根憑證讀取訊息；存讀檔的 assertion 是實際失敗，不能歸為憑證雜訊。

原始紀錄：

- `.godot-temp/godot_verify/20260912_042532_477/result.json`
- `.godot-temp/godot_verify/20260912_042818_316/result.json`
- `.godot-temp/godot_verify/20260912_042818_316/combined.log`
- `.godot-temp/godot_verify/20260912_103909_090/result.json`
- `.godot-temp/godot_verify/20260912_104203_308/result.json`
- `.godot-temp/godot_verify/20260912_104203_308/combined.log`
- `.godot-temp/godot_verify/20260912_105139_703/result.json`
- `.godot-temp/godot_verify/20260912_105139_703/combined.log`

## 下一段實作順序

1. 定位本次完整場景 GPU 逾時並完成部署／接觸／倒地畫面檢查；核對女隊長投影與測試退出的去重／保存邊界。
2. 重跑既有角色、存讀檔、隊形回歸，在真實多人路徑驗證友軍阻擋、箭矢第一接觸、慢幀及同時命中，再延伸補位及隊長接替。
3. 實測 100／9,000 人時間、記憶體與畫面；目前沿用既有 Sprite2D 圖集呈現，不冒稱已是 MultiMesh 萬人 renderer，不為普通士兵增加行為 Node／Skeleton3D。大規模 renderer 與碰撞資料串流仍待量測後處理。
4. 依已定案規則補位、追退、失能及拘押；其餘第 10 節未決制度在獲得必要選擇後延伸。人物存讀檔與角色素材本輪已驗證的切片不必重做。

未 commit／push。保留既有 `output/` 與 `scripts/tests/v03r_cleanup_test.gd.uid`，沒有清理無關檔案。
