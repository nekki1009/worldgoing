# Terrain Lab V6：整隊保持陣型前進、隊長居中與後隊追趕

日期：2026-09-10。設計基準：下文保留原核准契約。**2026-09-11 最終更新：S0–S4 的實作與功能／畫面驗收已完成；同版 e04f5ed4 有119次無畫面測試通過，含36／36生成矩陣、D01三種dt、完整舊回歸；11種GPU案例共100張圖已檢視，11段真活動效能已量測，編輯器完整匯入退出0。效能仍有7段 Army CPU P95 高於2ms比較目標，不宣稱所有效能目標PASS。D01符合預先訂出的V6 503秒預算，但不符合舊180秒；F01 30Hz亦保留舊20秒失敗。** 完整證據、未達標項及版本邊界見 `TERRAIN_LAB_V6_ACCEPTANCE_STATUS.md`、`TERRAIN_LAB_V6_FINAL_RUNS.json`。

使用者要求：目前行軍拉成長蛇陣，後方應跑步趕上；不能讓隊長先走而先接戰，應保持陣型前進。

本計畫優先於 V3–V5 重疊的「隊長先抵達、隊員沿路追趕、最後集合」行軍語義。V4／V5 的合法邊、逐人過口／出清、99 人完整落位、唯一 owner／claim、在途動作不回彈、搜尋預算及實際 GPU 驗收繼續保留。V5 的既有 PASS 是上一版行為基線，不代表本文件新增的行進隊形與隊長保護已通過。

## 1. 修改結論

改成「**陣型領軍，隊長是陣型內的一員**」，不再讓實體隊長當全隊導航的移動探針。

- 寬地：100 人以緊密隊形前進，隊長固定在中間，前排普通兵先形成保護層；不是隊長先跑、其他人追他的足跡。
- 有人落後：前方停止擴大距離，落後者沿合法空間跑步補位；只暫停尚未開始的推進，不凍結任何半途動作。
- 窄口：只在真正受地形限制的區段縮列，隊長按中排順序通過，沒有獨立插隊通道。
- 出口：先離開 core／egress，再向附近可用空間展開接應；不能讓先過口的人直奔下一座山或最終邊界。
- 中間平台太小：合併為同一段連續通道，維持受控縱隊直到下一個能合法容納整隊的地點；不能要求每個小平台都先塞滿100人。

這不是「全面加速」。先限制前方拉開距離，再讓真正落後且前路可走的人跑步。整隊推進可能比原本隊長單獨衝刺慢，但不再以隊長到終點時間衡量行軍品質。

## 2. 已確認的直接原因

以下行號以本次核對檔案為準，實作前按函式重新搜尋所有 caller。

| 現行位置 | 實際行為 | 與新要求的衝突 |
| --- | --- | --- |
| `terrain_army.gd:1560` `_captain_should_wait()` | FOLLOW 直接 false；EDGE／NONE 也直接 false，對現有合法命令沒有有效等隊機制 | 隊長不受隊尾與隊形約束 |
| `:1382` `_build_formation_targets()` | anchor 取 `cells[0]`；隊長移動後才帶動陣位更新 | 是隊長拖著隊形，不是整隊共同推進 |
| `:959` `_formation_preferred_cell()` | 普通兵 rank 全在 anchor 後方 | 就算隊員追上，隊長仍是前緣，不能滿足中排保護 |
| `:2713`、`:2928` `_build_passage_plan()` | 整條 route 的 passage 一次發布，整隊設 COLUMN | 尚未過口與早已走到寬地的隊員共用一個窄列模式 |
| `:3414` `_unit_should_run()` | COLUMN 且 width≤2 時全員禁止跑；普通兵另受 `blocked_time < 5` 限制 | 寬地上的隊尾／已出清者也不能跑；先前排隊等待可能壓掉追趕資格 |
| `:2193` `_next_passage_step()` | 出口之後直接以「下一個入口」為目標，最後才去終點陣位 | 五個坡口間容易形成一條長鏈，沒有中途收攏目標 |
| `:3946` `_formation_planning_order()` | index 0 固定第一位，其餘輪詢 | 隊長仍享有前進先序；沒有普通行軍的整體平移提交 |
| `:4076`、`:4193` 普通單兵移動／commit | 前方同袍尚佔格時，下一排不能直接移入；要等 vacate 或推擠 | 只移動目標而不處理共同 vacate，可能依然變成一排排蠕動 |
| `terrain_army_formation_smoke_test.gd:48,60,89` | 驗 slot 平移、最後99人、曾有人跑過 | 沒有證明「後隊」在跑，也沒有逐幀驗隊長前方有人、行軍長寬與最大掉隊距離 |
| `terrain_army_default_edge_test.gd:116,190` | 要求隊長本人到 `(99,75)` 且走最短253邊 | 隊長居中時不能仍被要求單獨貼邊，需更新目標角色而非放寬安全檢查 |

`TerrainLab` 的戰鬥按鈕目前作用於 PLAYER／NPC，沒有查到100人軍隊的獨立接戰流程。本輪先修行軍位置與隊長保護層，不新增戰鬥／仇恨／射程／傷害系統；也不宣稱能保證隊長免於遠攻或側襲。

### 2.1 核對版本

| 檔案 | SHA256 |
| --- | --- |
| `scripts/terrain_lab/terrain_army.gd` | `EB2A7BB8C5D8DB026C98AE6385FF700680DCE6F2504DF66196EE3CB49C3F896B` |
| `scripts/terrain_lab/terrain_lab.gd` | `6BF08FE98C935C727E872A3FC8D49589D64994344C4C22163B1FB3F1F9FC8EA1` |
| `scripts/tests/terrain_army_default_edge_test.gd` | `ECA832AD5BA8A40B6D406A425CC091FBC5FAE9A84D721ACB39D998044634726A` |
| `scripts/tests/terrain_army_formation_smoke_test.gd` | `2F79A1BDACD3B36A94A53C0ADE6F3A3EDAAEFD891E4ABE4191EFC4B1FDD330BA` |

上述 runtime 與上次封存版本一致；本輪原因判斷來自程式核對，不是新一次 GPU 重播結果。相關檔案仍 untracked；不得拿 Git HEAD 覆寫，實作前先保存副本與 hash。

## 3. 新行軍契約

### 3.1 隊長位置與命令終點

1. 開闊平地預設為100格、10列×10排，**其中一個中排中央格由隊長佔用**，其餘99格為普通兵；不是在99人矩形前再放一個隊長。建議隊長位置為從前方數第5排、第5列。
2. 開始行軍前先在可行範圍內整隊，前排形成後再整體推進；初始部署陣形若不規整，必須真實走位，不能重寫 cells。
3. `MOVE_TO_EDGE` 改成「陣型前緣到達可行的地圖邊緣」。原 `(99,75)` 可作邊界目標標記，不再是隊長本人必站的格；隊長終點是合法最終陣型內的中排格。
4. `FOLLOW_PLAYER` 同樣以陣型前緣與 PLAYER 保持距離，隊長不額外衝到 PLAYER 身邊。沿用現有跟隨距離參數時，要明確改成量前緣，不混用 captain 距離。
5. 邊界／PLAYER 附近沒有完整10×10時，嘗試地形允許的緊密較窄隊形；不能用陣型越界、非法邊或讓隊長獨自到點來宣告完成。真正容量不足仍明確回報。

### 3.2 推進節奏與追趕

沿用 MOVE=0.24s、RUN=0.18s、SIM_STEP=0.10s；先不改步速數值。

- 全隊正常行軍使用一致步行節奏，隊長沒有由「終點還很遠」觸發的獨立快跑權。
- 陣型參考點一次只發布一格的平移／局部變形意圖。必須檢查下一個陣形是否可行，以及全體100人的實際／在途位置，而不是只看隊長已走幾格。
- 建議第一版凝聚門檻：100人相對目前活動陣位的合法補位距離都≤1格才恢復共同推進；下一步若會讓任何人超過2格，暫不發布該步。這是**待實作驗證的初值**，不可宣稱已量測合適。
- 落後者距活動陣位≥2格，且下一條合法邊位於可追趕的寬地，就在下一次可開始動作時用RUN；回到≤1格改回WALK。不是量到遠端最終slot的距離。
- 較早排隊累積的 blocked_time 不得永久取消跑步資格。当前被堵時不能跑進去，空間釋出後重新判資格。
- 進／出窄口、坡道、交換、推擠保持原有安全節奏；**禁止跑步的判斷必須是本人的當前 phase／下一條邊**，不能由全軍 COLUMN 直接封鎖全員。
- 一個動作開始後 duration／插值不改；模式切換只影響下一條邊。locomotion 與 duration 用同一次決策，避免動畫RUN但實際WALK。

距離使用目前局部目標的既有共享距離資料／已知合法路線；跨牆、跨崖不能用曼哈頓近就算「跟上」。不得為100人各開全圖BFS。

### 3.3 不讓正常平地行軍變成100人的推擠鏈

在完整開闊 footprint 上，需要一個**限用途的一格同向平移批次**，放在既有 TerrainArmy 內；它不是新導航服務，也不新增另一份 reservation。

1. 全體參與者都走同一個四方向向量，source／destination 唯一、`can_step` 全合法；無外部角色、別人的 claim／lock、未完成 swap／push；不得越過未獲准的gate。
2. destination 可被「同一批次且會同時移走」的同袍佔用，不能被無關者佔用。整批驗證完才寫入同一 `_reserved_cells`，全部以既有 moving_to／move_progress 呈現。
3. 相同duration、相同開始時刻；batch到達提交點時，先驗證全批舊owner與claims，再移除全批source owner、寫入全批destination owner。不能在逐index普通commit途中暴露半套占格。
4. 普通 `_complete_move()` 的「目的格必須為空」規則仍保留；batch成員不得被其逐人提交。這是擴充既有 move／swap 的原子提交邊界，不是讓任意單兵穿過占格。
5. 每人仍有逐邊 commit 記錄、動畫、統計與合法性驗證。不得因整批提交省略逐人事件，不能直接把整隊 cells 平移來代替動作。
6. 新指令到來：未開始批次可撤銷；已開始的整批先完成，再交接最新命令。不得取消一半、回源格或清除另一人的claim。
7. `blocks_cell()` 已同時讀 owner／reservation，保留它對 PLAYER／NPC 的保護；prune、clear、cancel、completion 必須識別仍在途的批次。開始後的強制地形／fixture變更若破壞提交條件，報不變量錯誤，不暗中瞬移恢復。

只對合法開闊地的一格共同平移使用批次；局部追趕與窄口仍走既有單兵／swap／push規則。**2026-09-10 使用者核准補充：彎道局部重整可使用「同方向小批次」，只包含該批真實同向 vacate 的成員，不含無關占格者；共用既有 owner／claim 與完整提交邊界，隊長保護、逐人過口順序及時間期限不放寬。不同方向不得合為一筆原子交易。** 不要把既有串行push硬塞成100人的平地行軍，也不要順勢重寫成通用交易框架。少量短暫batch資料與既有owner／claim共用，沒有第二個持久狀態owner。

### 3.4 窄口只局部縮列；出清後先接應

1. 保留整條 route 的完整 descriptors／物理通過紀錄，但把「此刻要以幾列行軍」與「route未來某處最小寬度」分開。不能因遠處有單格口，讓整條行軍從開始到終點都width=1。
2. 用目前 footprint 與近端合法走廊判斷可用寬度；有連續合法寬地可展開時就展開，不能以隊長已過口或全軍尚未最終完成來猜。
3. 隊長使用同一 passage admission／phase，不再從 `_next_step(index==0)` 走獨立BFS繞過隊伍。隊長排在其中排對應的通行順序，前方護衛未前進時不能插隊；普通兵仍由就緒票序與真實空間仲裁。
4. 每段通道預先找**最近的合法下游整隊區**，排除 core、egress、外部占格與仍需保留的通行格。必須有真實可达布局，可容納隊長＋99人，而非只算散落格數。
5. 先過口者進入已分配的接應位置，形成前排並停止遠走；後到者出清後跑步填入後排。不能站在出口等，也不能把中途接應slot算最終settled。
6. 全體完成這一通道組的必要通過／出清，且恢復凝聚條件後，才開始下一段共同推進。不要每步都要求全隊完全idle；只有會改變正在使用的slot／交易所有權時才等安全發布邊界。
7. 若下一平台不具整隊布局容量，將相鄰窄口當同一個連續通道組，出口工作繼續，直到下一個可行接應區。必須有不夠容納的地形證據，不能用此例外迴避本可展開的平地。
8. 在長、真正狹窄的走廊內，單列本身是物理限制；允許隊形變長但保持順序與緊密間隔，隊長不得跑到最前方。走廊結束後不能把長蛇狀態帶到整片寬地。

原 `_next_passage_step(EXIT_CLEAR)` 的「直接去下一入口」須改成依**當前通道組／接應目標**選擇；需要連續出清的小平台例外才直通下一口。`_record_passage_commit` 的 crossed／cleared 仍只依成功物理commit更新，停在接應區不等於新一口已過。

### 3.5 轉彎、外部阻塞與等待不能死鎖

- 不隨導引路徑每個轉折立即旋轉100個slot。先完成在途邊，再在足夠空間發布一次局部重整；沒有空間則保持窄列沿真正走廊轉彎，到可展開處再展開。
- slot身分在直線行進維持；轉向／縮展只在明確revision重綁。不能每tick最近鄰重配，造成成員彼此交換目標。
- 「前方等隊尾」的hold只封鎖新的共同前進／超前意圖；後隊補位、已在core／egress中的出清、現有swap／push／batch完成仍要服務。
- 使用前排覆蓋、實際最大落後與通道組進度判斷，不以90%已到忽略最後一人；也不拿遠端固定終點99-slot的誤差當所有人的目前落後量。
- 隊长阻擋出口時只能先向已驗證的接應位置前進／讓位，不能站在口上等待全隊，也不能藉此一路衝到命令終點。
- 外部NPC暫占時顯示真實 WAIT_EXTERNAL，移除後恢復。若內部有合法可服務者而5秒沒有局部補位／通過／出清／整隊前進，仍視為無進度失敗；「正在等隊伍」不能成為永久免驗理由。
- 等待計時依真正commit或已驗證的布局發布進度更新，不得用插值、無效replan或遠處其他人的步數掩蓋卡死；結構規劃完成後不能靠不斷重啟規劃重設5秒時計。

## 4. 現有資料責任怎麼改

所有權仍在 `TerrainArmy`，`TerrainData.can_step()` 仍是合法邊唯一依據；TerrainLab只送命令／顯示。不得延伸正式World–Region–Site、Battle、存檔、地形生成器或新增每兵Node／NavigationAgent。

| 語義 | 沿用／最小調整 | 禁止混用 |
| --- | --- | --- |
| 命令目標 | `_march_goal` 等現有命令資料保存穩定目的意圖 | 不再等同 `desired_cells[0]` |
| 陣型參考位置 | 改造 `_formation_anchor_cell`、heading與offsets，由整隊步驟推進 | 不再每次從 `cells[0]` 自動搬走 |
| 共享導引route | 沿用現有一次有界尋路與完整snapshot；只由整隊導引消費cursor | 不能讓隊長個人commit提前消費整隊路徑；普通兵不能覆寫route |
| 活動目標 | 既有 `_formation_slot_cells`／`desired_cells` 明確作100人的當前步驟／接應目標 | 不把數十格外最終slot當目前隊形，也不與終點判定互相覆寫 |
| 最終布局 | 保留固定終點99個普通兵slot，另有隊長中排slot，與活動目標分開 | 中途完成一個活動slot，不等於命令完成 |
| 通道組 | 在既有descriptors保存相鄰口範圍、最近整隊區／接應slot與發布revision | 不另建第二套通道導航／reservation |
| 凝聚摘要 | 每tick一次掃100人產生最大落後、可追趕人數、前排覆蓋與hold reason | HUD不得觸發flood或每兵搜尋 |
| 共同平移 | TerrainArmy內短暫batch，使用既有owner／claims與movement欄位 | 不保存第二套單位位置，不新增外部manager |

優先改造現有欄位，不一次引入上述同義的新資料副本。沿用 OPEN／COMPRESSING／COLUMN／REGROUPING 與明確wait reason；不再套一層重複的全軍狀態機。

以下 captain-only 假設必须逐caller調整，而不是全檔把 `index > 0` 改成 `>= 0`：

- `_next_step`、`_next_passage_step`、`_passage_ready_for_entry`、`_record_passage_commit`、初始化ticket與phase：index 0需合法參與通道；99名普通兵原統計不變，另明列隊長跨口／順序。
- `_consume_path_route_step`、`_complete_move`與swap commit：個人到活動slot不再觸發命令終點／重發布全隊；共享導引與個人步驟分離。
- `_passage_complete`、`is_formation_complete`、`_finish_passage`：要求命令導引完成＋最終布局／99人完整出清＋隊長中排到位＋交易乾淨，不能中途hold就true。
- `_can_captain_swap_with`：不能作為隊長突破前排、繞过新行進節奏的後門；資料合法還需角色／通道順序資格。
- `_pending_push_matches_claims`、`_release_push_transaction`、`_prune_stale_reservations`及command drain：新共同步驟與既有在途安全規則協同；不要混成無條件cancel-all。
- `_unit_should_run`、`_step_duration_for`、動畫選擇：同一下一步context決策。

## 5. 驗收改成「沿途也要對」

先補會在現版失敗的反例，再實作。測試寫在既有formation／passage／width-transaction／default-edge／visual檔，最多新增一個專用formation-step測試檔；不另搭大型測試框架。

| 案例 | 必驗内容 |
| --- | --- |
| G01 開闊長直路 | 正式100人部署，整隊後連續移動；100個活動slot維持10×10且唯一；每個共同平移前後身份與offset一致，實際隊形不拉成單列；隊長中排、前方有真實連通的普通兵保護層 |
| G02 單人／多人掉隊 | 一次性合法設置落後fixture後不再改位置；前方在落後上限前hold，後隊下一個可開始的寬地步驟選RUN，補到≤1格恢復共同推進；前方不追著新anchor继续走 |
| G03 久等後解堵 | 後兵先被阻擋超過5秒，解除阻擋後仍能在合法寬地跑步追趕，不被舊blocked_time禁跑 |
| G04 原子平移 | 2×2小反例與真100人：destination由同批同向vacate可行，第三方占格／claim拒絕；全批开始与commit一致、0重复owner；换命令不半批回彈 |
| G05 單格口＋寬出口 | 100人含隊長通過；隊長不是首位，不可swap插隊。前隊在出口外接應，後隊出清後RUN；尚有人未過口時，下游已到寬地者不被全域COLUMN禁跑 |
| G06 兩口間大平台 | 第一口後真有100人可行布局，必須先收攏再推往第二口；不能頭到第二口、尾仍在第一口，而只靠最後99/99過關 |
| G07 小平台／長窄廊 | 獨立證明中間不能整隊，允許連續通過到下一可行區；不堵出口等待100人，也不在不可展開處強設10列 |
| G08 轉彎／回頭 | 90度、180度與坡道方向切換；不跨崖旋轉offset、不頻繁重綁、不瞬移，隊長恢復新方向的中排保護後才繼續 |
| G09 EDGE／FOLLOW目標 | EDGE為前緣接邊、隊長在內；FOLLOW前緣與PLAYER保距。活動goal變更不污染命令goal，不提前complete；PLAYER不動時命令目的不漂移 |
| G10 全部交接 | 單兵MOVING／兩種swap／push／全隊batch／出口接應期間換EDGE↔FOLLOW；在途完整commit、core出清、最新命令生效，claim／lock無殘留 |
| G11 預設D01／生成矩陣 | 同預設地形、出生與seed；五口99人逐人有序crossed／cleared，另验队长处于保护顺序；每段可整队处都恢复队形，最後99人固定slot＋隊長中排＋穩定5秒 |
| G12 等待與效能 | 有效hold期間後隊仍真實前進；內部無進度不能无限wait。结构256/frame、local4×1024/tick、push512/tick與原catch-up上限不變，正常dt不丟時 |

### 5.1 獨立 oracle 與原有矩陣

- 60Hz、30Hz、交替1/120與1/40；跑正式 `advance_frame`，不是只呼叫內部tick。
- 隊長保護用相同連通區／實際走廊進度與已指定前排，不能把牆另一側或另一層、幾何上位於前方的人算作護衛。
- 行進中記最大slot偏離、實際寬／長、隊長前方護衛、每個通道組spread、RUN的是哪些人、hold與補位commit；不能只驗結束的footprint。
- 保留V4／V5 W01–W05、E01–E12、F01–F03、Q01–Q05、M01–M02、X01、C01/C02、安全交換／推擠／owner與36組生成地形dt組合；只更新明確衝突的新語義，不刪掉難過seed。
- 最終普通兵99人／5s穩定／地形fingerprint／所有權與合法邊不放寬；隊長另有自己的通過與最终中排位置驗證。

### 5.2 必須明示的舊契約變更

1. 隊長個人最短253邊與本人抵達 `(99,75)` 不再是整隊行軍要求；保留「共享導引尋路正確、无无故折返、可达终点」的獨立測試。不能為讓舊assert過關把隊長從陣型中抽走。
2. 原D01／生成180秒、F01的20秒是上一版隊長先走模型的期限。**目前不改這些數字或先寫一個更大的常數。** S0保留舊結果；實作前以新路徑、必要通道流量與必需整隊停頓列出新時間預算，再比較實測。若無法維持舊期限，明列行為取捨與新基準，不能把timeout直接改PASS。
3. 5秒「有合法可服務者卻無物理進度」仍保留；有意等待要有別處正在完成本段工作的證據，而非拿hold當無限豁免。
4. V5四張「隊長已到終點、隊尾仍在高台」截圖不再是正面驗收。V6須驗中途的隊形、出清後接應與隊長中排，不可沿用舊圖宣稱新行為正確。

## 6. UI／畫面／效能交付

- 將 `Army: captain march to map edge` 改為整隊向邊界前進的文字；HUD分開命令目標、陣型參考點、隊長活動slot，避免仍顯示「隊長到邊界即成功」。
- 只顯示廉價摘要：共同前進／等後隊／局部過口／出口接應／重整、當前可用列寬、最大落後、追趕者數、前排保護及已完成本段人数。
- 至少實際檢視：開闊地行進中、後隊RUN且前隊hold、隊長居中的窄口通過、首批出口展開而尾部仍過口、大平台收攏後再次共同前進、最終整隊。
- 原子平移／追趕轉WALK／轉彎／swap／push／換命令保留連續幀和逐幀位置檢查。只有四張結束圖不足。
- 沿用Node2D地形、隊長現有presenter、99人baked atlas，不為改行軍規則重做資產／增加角色Node。
- 原效能腳本增列完整平地batch、多人補位、單口出清／接應、轉彎發布與五個實際坡口；不得挑全軍hold的idle frame當「進步」。保留未限速frame P95≤16.7ms、P99≤25ms、Army CPU P95≤2ms比較目標；同時報樣本數、最大值、>50ms、每類expansions與input/sim/drop。

## 7. 實作次序與停損門檻

| 階段 | 範圍 | 進下一階段的條件 |
| --- | --- | --- |
| S0 反例與語義 | 保存當前hash與V5資料；补G01/G02/G05/G09可失敗反例；鎖定隊長中排與前緣目標，列新時間預算 | 清楚證明現版行進隊形／跑步限制／先行問題；不能先改速度湊畫面 |
| S1 開闊整隊 | 分離命令目的與活動slot／導引；隊長中排；一格原子平移；凝聚hold與局部追趕 | G01–G04、基本目標／取消安全PASS；寬地連續GPU看起來是整隊走，非蠕動長蛇 |
| S2 通道與接應 | index0進相同通道、局部寬度與跑步context、最近接應區、必要通道組 | G05–G07、原E01/E02／多級／容量／EXEMPT正反例PASS；不能因hold退回舊入口死鎖 |
| S3 轉向及命令交接 | 轉彎發布、FOLLOW移動、EDGE前緣终点、所有commit／prune／cancel caller反查 | G08–G10與所有舊交易不變量PASS |
| S4 完整驗收 | D01＋36組生成dt、完整舊回歸、HUD／GPU連續幀與真活動效能 | 源碼／測試／log版本一致；沿途隊形與最終99人皆通過才報完成 |

任何階段若出現重疊、非法邊、claim遺失或半步回彈，先修共用提交／取消邊界，不准以瞬移、清鎖或停所有MOVING繞過。若平地一格同步平移尚未成立，不先把複雜山地cohesion gate全面打開。

遵守背景watchdog：本環境沒有 `schedule` 時僅用有界前景驗證；不啟動無timer的背景程序或subagent。實作完成前不得把本計畫或舊V5證據標成V6 PASS。保留使用者工作樹；不自行stage／commit。

## 8. 本輪交付與待確認邊界

原規劃輪只新增本計畫，當時未執行S0–S4；後續實作結果以上方更新與驗收紀錄為準。設計預設：隊長中排、普通步行共同推進、落後局部跑步、到邊緣以陣型前緣定義。這四點是為滿足使用者新要求而作的明示選擇；若後續要改成前排隊長、衝鋒或戰鬥陣型，另作不同命令，不以這次行軍規則暗中混入。

建議先實作 **S0＋S1「平地100人整隊同行、隊長不先走、後隊能追上」**，看過中途動態畫面並通過安全反例，再接上多坡口接應。最終仍需完成S2–S4才算整個需求完成，不能把第一個平地切片交成全部修好。
