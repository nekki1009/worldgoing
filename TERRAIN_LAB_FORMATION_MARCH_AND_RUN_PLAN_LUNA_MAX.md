# Terrain Lab 整隊行軍、狹口通過與自動跑步修正計畫

交付：Luna Max。日期：2026-09-09。狀態：規劃，尚未實作或執行本方案驗收。

本文件取代 `TERRAIN_LAB_CAPTAIN_LED_MARCH_PLAN_LUNA_MAX.md` 的陣型、集合、追趕與完成判定。沿用已存在的隊長路徑所有權與格子通行規則，不推翻 Terrain Lab，不整合 World / Region / Site，不改戰鬥。

## 1. 使用者要的行為

借鑑 Total War 的「隊伍整體行軍」，不是複製其引擎或加入戰術系統：

- 開闊地：100 人以寬正面陣型一起移動，不是 99 人追逐隊長的歷史腳印。
- 轉彎：隊伍逐步改變朝向，隊員走到自己的新陣位，不瞬移、不交叉穿人。
- 狹口：只有實際通行寬度不足才收窄；兩格寬可雙列，一格寬才單列。
- 出口：先出去的人離開出口、填入集結位置，讓後面的人持續通過；全隊通過後恢復寬陣型。
- 追趕：隊員落後自己的陣位時跑步，接近時回到走路。隊長遠離 PLAYER 時也跑步，不再讓所有人固定一種速度。
- 一格仍只能容納一人。坡道、峭壁、水、占格與交換規則不因陣型或跑步失效。

一格寬山口必然排隊，但不能把山口的隊列模式套用到整張空地。

## 2. 現況審核：必須修原因，不只調參數

已讀取目前檔案確認：

| 位置 | 現況 | 問題 |
| --- | --- | --- |
| `terrain_army.gd::_desired_formation_mode` | MOVE_TO_EDGE 無條件 COLUMN | 空地也必然排隊，沒有自動恢復 |
| `_formation_preferred_cell` | 第 i 人追第 i 個歷史格；不足時往後延伸 i 格 | 99 人拖成近百格長龍；隊長停止後仍可能把隊員留在山上 |
| `_route_is_narrow` | 看高度變化、鄰接度 | 高度變化不等於只有一格寬；沒有量陣型能通過的寬度 |
| `_update_formation_targets` | 朝向或高度變就重排；直接 preferred 只驗 is_walkable | 目標可能跨不可通行峭壁；fallback 自己位置也可能撞到已分配陣位 |
| `_next_follower_step` | Manhattan 靠近陣位或共享場下降，兩者任一成立即可 | 兩個目標互相拉扯，可能折返、卡住；沒有明确的進口／出口階段 |
| `_captain_should_wait` | EDGE 與 COLUMN 不等隊員 | 隊長到終點不代表整隊完成 |
| `_update_formation_status` | 依離隊長距離判掉隊 | 正常後排也被當成掉隊，陣型尺寸和追趕誤差混淆 |
| 移動／顯示 | 全員 MOVE_DURATION=0.24；每格 smoothstep | 沒有跑步模式，每格反覆加減速；固定 tick 超出的時間也需審查 |
| 士兵圖集、baker | 只有 idle / walk | 不能只把位移加速就宣稱有跑步動畫 |
| `terrain_lab_army_test.gd` 多層測試 | 驗證隊長 3→2→1→0 抵達與唯一占格 | 沒要求 99 人全部下山、集合完成 |

保留目前 `TerrainArmy` 集中模擬、packed arrays、隊長 live 3D presenter、99 人共享 baked atlas 的呈現。不要恢復 99 套即時 3D 骨架，也不要為本次額外換渲染架構。

## 3. 責任與不可破壞的契約

仍由 `terrain_army.gd` 負責隊伍命令、共享導航、陣位、預約和移動提交。`TerrainData.can_step(a,b)` 是通行權威，Renderer 不參與通行決策。

1. 只有隊長／隊伍的一條主路徑通往 PLAYER 附近或真實地圖邊緣；普通兵不得提交全圖終點搜尋，不得覆蓋隊長 route/cursor。
2. 分清三件事：命令終點、整隊陣型錨點、個別士兵陣位。不是所有人追同一格。
3. 錨點由隊長已提交位置及当前路段導出；過口／終點可使用明確的出口或內側集結錨點。不要另建一個能無限跑在隊長前面的虛擬軍隊。
4. 導航查詢可忽略友軍作為永久障礙；真正走一步時必須检查当前占格、目的預約、外部角色、反向交換與 can_step。
5. 每個陣位必須唯一、可達。找不到陣位必須顯示 formation blocked，不得拿重複 fallback 格假裝分配成功。
6. 改命令可取消未執行路徑／陣位工作，不能取消半格移動造成瞬移或丟失預約。
7. 模型尺寸、腳底 anchor、角色方向契約不變。跑步不是縮圖、位移貼圖或跳過地形。

## 4. 陣型狀態：補完原有模式，而不是建立第二套控制器

保留 OPEN / COLUMN / REGROUPING，增加必要的收窄過渡狀態 COMPRESSING。另用 `formation_width` 表示列數，不為每一種寬度建立狀態類別。

| 模式 | 目標與轉換條件 |
| --- | --- |
| OPEN | 預設 10 格寬；足夠空間時保持穩定陣位、同步推進 |
| COMPRESSING | 已辨識前方瓶頸；在入口前重新排成可通過寬度，不在入口格停下整隊 |
| COLUMN | 以固定瓶頸、列號及通過進度行軍；寬度可 1～9，不能按兵號追歷史腳印 |
| REGROUPING | 出口有可用區域後逐步填入寬陣；後方仍在過口，已出者不得堵門 |

### 4.1 開闊地陣位與朝向

- 100 個格子包含隊長，預設 10×10；隊長占前排靠中央的一格，其餘 99 個才分配隊員。不要把隊長放陣型外又額外造成長度偏差。
- 陣位以整數格計算，偶數寬左右偏一格是格子選擇，不准半格占位。人物渲染仍在格中心。
- 兵與 slot 的配對只在首次集合、陣型寬度變化、必要的轉向或 slot 失效時重算；正常直行只平移 slot。
- 首次配對依現有位置在前後／左右方向排序，再按列排入；距離相同以兵號決定。不要用兵號決定穿越整隊的路線，不需要 Hungarian 演算法。
- 路線前看數格取得穩定 cardinal 朝向，不跟每次左右鋸齒步進旋轉整隊。90 度轉彎在足夠空間時重整，窄處沿通道前進，出來才轉寬陣。
- 陣位投影須檢查 can_step 可達性，不能把峭壁另一側的近格當成可用陣位。形狀不適合完整矩形時可暫時縮寬，不能把目標散射到周圍不同平台。

### 4.2 通道寬度與跨層

重寫 `_route_is_narrow` 為回傳瓶頸描述的檢查，至少包含入口、出口、路段範圍與可通過列數。

- 前看約 12 格主路徑，沿路段橫向檢查連續可行走通道；不只數當前格四鄰居。
- 每個候選橫向 lane 都要有縱向連續合法 can_step，包含坡道方向。十個可站格不代表十條能跨高度的路。
- 所有格只要高度改變就切單列的判斷必須移除。寬坡保留寬度；一格坡道才單列。
- 收窄需提前；放寬須有連續出口淨空與實際可用集結格，避免每格 OPEN/COLUMN 閃爍。
- 若隊伍仍跨在同一瓶頸兩側，保留瓶頸資料和個人通過狀態，不因隊長離開就丟掉入口。
- 彎道不足以容納寬陣時也收窄，不能旋轉後把 slot 丟到牆裡。

### 4.3 排隊只限瓶頸，離開後主動讓出出口

- 進口順序根據實際到達位置／行進進度決定，固定到此瓶頸結束；不能讓 index 1 從後排插到最前面。
- 個人階段至少能分辨：進口前、通道內、已通過出口。以已提交格與合法路段進度判斷，不以直線距離猜是否通過。
- 出口與必要延伸通道設成禁止停留的通行區。已出去者先往遠側／外側集結格走，不能停在口上等後排。
- 接近出口的 slot 延後填入，避免先到的人把後續隊員包在外面。
- 全隊通過、所有 slot 可達且陣型穩定後轉 OPEN。若接著還有狹口，繼續壓縮行軍，不能硬展開再立即收窄。

## 5. 導航與讓位：只共享遠程方向，局部處理陣位

### 5.1 主路徑與共享距離場

- 命令成立即開始隊長的主路徑工作；跨層不能靠貪婪走幾步再碰運氣找路。
- FOLLOW 的玩家未實質移動、原目標仍有效時沿用主路徑。隊長走一格不是換終點的理由。
- 保留现有 packed BFS、route/cursor 與幀預算。共享場根依階段指向入口、出口或集結區，不一律是隊長當前格。
- `_ensure_distance_map` 現有同步全圖重建也要納入同一導航預算；場採命令／地形／目標版本，完成後才換入，不能讀半張新場。
- 正常移動不要每人每幀重建全圖場。每幀導航建議起始上限 512 次展開，主路徑優先，剩餘供共享場與局部修正；數值可量測調整。

### 5.2 遠處與近處使用不同目標

- 遠離所屬區域：沿共享場往当前階段目的地；不要同時追跨峭壁的 slot Manhattan 距離。
- 進入陣位鄰域：轉成自己的 slot 修正，不再被共享場拉向隊長腳下。
- 近處被地形擋住：允許有預算的局部搜尋，半徑初值 8 格、每次最多 128 次展開；納入上述總預算，不是給每人各 128。
- 遠／近切換需遲滯，例如距 slot 所在合法集結區 6 格內轉局部、超過 9 格回共享引導。這裡的距離是可通行圖距離，不是穿牆直線。
- 暫時無局部路徑要區分友軍阻塞和地形不連通：前者等候／讓位；後者重新選合法 slot／縮寬，不能永久重試原無效格。

### 5.3 占格、交換及死鎖

- 保留隊長與隊員的原子交換，但雙方必須可反向 can_step 且無第三人預約；交換同一時刻提交。
- 刪除「只要 COLUMN 就能反覆互換」的寬鬆觸發。交換要解決明確互等或讓出必要通道，記錄最近 pair 避免來回互換。
- 一般優先序：正在離開出口者、通道內前進者、隊長必要下一步、等待較久者；不能永遠兵號小先走。
- 已站定但擋住合法通路的士兵需暫時讓到合法空格，走完再回自己的 slot；保留 slot 身分，不把讓路格當永久陣位。
- 下一步預約由同一 owner 仲裁；同一對向邊只允許明確交換，普通走路不可穿越。
- 堵塞計時看「路線進度／slot 誤差有沒有改善」，不是只看是否曾移動；來回走和來回交換不算進展。
- 同一位置 3 秒無進展先局部修復；10 秒仍無進展顯示具體阻塞原因與格位。不可瞬移或放寬碰撞來讓測試過關。

## 6. 隊長與隊員自動跑步

### 6.1 距離定義

- 隊員追趕誤差 = 到自己當前階段目標的合法路徑長度，扣除該隊列位置原本應保持的間距。正常後排不是落後。
- 窄口被前人擋住屬於排隊，不是需要加速衝撞。沒有可預約下一步時不能播放原地奔跑。
- 隊長 FOLLOW：以主路徑上到合法跟隨位置的剩餘距離判斷；不是直接到 PLAYER 的直線距離。
- 隊長 EDGE：遠離終點且全隊能跟進時可跑；陣型散開時等候或走路。不能「隊長永遠跑，隊員永遠追不上」。

### 6.2 初始參數與優先序

建議先對齊現有測試角色 `MOVE_DURATION=0.38`、`RUN_DURATION=0.18` 秒／格；目前軍隊 0.24 與 PLAYER 步速不一致，不能只加一個更快常數。集中軍隊設定並記錄校準結果，本次不擅改 PLAYER 操控。

| 項目 | 初始值／行為 |
| --- | --- |
| 隊員自動跑 | 落後 slot 超過 6 格進 RUN，回到 2 格內進 WALK |
| 隊長 FOLLOW | 距跟隨目標超過 10 格進 RUN，4 格內進 WALK |
| 隊長 EDGE | 剩餘路程超過 16 格且隊形可跟進時 RUN，6 格內 WALK |
| 瓶頸 | 入口／通道內預設 WALK；出口清空後可 RUN 追陣位 |
| 整隊分散 | slot 誤差 P90 > 8 格時隊長停止加速；> 12 格在安全位置等候，恢復到 P90 < 4 且無滯留成員才續行 |

P90 不能忽略最後幾名：另追蹤最老的未通過成員與最大落後誤差，必須處理其阻塞原因。隊長等候點不可在坡道、出口或單格通道上；先清空瓶頸再等待。

速度優先序：合法通行／預約 > 清空瓶頸 > 集合協調 > 自動跑步 > 平常走路。跑步只是速度和動畫，不是尋路模式。

### 6.3 移動時鐘與動畫必須同時接入

- 加 packed `locomotion_mode`、`step_duration`／`step_elapsed` 等必要欄位；每次合法起步凍結當步時長，中途切 RUN 只影響下一步。
- 交換 pair 使用相同時長（採兩者較慢值），共用完成時刻；不能一人先抵達還占著對方格。
- 固定模擬 tick 保留，但不能完成 0.24 秒步伐時丟掉 tick 超額時間。連續行走保留餘額，停下則不累積「未來爆衝」額度；每 tick 設有限完成步数上限。
- 渲染依各人同一移動時鐘插值，不再全員除以 MOVE_DURATION。連走改線性位移，不要每格 smoothstep 都煞車；停走過渡留給動畫即可。
- 30 / 60 / 144 FPS 下相同命令、相同經過時間應有相近進度。超出追幀上限需記錄 dropped simulation time，不准用丟時間掩蓋效能。
- 隊長 presenter 根據 locomotion 使用既有 `run` 動作；普通兵 baker 新增四方向 run，重建 atlas、manifest、匯入資源。
- 新圖集的共同裁切範圍需覆蓋 idle/walk/run 全部動作。所有 frame 保持相同腳底世界錨點及角色比例，不能靠每 clip 縮放補裁切。
- 動畫播放由移動速度／步態決定；idle、walk、run 切換不應每幀重新播放第一幀。不得拿 walk 加速當最終 run 驗收。

## 7. 邊緣命令與完成判定

- 保留一個隊長真實邊緣目標；普通兵沒有個人邊緣終點。
- 選目標時一併確認其內側存在可達集結區，能容納 99 人且不占 PLAYER/NPC、不堵最後出口。優先選可集結的可達邊緣，而非只取最近一格。
- 邊緣只有一格入口時，隊長可保留邊緣終點、隊伍錨點留在內側；不能把 10×10 中心放到圖外，或把 99 個歷史格當停靠位置。
- 最終 COMPLETE 必須：隊長已到真實邊緣；99 人均已通過必要瓶頸；99 個唯一有效 slot 全數到位；無移動／交換未提交；無人仍停在通行區。
- 終點與高度變化、狀態切換時強制同步陣型錨點，不能因「未走滿 3 格」仍使用舊錨點。
- 無法容納全隊時回報 INSUFFICIENT_RALLY_SPACE，保持合法停止；不得宣稱已完成，也不自動刪兵。

## 8. 預計修改檔案與順序

1. `scripts/terrain_lab/terrain_army.gd`：先移除 EDGE 強制單列；補陣位唯一／可達契約、OPEN 同步直行；再做瓶頸寬度、通過狀態、出口集合；最後接跑步、時鐘與 presenter。
2. `scripts/terrain_lab/terrain_lab.gd`：既有 HUD 加 formation width、phase、隊長步態、跑步人數、通過瓶頸人數、slot 誤差、最久阻塞、路徑／場版本、導航用時；不新增正式遊戲 UI。
3. `scripts/tools/bake_terrain_army_soldier.gd`、`assets/characters/terrain_lab_army/standard_soldier/`：新增 run 四方向，保留共用 anchor 和目前 baked runtime 接口。
4. `scripts/tests/terrain_lab_army_test.gd`：補全隊行軍／過口／跑步契約，主要整合情境走 `advance_frame`，不只直接呼叫 `_simulate_step`。
5. `scripts/tests/terrain_lab_army_visual_test.gd`：改成能看見整隊與瓶頸全程；近景驗證隊長、士兵的 walk/run 與腳底。
6. `scripts/tests/terrain_lab_army_performance_test.gd`：延長移動採樣、同時量實際隊伍進展，不能只有動過一步就 PASS。
7. `TERRAIN_GENERATOR_LAB.md`：實作完成才更新操作、行為、限制與實際結果。

不要新增每人 NavigationAgent、CharacterBody、完整 AStar 工作佇列或新的 ArmySystem。若 terrain_army 文件過大，先刪除被替代的 trail 目標／舊分支；本次不以拆檔本身當成果。

## 9. 實作里程碑與停止條件

### A：先做開闊地整隊

空白 100×100 地圖，100 人集合成 10×10，向前移動 30 格，停下重新到位。驗證普通兵沒有主路徑請求、同時多列移動、不重疊。通過才開始下山。

### B：收窄、通過、恢復

先做直線單格口，再做三格口，再用既有三層下山夾具。必须看到第一人出去讓出出口、最後一人通過、99 人重新到位。只隊長到達不算通過。

### C：跑步與動畫

兩個命令皆接隊長步態；隊員掉隊可跑、正常後排不跑；全員受預約約束。重烘 run 並目視確認，不能只過資料測試。

### D：預算、死鎖回歸、真實生成地圖

再處理全圖場重建尖峰與局部搜尋調度；性能測試同時持續有進度。上述任一阶段失敗需修完，不可用未完成模式當最終交付。

## 10. 必跑驗收矩陣

| 情境 | 必須驗證 |
| --- | --- |
| OPEN 直行 30 格 | 中途至少 8 列、不是單列；至少 90 人 slot 誤差 ≤2 格；停後 99/99 到位 |
| 90 度轉彎 | 無瞬移、非法跨格、重複占格；有空間時重整後恢復寬正面 |
| 一格口／三格口 | 分別合理收成一／多列；不拿相鄰可站格假冒可通行 lane；100 人全部通過 |
| 既有 3→2→1→0 地圖 | 不只 captain visited；逐人記錄合法過口，99 人最後到低地合法集結格 |
| EDGE 寬終點／唯一邊缘口 | 隊長真實邊緣；隊員內側集合；無人堵最後入口，99/99 才 COMPLETE |
| FOLLOW 玩家遠離再停下 | 隊長 RUN→WALK；隊員落後 RUN→WALK；正常後排不因距隊長遠而誤跑 |
| 追趕遇狹口 | 不穿人、不跑過水／峭壁，不原地播放 run；出口持續釋放 |
| 隊長被隊員擋住 | 合法交換或讓位；無來回交換假進展 |
| 命令在半步／交换中替換 | 占格仍唯一、預約不丟失，新路徑版本不讀舊工作結果 |
| 陣位／邊緣不可達 | 有明確原因與停止狀態，沒有永久無聲抖動或假 COMPLETE |
| 30 / 60 / 144 FPS | 同一 60 秒直行／追趕序列，移動路程差控制在 1 格內；檢查時鐘與預算 |

全隊通過測試需有有限超時：以主路徑步數、瓶頸人數和 walk 時長算合理上限並加裕量，列印實際耗時。既有長繞路三層夾具初始上限可设 240 模擬秒；超時算失敗並輸出卡住士兵／slot／瓶頸，不能只印 PASS。

測試入口必須確實回傳非零失敗碼；子函式 assert 後外層仍印 PASS 的情況要排除。模擬契約、視覺與性能分別報告，不可互相替代。

### 視覺驗收

- 至少錄下 OPEN 直行、收窄、通道內、出口清空、重整完成五個階段；鏡頭框住整隊，不只隊長。
- 另外拍四方向 walk/run 近景，確認方向、比例、腳底和無裁切。
- 截圖等待實際渲染完成，必須親自打開檢查，使用可載入的絕對路徑提供結果。
- 在實際 TERRACED_HIGHLAND 與 RIVER_VALLEY 各跑至少一個固定 Seed，記錄起終點；若生成地形本來無可達邊緣，另報無路結果而非跳過碰撞。

### 性能驗收

沿用原 renderer／解析度／硬體設定比較修改前後，分別採樣無軍隊、100 人待機、OPEN 行軍、瓶頸過口。每項預熱至少 5 秒、採樣至少 30 秒。

報告 frame P50/P95/P99/max、軍隊模擬／導航耗時、場重建次數、每秒合法提交步數、通過出口數、阻塞人數；60 FPS 是目標，不是預先宣稱的结果。至少確認主路徑／共享場沒有單次同步全圖尖峰，純導航每幀先以 2ms 為調校目標。達不到須提供實測與原因，不能讓軍隊少走或停止來換 FPS。

## 11. Luna Max 交付要求

只在本 Lab 範圍實作。交付須列：實際改檔、移動行為差異、全隊過口與集合耗時、跑步切換結果、實際預覽、性能數字、未完成項目。

本次硬性底線：空地保持寬陣，狹口可收隊且能全部出去，隊長與隊員均能合理跑步，人物和通行規則不被破壞。若只完成「隊长到達、剩餘士兵排長龍」，此方案仍未完成。
