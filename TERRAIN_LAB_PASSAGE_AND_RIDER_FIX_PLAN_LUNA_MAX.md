# Terrain Lab 山口卡死／抖動與騎乘陷入修正方案

交付對象：Luna Max。日期：2026-09-09。狀態：方案；本次只檢查程式並建立文件，未修改遊戲程式或執行新的 runtime 驗收。

本方案優先於 `TERRAIN_LAB_FORMATION_MARCH_AND_RUN_PLAN_LUNA_MAX.md` 中與實際程式不一致的內容。修正既有 TerrainArmy、MountHorse3D 與 HumanCharacter3DEditor；保留一格一人、TerrainData.can_step、隊長主路徑、99 人共用 baked atlas、共享角色呈現與固定模型尺寸。

## 1. 驗收目標與範圍

1. 100 人經過山口時持續前進；允許正常排隊，不允許原地交換、來回一步、反覆改目標或永久堵門。
2. 已過口的士兵離開出口，進入出口側合法集結區；未過口的士兵依序通行。三個錯位坡道全部通過後，隊長與 99 人都在終點側集結。
3. 開闊地維持寬陣型，窄口依實際寬度收窄；遠距離隊長與隊员可跑步，入口等待不可播放跑步。
4. 騎乘時臀部坐在馬鞍／馬背上，身體不陷入也不懸空。待機、慢走、跑馬、揮劍、槍刺及切換都要成立。
5. 不改 terrain_cell、模型尺度、地圖像素／公尺契約來掩蓋騎乘問題。編輯器與 LAB 使用同一套修正。

不增加戰鬥、世界整合或新的群體渲染框架。工作分成山口與騎乘兩個可獨立驗收的修正。

## 2. 已確認的程式問題

以下是本次靜態檢查的結果。行號可能變動，以函式名稱定位。

| 檔案／函式 | 目前行為 | 修正意義 |
| --- | --- | --- |
| `scripts/terrain_lab/terrain_army.gd::_update_formation_targets` | force／mode 改變時依距離配對士兵與槽位，但後續普通更新直接用 `slot_targets[index]` | 配對身份沒有保存；原本分給 A 的槽位下次可能變 B，造成交叉、折返 |
| `_formation_preferred_cell` | COLUMN 仍依兵號從隊長歷史尾端取第 N 格 | 隊長停下時後排永久目標可能還在山口或上一層；缺少「過口後前進到出口集結區」語意 |
| `_desired_formation_mode` | 用所有人抵達目前槽位作為瓶頸解除條件 | 舊槽位本身壓在通道上時，形成循環等待；到位不等於已過口 |
| `_follower_navigation_goal` | 高度相同就直接追目標；高度不同挑最近的歷史高度轉折 | 同高度可能是不同連通平台；也沒有記錄該士兵已通過哪個入口 |
| `_simulate_step` | `_schedule_follower_blocker(occupant, delta, {index: true})` 未傳 requester | 第一層呼叫 requester=-1，依 requester 啟動的推讓分支不會在這一層執行 |
| `_pending_pushes`／`_advance_pending_pushes` | 有推讓鏈，但沒有完整參與者鎖、版本或逾時取消；正常排程仍可處理參與者 | 推讓未完成前，同一士兵可能接到另一個移動／交換；鏈失效後可能殘留 |
| `_find_push_chain` | 搜尋時不完整排除已屬其他交易的士兵 | 多條鏈可能爭用同一人，需明確排他與提交規則 |
| `_simulate_step`／`_begin_follower_swap` | 主排程遇占格可直接嘗試 follower swap；局部 BFS 也可能回傳占用格 | 必須統一交換資格，不能讓不同入口繞過互惠目標條件 |
| `_local_path_cursor` | 只允許靠近游標的八個兵號，游標主要在搜尋成功進入時前進 | 需驗證已到位單位附近是否導致其他等待者飢餓；以明確輪詢取代隱含窗口 |
| `_rebind_stalled_open_formation` | 停滯後把現在位置當作新槽位，可能直接算 99/99 | 不可用這個判定證明完整隊形成功，否則停滯被包裝成完成 |
| `human_character_3d_editor.gd::_calibrate_rider_mount_pose` | `global_position += socket_world - hip_world`，將骨盆中心貼到 Socket_Rider | 骨盆中心不等於臀部接觸面；需量測 socket 與坐面語意後修正 |
| `_attach_rider_to_mount` | 先套 `MOUNT_RIDER_OFFSET=(0,-1.20,0)` 再校正 | 不應盲調此常數；後續骨盆對齊會抵銷其效果 |

先前執行紀錄中的多層測試只到 55/99，延長模擬後步數不再增加，因此它是未完成且停滯，不應描述成正常等待。現有測試只要求部分集合仍會 PASS，必須補正驗收。

騎乘陷入的視覺原因目前是待驗證假設：本輪尚未量測模型坐面或拍攝新騎乘畫面，不能直接宣称只提高固定數字即可解決。

## 3. 山口修正：資料與責任

所有移動決策仍集中在 TerrainArmy。只補足以下最小資料，不建立第二套 FormationController。

- `command_revision`：每次新命令／重新生成遞增，過期路徑、槽位與推讓不得提交。
- `unit_slot_id`：PackedInt32Array，保存兵號到槽位的配對；`slot_cells` 保存槽位位置。每次平移只更新 slot_cells，使用 `desired_cells[i] = slot_cells[unit_slot_id[i]]`。
- `unit_passage_stage`：每人已完成的通道序號；通道內另有 APPROACH、CROSSING、EXITING、RALLY 狀態即可。
- 隊伍共用 `passages`：從隊長合法主路徑導出的少量通道描述，含入口／出口格、route index 範圍、連續可通行列數、出口側集結格與走廊格集合。
- `unit_transaction_id`：每人目前是否被推讓／交換占用。沿用 `_pending_pushes` 的容器，補版本、參與者、下一步及進展時間；不用為每人新增 Node。
- `last_progress_tick`：只在已提交移動、通道階段前進或合法交易完成時更新。換目標、換狀態、來回震盪不能重置「有效前進」統計。

已有等價欄位就擴充沿用；被取代的游標／舊推讓分支要移除，避免兩份狀態同時運作。

## 4. 先修配對，再修通道，不先堆更多搜尋

### 4.1 槽位身份穩定

1. 首次、明確模式切換或局部槽位失效才重配；普通三格重算、隊長朝向小變化與每 tick 更新不能重新綁兵號。
2. 過口期間不把已通過者配回入口側，不把未通過者標成已到位。配對需尊重通道 stage／路徑進度，不能單看 Euclidean 或 Manhattan 最近。
3. 停在已有合法集結格的人優先保留該槽位。只重配受影響者，不讓 99 人一起重排。
4. 槽位失效時明確標記 pending／blocked；沒有足夠合法格就不能回報整隊完成。
5. 移除以停滯直接改寫 desired=current 來報完成的行為。若容許非矩形集結，須先通過同側、範圍、連通性、走廊不占用及唯一格檢查，再採納為有效陣位。

### 4.2 通道與出口集結

1. 由隊長主路徑辨識受限路段及合法跨高度邊，連續窄段合併。橫向寬度只計與主通道連續、且每一步 can_step 的 lanes；不能把另一側隔著峭壁的格數進來。
2. 每個通道有固定版本與方向。已進入通道的人沿該版本完成出入口，不因隊長走出十二格前看範圍就丟掉通道。
3. 出口建立不可停留的通行帶：包含坡道兩端及出口後 2～3 格的合法前進路段。這些格能通行、不能分配最終集結槽位。
4. 在出口側以合法連通搜尋建立集結區，排除入口逆向邊、通行帶、外部角色及重複槽位。多層錯位山口不能沿高度值推定連通性。
5. 已過口者目標向外填入集結區，優先填遠於出口且不需穿越已填槽位的位置。若出口區不足以容納 100 人，隊伍繼續向下一片可用區推進；不能要求所有人先塞在小平台上才允許前進。
6. 隊長到邊界後在地圖內側建立最終集結區，隊長的邊界格保留給自己；99 人不以百格歷史尾巴為最終目標。
7. 個人可以先 EXITING／RALLY，後方仍 CROSSING。全隊 mode 可以仍 COLUMN，但每個人的目的不同，不能等 99 人都站進通道才開始展開。
8. 通道 stage 只在實際跨越該合法邊且離開出口帶後提交。到同高度、距離很近或視覺動畫已播完都不能當作通過。

### 4.3 導航與局部避讓

1. 隊長仍擁有唯一命令主路徑。隊員依當前 stage 追入口、通道下一格或出口槽位。
2. 刪除 `_follower_navigation_goal` 的「高度相同所以直接追終點」捷徑；以通道進度／實際連通可達性判斷。
3. 整隊共用到當前入口或集結區的導航場／連通查詢。只在地形或通道目標變更時重建，別對每人每 tick 建全圖 BFS。
4. 現有局部搜尋保留固定預算，目標是短距避讓。有限搜尋沒找到不等於永久不可達，不可原地無限重跑同一請求。
5. 候選方向以合法路徑進度為主。避讓步可以短暫離開槽位，但需有明確交易目的與結束；禁止兩種啟發值輪流拉回。
6. 排程以通道前後順序、等待年齡及兵號做穩定排序；兵號只作最後 tie-break。搜尋輪詢每 tick 前進，即使選中者已到位或無需搜尋，也不可卡住整個游標。

## 5. 統一推讓與交換交易

這一節一次替換舊的递迴推讓與新增 chain 重疊部分；不能只加第三條 fallback。

1. 主呼叫明確傳 requester。先記錄目前漏傳是否讓分支不執行，再修所有呼叫點。
2. 交易建立前驗證整條鏈：command revision、參與者尚未移動／交換／加入其他鏈、每一步 can_step、外部角色與目的格預約。
3. 成功時鎖住所有參與者。一般排程、其他 push、swap 及槽位重配跳過鎖定者。
4. 從空格端向內逐步移動；前者提交占格釋放後才允許後者進入。任何瞬間仍維持一格一人，不能先清掉整條鏈占格。
5. 每次執行前重驗 source owner、destination reservation 與地形。目的暫時占用可等候；地形失效、版本過期或交易無進展逾時則取消尚未開始的步驟。
6. 取消只釋放該交易自己的鎖與預約。正在插值的合法一步正常提交後再解鎖，不可瞬移回原格。
7. 若交易完成的讓路者要回原槽位，必須等待 requester 通過／下一 tick 重新規劃，不能立刻回堵入口。
8. follower swap 必須統一經過同一個資格判斷：合法相鄰雙向邊、互惠目標或明確可證明前進的交換、交易排他、pair cooldown。主排程不能繞過此判斷。
9. 正常單列排隊僅等待前人釋放格，不啟動反向 swap 或把前人推出出口。推讓只處理真正錯位阻塞。
10. 長時間阻塞先記錄原因，再做一次受限恢復：取消過期交易、清理自己失效預約、重建受影響局部配對。沒有進展要顯示 blocked，不能偷偷宣告 assembled。

## 6. 山口驗證與效能門檻

擴充 `scripts/tests/terrain_lab_army_test.gd`、`terrain_army_formation_smoke_test.gd`，保留既有 swap、合法步進與隊長路徑所有權測試。使用必要的整合 fixture，不為每個 getter 建測試。

| 場景 | 必須驗證 |
| --- | --- |
| 100×100 平地，跟隨與移往邊緣 | 寬陣型、99/99 真實到位；槽位唯一且未用停滯 rebind 偽造成功 |
| 一格山口／雙格山口 | 通行數持續增加；一格單列、雙格能利用兩條合法 lane；出口沒有停留陣位 |
| 三個錯位坡道 3→2→1→0 | 隊長與全部 99 人到終點側，最後完成集結；55/99 必須 FAIL |
| 相同高度但被水／峭壁隔開 | 不能因高度相同直追隔壁，需沿已知合法通道 |
| 已到位者堵出口、密集初始部署 | 可有有限推讓，交易完成、鎖歸零、無週期性交換 |
| 隊伍一半過口時改命令、清除、重新生成 | 過期交易不提交；無重疊、無殘留預約、無瞬移 |
| PLAYER／NPC 暫時站在入口再離開 | 等待時不抽動，障礙離開後恢復；永久障礙可明確 blocked |

每 tick 檢查占格與已排程邊合法；停滯判定同時看「未通過人数、到出口區的路徑進度、未到位槽位誤差」。單純 completed_steps 增加可能只是來回走，不能作唯一成功條件。

三坡道 fixture 建議上限 360 模擬秒；最後 30 模擬秒有效進度不變且仍未完成即 FAIL 並輸出阻塞摘要。測試失敗要非零退出，且不得再印總 PASS。較短的單口測試用路徑長度、人数及實際步速估算合理上限。

Debug 摘要只在狀態變更／失敗輸出：unit id、slot id、stage、route progress、source／next／goal、owner、reservation、transaction id、wait reason、last progress。禁止每幀刷 100 行。

重跑 `terrain_lab_army_performance_test.gd`，平地及山口分開量，固定硬體／解析度／seed，各 warmup 後採樣至少 30 秒。記錄模擬 tick、導航與配對耗時、frame p50/p95/p99/max、nodes、active 3D sources。先前短測曾有約 105ms 尖峰，不能只報 61 FPS。

預算初值：每 tick 最多兩筆 256-cell 局部擴展，最大全圖共享工作拆為 256-cell 批次；依實測調整，但必須保留公平性。若不足以找到路，續算或用共用場，禁止反覆從零開始。驗收期待 60 FPS 目標硬體下 p95≤20ms、p99≤33ms，且本次軍隊工作不再產生反覆的 100ms 尖峰；未達要列實測與剩餘原因。

## 7. 騎乘修正：先確認插槽語意

責任檔案：`scripts/mount/mount_horse_3d.gd` 提供馬鞍／馬背坐面參考；`scripts/ui/human_character_3d_editor.gd` 負責騎手掛接與姿勢；`scripts/ui/character_render_contract.gd` 保留投影尺度；`terrain_test_character.gd` 只發出 mounted／animation 狀態。

### 7.1 量測與基準

1. 讀取現有男女模型、`standard_horse_pack.glb`、Socket_Rider、saddle seat 與骨盆骨。量測 socket 在模型局部與世界座標的位置、旋轉、尺度，以及與馬鞍坐面的關係。
2. 捕捉修正前 ride_idle 的側面、正面、背面、3/4 與 LAB 實際視角。加暫時可開關的小標記表示 socket、骨盆中心與接觸點。
3. 骨盆中心不是臀部表面；以模型座姿的臀部接觸點到骨盆的向量作校準值。可用一個共用參考點加男女模型必要的固定差值，不以頭盔／武器／鏡頭方向另加偏移。
4. 不以整個角色或馬的 AABB 最高／最低點當坐面；尾巴、頭、武器都會污染結果。坐面取命名參考或核對過的座墊幾何，最後用實際輪廓驗證。

### 7.2 唯一定位公式

目標：`騎手坐面接觸點 = 馬的坐面參考點`。

- 在共同座標系中計算平移差，再將校準結果保存為 attachment local 的絕對變換；不要每次從上一輪結果累加試錯。
- 如使用骨盆作 proxy，目標必須是「坐面 + 量測出的骨盆相對坐面向量」，不能直接 hip=socket。
- 旋轉基底與向量轉換要照實際 transform 計算，不能依舊註解假定「轉到 local 會翻轉所以不能轉」。正確 inverse basis 必須能處理該 socket 的旋轉與尺度。
- 保持 model_root.scale、馬尺度、terrain_cell、Sprite2D 的地面 anchor 不變；只調騎手相對馬的掛接姿勢。
- 基準在固定參考姿勢與時間取樣，重複校準需具冪等性。不能在任意動畫時間採樣，再讓同一動畫從不同時間開始產生不同坐姿。
- 馬的 bob 由骨骼插槽跟隨；人物保留原本動作。先調共用 mount bind 位置，僅在確認某 clip 的 authored root offset 不同時新增該 clip 的小型校準資料。
- 不每幀強制骨盆完全貼死而抹掉跑馬／攻擊動作；也不把動畫從 clip 中刪掉。若 animation track 會覆寫校準根，可在 attachment 下加單一校準 Node3D，animated model 作子節點，避免寫入互搶；只有確認需要才增加。
- MOUNT_RIDER_OFFSET 應由這個基準取代／收斂，不保留「先 -1.20、再任意加高、最後再推骨盆」三層補丁。

### 7.3 呼叫時機與恢復

集中處理 mount enabled、模型重建、必要的 clip 基準切換。檢查 `select_animation_by_id`、`set_mount_enabled`、`_attach_rider_to_mount` 的重複校準順序，避免一個動作觸發多次不同姿勢量測。

Unmount 恢復保存的步行根變換；20 次上下馬、切換男女、暫停／seek／播放都不能累積高度偏移。LAB 與編輯器不可以各自補 sprite.position.y。

## 8. 騎乘驗證矩陣

擴充既有 `horse_mount_test.gd` 的掛接／不累積位置驗證，沿用 `verify_character_riding_anims.gd` 確認 clip 存在；新增一個專用 GPU capture script 即可。

- 男女各驗 ride_idle、ride_walk、ride_run、ride_slash、ride_thrust。
- 每 clip 至少取 0%、25%、50%、75%、尾端；四個 map 朝向及編輯器側面／3/4 要有 contact sheet。
- 檢查臀部與坐面、膝腿在馬腹兩側、裝備完整、頭身比例與待機一致。不能以改俯視角、裁掉下半身或縮小角色掩飾。
- 靜止坐姿接觸標記誤差建議≤0.02 模型公尺，重複校準根位移誤差≤0.001；只有確認模型尺度後才套此門檻。動畫的容許起伏依 authored motion 量測，不能把自然起伏當錯誤，也不能接受肉眼明顯陷入。
- 同狀態／同 clip 時間，LAB 與編輯器的角色相對馬變換一致；UI 縮放只改顯示大小。
- 加測 F 上下馬20次、idle→run→slash→thrust→idle、pause/seek 後恢復、換角色與鞍具顯示。沒有累積浮高、陷入、朝向翻轉或下馬位置改變。
- 真正開啟輸出圖片逐張比對；生成 png 或測試 PASS 不等於視覺合格。提供修前／修後同視角對照與可內嵌的絕對路徑。

## 9. 實作順序與交付

1. 讀目前檔案與 dirty tree，記錄現有問題；先建立能讓「55/99 停滯」明確 FAIL 的 fixture 與騎乘修前圖。
2. 保存 unit-slot mapping，移除普通更新時用兵號重綁的分支。
3. 建立通道進度、出口禁停帶與最終出口側集結，替換歷史尾巴終點。
4. 統一交易鎖、requester、取消／清理、交換門檻與公平排程；移除被取代的递迴 fallback。
5. 通過單口→雙口→三層→改命令測試，再量效能與擷取行軍短序列。
6. 量測騎乘接觸點，收斂唯一校準，驗證上下馬與 clip 切換。
7. 跑 Editor scan、相關整合測試、GPU captures，逐一檢查退出碼／錯誤與畫面。
8. 回報實際變更、100人全員結果、等待／抖動狀況、完整frame time數字、騎乘對照圖及真正剩餘限制。

任何階段未達全員通過或視覺驗收都不能標整體完成；保留 failing fixture 與可重現 seed。不要放寬 assert 到「有一人動」或「部分到位」來得到 PASS。
