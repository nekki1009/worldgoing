# V0.4RB

發布日期：2026-09-14。沿既有 `master`、單一 Site 與 `Node2D + Camera2D`；承接 V0.35R。

## 本版內容

- 相鄰能力交鋒：小勝、平手、大勝；原 HP／暈眩／硬直／合法擊退、設施與人物技能。
- 弓弩落點遠程、真實彈藥、原營地製作／領裝；近戰接戰包圍與原格位預約。
- 短招動畫與移動接續、連續受擊反應合併，保留原交鋒與遠程節奏。
- 共用人物疲勞、多人工隊、休息、供糧／士氣／訓練、指揮與名冊。
- 昏迷／死亡搜刮、原裝備與物資守恆、俘虜／家人接續及稀疏保存。
- 所需原角色／武器／普通兵圖集、製作工具與定向測試；保留預設關閉的歷史效能試驗，不宣稱它們已取得加速。

規則與操作見 `SITE_COMBAT_EXCHANGE_V1.md`、`SITE_COMBAT_INSTITUTIONS_V1.md`、`SITE_COMBAT_IMPLEMENTATION.md`；架構仍以 `PROJECT_ARCHITECTURE.md` 為準。

## 已完成的驗證與限制

本機 Godot 4.6.2 Mono，短動畫最後 2,520 次固定步原版／新版狀態比較通過；12 組原角色 GPU 近景通過。原近戰、遠程、包圍、工作隊伍、裝備與保存的逐項開發記錄保留於實作文件。

200 人含 40 射手、相同平地與固定鏡頭，完整 60 秒：平均 **254.97 FPS／P95 17.673 ms**，1,031 次交鋒、800 發實際扣彈／結算、218 個包圍步、3,935 Nodes 前後不變。原 26 項門檻通過，但比本輪短動畫修改前 **282.39 FPS／14.884 ms** 退步；這不是零成本、全地圖／任意裝備／騎乘或更大規模效能保證。

發布檢查確認弓／弩正式圖集來源相符。既有本機 `recipes/v1` 的 32 組非預設近戰配裝圖集來源已落後，依原規則拒絕 admission，過期批次不納入提交；**本版不宣稱它們已重新烘製或可用**，不更改指紋來繞過檢查。原預設兵與本輪弓弩驗證不代表所有缺裝配方可用。

發布前另跑 `20260914_093927_928` editor PASS（exit 0、7.383 秒，載入掃描而非完整重匯入）；改用正式 catalog 後 `20260914_094002_875` GPU 200 人短程檢查 PASS（exit 0、32.981 秒）。短程僅驗初始化與真戰鬥流程，不冒充又跑一輪完整 60 秒效能。既有 Windows 憑證提示與保留清理的鎖檔警告未掩蓋。

開發產物 `output/`、Godot 快取、本機存檔、備份 `.old`、孤立 UID 不納入；僅兩份短動畫 `.gd.txt` 舊版基準與一份舊碰撞腳本作明確白名單，維持相關回歸可載入。其他文件中的 `output/...` 路徑是本機證據索引，不能當成遠端已包含完整圖片／失敗輸出。Git LFS 原 clean guard 保持，首次取用須取得 LFS 資產並完成 Godot 匯入。

遠程即時測試改選正式 catalog（其實際隊伍一直是預設近戰＋正式弓弩，未用歷史 M25 缺裝配方）。父類 `site_exchange_realtime_test.gd` 與較早混裝／效能試驗仍需重建各自指定來源的 M25 fixture；本版不包含過時整批暫存素材，也不宣稱所有歷史 benchmark 能在新 checkout 直接重跑。

本版不改寫或壓縮既有提交，不強制推送，不建立額外版本標籤或 PR。
