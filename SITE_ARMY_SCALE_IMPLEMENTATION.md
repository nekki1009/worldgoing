# V0.41R 後：原 Site 大規模效能第一階段

2026-09-14。基準提交 `51adc2f9c25d91b17653beb7f4f98b3b2c350a36`（V0.41R），已依後續要求推送至 `origin/master`；遠端完整雜湊已核對，3 個 LFS 物件／116 MB 上傳完成。
以下是基準之後的工作區變更，不是該提交已含有的功能。

## 已落地的範圍

- 唯一入口仍是 `TerrainLab.tscn`，沿用 Node2D、Camera2D、原地形、原人物與裝備。
- `TerrainArmy` 的交鋒模式在隊伍至少 64 人時，普通兵改由子呈現器批次繪圖；百人正式隊伍也會使用。每個實例仍引用原人物，不新增代理 HP／隊伍共同生命／第二份位置或保存資料。
- `terrain_army_batch_view.gd` 使用一個共用矩形網格、原圖集、原染色遮罩、連續實例緩衝；按原地面列及人物索引排序。同列不同圖集分成連續批次，原女性呈現者與不支援配裝的 Sprite 形成排序分界，避免錯誤遮擋。清隊及名冊重建先交還原 Sprite。
- 影格與座標仍沿用 CPU 的原精度、原動作時間；同一影格查詢可在當次呈現中共用，GPU 讀取影格矩形與配色。這不是 VAT，也沒有宣稱已做到 GPU 自行推進動畫、Compute 剔除或全面 ECS。
- 原近戰配對先排除友軍／不可配對者，再查地形；後排不再逐人預算不會用到的出手資格。包圍查詢不要求出手資格。配對輪替、技能、預先取值再結算與一人一組規則不變。
- 休息威脅查固定 145 格內的八格曼哈頓範圍；保留原敵人出發／目的格與箭路。精確零疲勞及沒有恢復工作的閒置人物走快速路徑；跑步、救援、硬直、KO、工作、供養仍走原擁有者。
- 增加可選分段計時，未修改 30 Hz 動作步、10 Hz 交戰檢查、傷害、尋路規則或時間倍率。染色緩衝容量為 131072，與原 5000 人成功測試相同。

## 同一主畫面的 5000 人長測

PLAINS、seed 581、2500 對 2500、原天然資源與兩位隊長，原地圖縮放。Godot 4.7.2 Mono／D3D12、i5-14400F／RTX 5060；1600×900 視窗、2560×1440 邏輯視口。兩次測試的 viewport texture 查詢均回報 1000×563，但實際擷取 PNG 都是 1600×900；不把該查詢直接當成內部渲染解析度。沒有為此次改善降低解析度、換場景或藏人。各自靜態暖機 3 秒，再讓原場景自然交戰；不是預先交戰後的穩態測量。

| 指標 | 原基準 | 第一階段 |
| --- | ---: | ---: |
| 現實測量秒數 | 60.140939 | 60.244620 |
| 渲染幀 | 111 | 212 |
| 平均 FPS | 1.845665 | 3.518986 |
| P95 幀時間 | 847.944 ms | 470.247 ms |
| P99 幀時間 | 864.855 ms | 490.715 ms |
| 原戰鬥時鐘前進 | 14.183242 s | 27.589130 s |

約 1.91 倍，但 **5000 人依然不可玩，30 FPS／即時戰鬥目標未完成**。不能以測試正常退出冒充效能達標，也不能把兩輪不同戰鬥進度的累計耗時直接相減。

第一階段長測實例為 4998 名普通兵＋兩位原隊長，節點由原測試約 8995 降至本輪暖機後 4195（結束 4201）。原程式與地形雜湊在本輪測量期間未改變。正式名冊／保存的 200 NPC 守衛沒有放寬；5000 只由壓力測試在程序記憶體中放寬部署守衛，不寫入真實存檔。

本機證據：`output/site_army_scale_20260914/run_1789377060/` 的 measurements.json、before.png、after.png；原基準已複製至 `output/site_army_scale_20260914/baseline_5000/`，原處 `.godot-temp/fps5000_20260914/final/` 保留。完整執行條件、視口查詢、逐幀間隔與檢查點均在原 JSON，未改寫測量結果。

同主畫面 200 人 A/B 短測（各 15 秒）：批次 75.81 FPS／P95 36.08 ms、Sprite 74.62 FPS／P95 36.76 ms，兩者原戰鬥時鐘均前進約 15 秒。這是一輪對照，未見明顯退步，不宣稱約 1.6% 差距具有統計意義，也不代表所有部署與高負載情境的穩定 60 FPS。輸出分別為 `run_1789377213`、`run_1789377252`。

## 驗證與已知限制

- GPU：181 組逐像素相等，包含長劍／弓／弩的四方向、五姿態、三時刻、實裝三個染色部位，以及同列混合 Sprite／批次的重疊排序。另檢查非法染色、非法索引及非有限時間的拒絕。不是全部動畫時間、任意裝備、所有鏡頭或完整死亡／拆併視覺矩陣。
- 規則：從 V0.41R 取出的原函式作獨立測試基準；200 名原人物跑 360 個共同時間步，完整逐人狀態相等，108 次交鋒；3813 次休息威脅查詢一致。
- 回歸：近戰流程、Army 結算、Army 短動畫、遠程流程、遠程 Army、包圍、出手資格／繪圖失效、短動畫玩法一致性、歷史疲勞零路徑均通過。歷史疲勞測試明確指定舊精確模式，原正疲勞／負零／工作／保存斷言未刪除。
- 過程失敗照實保留：測資對不存在裝備染色／舊攻擊名稱、上下翻轉、邊緣像素、Dictionary 鍵型別、shader 唯讀值、尺寸比較型別與過期預設斷言。前兩次 assert 中止未退出，觸及 120 秒上限，均為 FAIL；後續加入內部退出／時限。
- 仍有原遮罩 `Image.load_from_file` 匯出警告；本機 GPU 通過不代表匯出包驗收。Windows 憑證與使用中舊驗證資料夾保留警告未被隱藏；原驗證 helper、保留最新 100 輪規則及 LFS guard 未更改。
- 此次沒有完整改造逐人物件／Dictionary 的高頻資料路徑，也沒有流場化全軍機動、遠程全掃描降維、正式 5000 人存讀檔准入。GPU 繪圖呼叫沒有隨節點數等比例下降；不能聲稱已解決 Draw Call 瓶頸。
- 最終 editor 掃描 PASS：`20260914_171645_176`，exit 0，9.290 秒。最後像素／准入 PASS：`20260914_171047_882`，10.141 秒；5000 人長測正常退出：`20260914_171058_203`，88.413 秒（含載入／暖機），但 FPS 驗收未達標。
- 驗證結果及原始完整日誌已複製至 `output/site_army_scale_20260914/verification/`，含全部過程失敗；`index.json` 列出每輪參數、真實 exit code、耗時與首個錯誤。沒有停用原保留機制。編輯器自動改動的 C# SDK 版本已還原，沒有順便升級 SDK。

下一階段應以每一「模擬秒」的分段成本為準，處理仍逐人跑的狀態推進、出手快照與呈現資料生成；保留原人物擁有者，逐段建立原／新一致性測試，再決定連續欄位與原生批次核的邊界。不能以降低 AI 頻率、讓時間變慢、少畫人或改成共同 HP 充當改善。

矩形相容性核對來源：[Godot Sprite2D](https://github.com/godotengine/godot/blob/4.7-stable/scene/2d/sprite_2d.cpp)、[Godot Canvas RD shader](https://github.com/godotengine/godot/blob/4.7-stable/servers/rendering/renderer_rd/shaders/canvas.glsl)。

## 第二階段：局部查詢與配裝快照（2026-09-14）

V0.41R 的推送先於本階段改檔。第一、二階段效能修改仍在工作區，沒有夾帶進該次推送，也尚未另作效能提交。

本階段沿用現有擁有者，沒有新增人物系統、共享 HP、時間膨脹或另一個主畫面：

- Army 包圍搜尋把前線人物放入八格寬的暫態格桶；敵人只查相交格桶，仍以原八格曼哈頓距離精確判定。敵人遍歷、目的地選擇、512 格／八步搜尋及每次最多八個原移動預約的順序不變。
- 近戰的可受擊資格與出手資格都只在遇到可配對敵人時，向原人物擁有者查詢；仍在套用任何一組結果之前完成全部配對，保留原輪替與結算語意。共用 `combat_can_act` 在一次呼叫內只取一次原人物列。
- 交鋒休息威脅直接查 Army 原 `_cell_owners`／`_reserved_cells`；當次同步疲勞結算只記住已查格的答案，不再先掃完整支敵軍。保留原玩家／NPC 移動起點、敵兵出發／目的格、KO／起身排除與友軍箭路；歷史精確模式仍走原路徑。
- SiteController 在既有 `equipment_changed` 發布深層唯讀外觀；換裝仍替換該 owner 的原投影，不新增裝備資料來源。Batch 對已確認深層唯讀的外觀使用同一引用快速判定；一般可變回呼及僅根層唯讀、內層可變的資料仍保留深複本比對，不能漏掉原地染色變更。
- 可選計時細分 Army 指揮、逐人更新、呈現提交／上傳，以及 Site tick；`--render-profile` 的靜態 CPU 小段測量明確放在 FPS 測量之前，不當作 FPS。

Godot 的唯讀旗標不會自動凍結巢狀內容，實作因此逐層處理，而非只凍結根 Dictionary。[官方 Dictionary 說明](https://docs.godotengine.org/en/4.6/classes/class_dictionary.html#class-dictionary-method-make-read-only)。

### 相同主畫面 5000 人、60 秒長測

維持第一階段的引擎、硬體、PLAINS seed 581、2500 對 2500、視窗／視口／鏡頭、三秒靜態暖機、無 FPS 上限及隔離保存。這輪未開分段計時。

| 指標 | 第一階段 | 第二階段 |
| --- | ---: | ---: |
| 測量現實秒數 | 60.244620 | 60.037895 |
| 渲染幀 | 212 | 243 |
| 平均 FPS | 3.518986 | 4.047444 |
| P95 幀時間 | 470.247 ms | 417.070 ms |
| P99 幀時間 | 490.715 ms | 442.667 ms |
| 原戰鬥時鐘前進 | 27.589130 s | 31.851437 s |

FPS 約增加 15.0%，P95 約降低 11.3%。**5000 人仍不可玩，也未達即時戰鬥／30 FPS；不是完成效能驗收。** 共 2971 次原交鋒；結尾 5000 個原人物、5 人 KO、0 死亡，4998 個批次普通兵加兩位原女性呈現者。前後可見計數都是 5000，落在未被面板遮住的地圖外者為 0。最終 5766 個繪圖呼叫；未宣稱已解決 Draw Call。測量期間五個實作檔案的雜湊不變，前後主畫面截圖已檢視。

本機輸出：`output/site_army_scale_20260914/run_1789379985/`；原 bounded helper 為 visual PASS、exit 0、88.700 秒（含載入／暖機），紀錄 `20260914_175942_783`。PASS 指執行與資料完整，FPS 門檻仍未達。

200 人同主畫面短測：`run_1789380117`，15.026875 秒、1106 幀、73.60 FPS、P95 36.673 ms，原戰鬥時鐘前進 15.002912 秒；這是單次樣本，不將與第一階段 75.81 FPS 的細小差距判作顯著退步或提升，也不宣稱各情境穩定 60 FPS。

### 分段證據與回歸

- 第二階段前重新量測：`run_1789378913`（3.713 FPS），進一步細分為 `run_1789379019`（3.792 FPS）。依序開發短測為 `run_1789379214`（3.971）、`run_1789379538`（4.099）、`run_1789379735`（4.219）；最後兩輪另含計時前的靜態呈現小測，不拿它們代替最後 60 秒結果。
- 依模擬秒正規化，休息威脅從 `run_1789379019` 的 1,009,991 μs／6.803332 模擬秒，降至 `run_1789379735` 的 154,633 μs／7.858756 模擬秒，該段約降低 86.7%。累計耗時不直接相減。
- 靜態 5000 人的「查外觀＋深比對」小測從 18.762 ms 降至 9.292 ms；普通兵完整呈現路徑從 40.603 ms 降至 35.084 ms。這是原 Army 的三次 CPU 小測平均，不是穩態動畫／GPU 時間。
- 原 V0.41R 規則基準：200 人 360 個共同時間步逐列狀態一致，108 次交鋒、3813 次威脅查詢一致。
- 新的原包圍函式基準：200／5000 人各 36 步，逐人列、位置、進度、隊形格、來源／目的預約、指揮 RNG 與包圍步數一致；分別有 3／6 個實際包圍步。這是 CPU 規則對照，不是替代場景 FPS、5000 人完整生命週期或保存准入。
- 新增休息邊界：距離八／九格、移動中 KO、起身、醒來及友軍箭路均與原函式一致。
- GPU `pixels_1789379931`：181 組完整 RGBA 逐像素相等，另驗證深層唯讀、僅根層唯讀但染色可變、重新發布外觀的快取准入。visual PASS／exit 0／10.744 秒，`20260914_175849_632`；已檢視混合遮擋圖及正式主畫面。
- 最後一組 12 項 headless 回歸全部通過：scale_rules、exchange_flow、exchange_army、exchange_animation_army、exchange_readiness_render、exchange_animation_gameplay_parity、ranged_flow、ranged_army、encirclement、fatigue_zero_fast、loot_lifecycle、equipment_depot_ui。另有 equipment_palette_policy 通過。既有原物主、換裝、遺物、保存斷言保留。
- 本階段唯一執行失敗：`20260914_175755_775`／exit 1，新測試把起身完成誤當兩秒；原／新結果都仍是不能行動。核對原 `get_up=2.2` 後改讀原時間常數加一步，保留正向醒來斷言；`20260914_175832_252` 通過，失敗日誌保留。

階段前源碼快照與完整本階段 helper 日誌集中於 `output/site_army_scale_phase2_20260914/`，包含失敗；不停用原保留最新 100 輪的機制。正式保存仍最多 200 NPC；5000 只在測試程序內放寬部署守衛。

最後 editor 掃描：`20260914_180556_755`，PASS／exit 0／10.862 秒。編輯器自動變更的 SDK 4.7.2 已還原為原 4.6.2，不夾帶 SDK 升級；GDScript/GPU 測試使用的引擎仍為上述 Godot 4.7.2 Mono。原 helper 的使用中舊資料夾保留警告與 headless 憑證雜訊保留在完整日誌。

### 尚未完成

逐人 Dictionary 更新、完整出手快照／配對、每個射手掃全場，以及普通兵呈現資料生成仍有明顯成本。本階段沒有完整 DOD／原生批次計算、流場全軍機動、GPU 自行推進動畫或正式 5000 人存讀檔。下一個較大的切面應先選定高頻欄位的唯一寫入邊界，再以連續資料批次處理並保留原／新逐步一致性；不能維持一份未校準的平行 HP／位置快取，也不能靠降 AI 頻率或降低畫面人數達標。

## 第三階段：同步查詢連續欄位與編譯式候選篩選（2026-09-14）

使用者同意依資料評估繼續實驗。本輪先選查詢暫存這個較小切面，沒有一次搬走所有 HP／冷卻／人物列；沿用 ponytail 最小改動原則，不新增 ECS／Job System 或另一個戰鬥擁有者。

### 實作及不變條件

- `site_exchange_snapshot.gd` 是一次同步查詢的暫態連續欄位：原 owner、索引、64-bit 人物 ID／陣營、整數格位。不保存 HP、時鐘、裝備或 RNG；每次查詢結束即丟棄。普通兵只在確有相鄰敵人候選時建立既有 Dictionary 查詢介面，原 Actor 的 eager readiness 保留。
- `compiled_exchange_candidates.cs` 只接收位置／陣營陣列並回傳原名冊索引。在 .NET 的普通集合中一次批次查詢，沒有逐人 Godot 集合互操作、Node 存取或場景樹寫入。這輪仍是單執行緒，不聲稱自動 SIMD、實測 Cache Miss 或多核心收益。
- 篩選是保守幾何條件，不決定輸贏或省略前線生命。優先輪替仍按完整原名冊長度及 `_exchange_round`，不是按縮短的候選數；同格人物仍維持原遍歷次序。所有相鄰異陣營人物都留在 context 占格中，故包圍扇區、地形阻隔及後續資格判斷不變。
- 所有配對仍先取值、最後才套用；技能、格位預約、擊退、救助、物品／死亡及存讀檔仍在原 owner。`_exchange_people` 的其他呼叫者沒有被替換。
- 遠程入口在近戰及其訊號處理完成後才讀原彈藥／裝備，不能用近戰前的武器分類快取。沒有實際彈藥可發射時不建立全場 Dictionary；一旦存在可用遠程武器／彈藥，仍交給原全目標選敵路徑，保留擊退前的查詢格位及候選順序。尚未優化有彈藥的大量射手選敵。
- 原 `exchange_batch_enabled=false` 是同一 Lab 中的第二階段 A/B；無 C# 核時有相同候選規則的 GDScript 路徑。`worldgoing.csproj` 只增加一個明確編譯檔，不升級原 4.6.2 SDK、不新增套件；既有 bounded build helper 成功，零編譯警告／錯誤。
- 30 Hz 動作步、10 Hz 查詢、原始分辨率與批次呈現、兩位女隊長、200 NPC 正式保存守衛均未改。5000 人仍是測試程序內暫時放寬部署，不入真實保存。

### 新鮮同條件 5000 人 A/B

兩輪都使用原 `TerrainLab.tscn`、PLAINS seed 581、2500 對 2500、相同地形／裝備／鏡頭／視窗／GPU、三秒靜態暖機、60 秒自然交戰；無 FPS 上限，均未開分段計時。引擎仍為 Godot 4.7.2 Mono／D3D12，硬體同前。原 helper 未改。

| 指標 | 第二階段路徑 `--legacy-query` | 第三階段 |
| --- | ---: | ---: |
| 輸出 run | 1789382243 | 1789382363 |
| 測量現實秒數 | 60.068068 | 60.088122 |
| 畫格 | 245 | 294 |
| 平均 FPS | 4.078706 | 4.892814 |
| P95 幀時間 | 415.897 ms | 344.828 ms |
| P99 幀時間 | 437.159 ms | 364.397 ms |
| 原戰鬥時間前進 | 31.983938 s | 38.368680 s |
| 原交鋒數 | 2972 | 3617 |

FPS 約提高 19.96%，P95 約減少 17.09%。**仍未達 30 FPS 或即時戰鬥，5000 人仍不可玩。** 這是單次相同条件 A/B，不是重複樣本的統計估計。兩輪實際模擬進度不同，不能把最後 HP／KO 差異當作同一步的規則差異；規則一致性另用下列固定步對照驗證。

前後可見計數均為 5000，未遮擋地圖外為 0；仍是 4998 批次普通兵＋兩位原隊長。兩輪結尾皆為 5766 Draw Calls，並非靠減少畫面人數或繪圖呼叫取得本輪收益。原人物都在，新版結尾 7 KO／0 死亡。前後地形 fingerprint 相同，實作檔與編譯組件在測量期間 SHA-256 不變，正式主畫面截圖已檢視。

原始 JSON／before.png／after.png：`output/site_army_scale_20260914/run_1789382243/`、`run_1789382363/`。helper 分別為 `20260914_183721_456`（88.508 秒）、`20260914_183920_983`（88.694 秒），visual PASS、exit 0；這個 PASS 不表示 FPS 達標。

### 分段與規則證據

- 第一輪開發短測 `run_1789382083`：15.244890 秒、5.050873 FPS，實際使用 C# 核。以每模擬秒正規化：配對/context 由上一階段短測的約 156.6 ms 降至 11.1 ms，查詢建表約 137.6 → 85.1 ms，遠程入口約 148.7 → 29.9 ms。這三段包含整體資料路徑改造，不是 C# 語言本身的獨立 A/B，也不是 GPU 時間。
- 同一短測，逐人準備仍約 320.5 ms／模擬秒，指揮約 227.6 ms／模擬秒；普通兵與隊長呈現仍約 40.8 ms／渲染幀 CPU。尚不能達成流暢；下一切面仍是原人物狀態推進與呈現資料生成，而非宣稱完整 DOD 已完成。
- 既有 V0.41R 固定基準：200 人、360 個共同時間步，原逐人狀態一致，108 交鋒、3813 次休息查詢一致。
- 新增 `site_exchange_batch_test.gd`：100 組固定種子配置，C#、GDScript 與獨立兩兩暴力掃描的候選順序相同，涵蓋空集合、負座標、同格混合陣營、大輪次及 64-bit 陣營。
- 200＋4 原 Actor／120 步、5000＋4 原 Actor／36 步，以第二階段原路徑比對完整 Army 列、格位／進度、格子占用／預約、RNG、Actor 狀態與近戰／遠程事件順序；另強制 200 人走 GDScript fallback。涵蓋中途障礙、KO、陣營變更，以及近戰訊號後替遠方原人物換裝／補彈。

源碼快照及每輪完整 helper 紀錄保留在 `output/site_army_scale_phase3_20260914/`；沿用最新 100 輪保留規則。測试正常退出仍有既有 Windows 憑證雜訊／使用中舊驗證目錄保留警告，未隱藏。完整 5000 人保存、全武器大量射手效能、匯出包及所有生命週期情境不在本輪驗收內。

### 最後回歸與失敗紀錄

- 200 人同主畫面 A/B 各 15 秒：舊查詢 `run_1789382496` 為 74.305558 FPS／P95 36.680 ms，新查詢 `run_1789382555` 為 75.612776 FPS／P95 36.476 ms。各自遊戲時間前進約 15 秒、前後 200 人可見。單次樣本未見明顯退步，不把約 1.8% 差距宣稱為統計顯著改善。helper 為 `20260914_184133_956`、`20260914_184232_720`，visual PASS／exit 0。
- 最後 11 項 headless 測試通過：exchange_batch（含 Actor 完整 `capture_state` 及原飛行事件）、scale_rules、scale_spatial、exchange_flow、exchange_army、exchange_readiness_render、exchange_animation_gameplay_parity、ranged_flow、ranged_army、encirclement、loot_lifecycle。紀錄 `20260914_184445_767` 至 `20260914_184533_452`。
- `site_army_command_save_test` 的 headless 輪 `20260914_184537_097` **FAIL**：雖然斷言完成且 Godot exit 0，dummy renderer 仍回報 `Parameter "material" is null`／`material_get_instance_shader_parameters`，因此不能列為 PASS。追查原 `restore_combat_state → _deploy_selected` 會重建角色呈現；沒有修改測試、斷言、保存邏輯或驗證器，改用正常 GPU 跑同一份測試，`20260914_184644_510` visual PASS／exit 0／14.219 秒，無該材質錯誤。此為存讀資料斷言驗收，不是外觀截图驗收。
- Editor bounded 檢查 `20260914_184723_379`：PASS／exit 0／47.878 秒；有 `Scan thread aborted` 與既有嵌套測試專案忽略警告，不宣稱完成全資產匯入。新實作腳本已在上述實際 runtime 測試載入。編輯器自動將 SDK 改為 4.7.2，已還原為本轮編譯／測量所用的原 4.6.2；`project.godot` 未因本輪改變。
- 效能目標依然未達成。V0.41R 原提交／遠端不動，本輪及前兩輪效能改動尚未另行 commit／push。
- 收尾以原 4.6.2 SDK 重建，零警告／錯誤；全部九個量測追蹤檔（含 `worldgoing.dll`、csproj、主場景腳本與既有呈現程式）的 SHA-256 與新版 60 秒量測完全相同。`git diff --check` 通過。

## 第四階段：既有呈現器的相同取樣重用（2026-09-14）

使用者要求「再試」。沿用 ponytail 的最小既有擁有者切面：本輪產品程式只修改 `terrain_army_batch_view.gd`，沒有再改 Army、Lab、人物生命列、C# 或正式保存限制。完整實作／基準／所有 helper 紀錄見 [第四階段](output/site_army_scale_phase4_20260914/README.md)。

- 每次 `begin()` 清空上一筆成功取樣；僅圖集頁、原 clip 字串、方向及精確 double 時間均相同時重用 sequence、frame、scaled anchor。每個人仍讀自己的原地面位置、外觀與 palette，維持原 Sprite 算式精度和遮擋順序。
- 可變外觀的深快照／不可變發布身份比較在重用前執行；失敗不污染上一筆成功取樣。原圖集／Shader／女性呈現者及 Sprite 回退完全保留，不把獨立人物動作變成共用時鐘。
- 凍結第三階段完整呈現器為測試 fixture，與階段前源碼逐位元相同；`--legacy-render-samples` 僅在測試程序載入舊呈現器，另記錄記憶體原始碼雜湊。兩路都保留第三階段 C# 查詢，不混用第二階段的 `--legacy-query`。
- 30,480 筆取樣與舊碼的完整呈現欄位一致：所有已准入序列、長劍／弓／弩、切幀前後 ±2ns、循環／非循環、長時間、不同地面／染色、失敗穿插及重置。另驗證原 GPU buffer／bounds，181 組完整 RGBA 對照相同；180 組包含真正連續命中的双人不同染色重疊。

### 真實 5000 人長測

原主畫面、相同 seed／2500 對 2500／地形／裝備／解析度／鏡頭／三秒靜態暖機／硬體／引擎，未開分段計時，依舊→新→新→舊執行，每輪 60 現實秒：

| 指標 | 第三階段舊呈現，兩輪 | 第四階段，兩輪 |
| --- | ---: | ---: |
| FPS | 4.827577、4.831126 | 5.027654、5.016571 |
| 每路徑 FPS 中位數 | 4.829351 | 5.022112 |
| P95 幀時間 | 347.074、309.319 ms | 294.123、295.550 ms |
| 原戰鬥時鐘 | 37.969、37.810 秒 | 39.406、39.403 秒 |

FPS 約 **+3.99%**，小幅收益可在反向順序重現，但兩輪／路徑仍不是充分統計樣本。**依然不可玩、未達 30 FPS 或即時戰鬥。** 四輪前後均為 5000 人可見、地圖外 0、4998 批次＋兩位原女性；Draw Calls 都是 5766，不是靠減人取得收益。首組四张主畫面截图已檢視。

開發短測的三次静態 CPU 小測，普通兵完整呈現資料路徑為 34.947 → 26.827 ms（-23.24%）；自然交戰整隊呈現 CPU 約 40.533 → 33.733 ms／渲染幀。這不是 GPU 時間，亦不把 CPU 段降低比例說成整體 FPS 增幅。逐人準備仍約 326.1 ms／模擬秒、指揮約 222.6 ms／模擬秒，尚需處理原人物欄位推進與呈現資料批次化；本輪沒有全面 DOD、SIMD、Job System、GPU 自推動畫、異步全動作壓測或完整 5000 人存讀檔。

### 回歸與收尾

- 五項 headless 檢查均通過：exchange_readiness_render、scale_rules、scale_spatial、exchange_animation_gameplay_parity、exchange_batch。涵蓋 V0.41R 200 人 360 步完整狀態、5000 人固定步空間／預約／RNG 及 native／fallback 規則一致性；不是以短測最後 HP 比較替代固定步驗證。
- 原軍隊指揮／format3 存读檔測試在正常 GPU 通過，不在先前已知不適用的 dummy renderer 重試；保存程式及斷言未改。GPU 像素驗證與資料保存驗收分開記錄。
- 正式 200 人各 15 秒新鮮 A/B：73.519574 → 73.889201 FPS、P95 36.855 → 36.551 ms；各自原戰鬥約 15 秒，前後 200 人可見。未見明顯退步，但不宣稱單次約 0.5% 差距顯著。
- 本輪共 15 次 bounded helper 全部 PASS／exit 0，完整紀錄已歸檔，原保留最新 100 完成輪次的機制未修改。沿用的圖集 raw-image／export 警告、Windows 憑證雜訊與使用中舊目錄保留警告均保留；沒有宣稱匯出已驗收。
- 收尾九個實作／組件 SHA-256 與新版 60 秒量測一致，`git diff --check` 通過，原 4.6.2 SDK 不變。本輪無 C# 修改，未另做建置或全資產 Editor 匯入；runtime 已載入新腳本。HEAD 保持 `51adc2f V0.41R`，四階段效能改动均未另行提交／推送。

## 第五階段：原 Army 高頻路徑的四項實驗（2026-09-14）

使用者要求「繼續多試一些」。依 ponytail 最小切面原則，最終產品程式只改原 `terrain_army.gd` 兩處；無新狀態擁有者、持續快取、Job System 或額外依賴。基準源碼、全部試驗與失敗紀錄見 [第五階段完整證據](output/site_army_scale_phase5_20260914/README.md)。

- **保留：包圍候選的保守幾何篩選。** 用原最多 512 格距離場反推四方可能起點，不可能走進場內的人省去後續資格查詢；仍保留原完整名冊順序、pinned／工作／裝備資格、最佳步、排序、預約及 RNG。只有建表上限筆數小於名冊人數才啟用，小隊伍沿用原掃描。固定步完整 bytes 一致；200 人資格查詢 22→22、5000 人 4708→16。
- **不保留：六次待機清零合成 Dictionary.merge。** 完整 bytes 一致，但獨立主畫面短測無收益，已恢復原寫入。候選碼保存在 rejected，不掛入正式程式。
- **不保留：C# 直接逐欄更新原 Dictionary。** 只在原 5000 人資料複本做四轮交替 CPU 小測，完整 bytes 一致；中位數 254.9 → 562.6 ms，耗時約 2.21 倍。這是跨語言資料存取成本的子集實驗，不是 FPS 或 SoA／SIMD 的結論。試驗 C#／GDScript／Compile 項已移除并歸檔，原 csproj／DLL 完全恢復，SDK 仍 4.6.2；三次 bounded build 均零警告／錯誤。
- **保留：待機资格免建預設空 Dictionary。** 欄位不存在時先判斷 `has`，存在時仍查原內容；原 age／think 與六次清零不變。獨立短測 prepare CPU 約少 6.6%，整體短測 FPS 幾乎不變，不將段落改善當成整體 FPS 增幅。

### 原主畫面結果及限制

全部 FPS 測試均為原 `TerrainLab.tscn`，相同地形、seed 581、2500 對 2500、1600×900 視窗／2560×1440 邏輯 viewport、原裝備／女性呈現者／鏡頭；未改 30 Hz／10 Hz、生命、移動、救助或保存。5000 人仍是隔離測試程序內放寬單一部署守衛，正式 200 NPC 保存上限不變。

| 對照 | 第四階段 | 第五階段 |
| --- | ---: | ---: |
| 合併版 5000 人，60 秒 ABBA 每路徑 FPS 中位數 | 4.964524 | 5.348338 |
| 最後小隊伍守衛版，5000 人各 60 秒 FPS | 4.899284 | 5.407136 |
| 最後 5000 人 P95／P99 幀時間 | 341.857／373.124 ms | 265.147／297.040 ms |
| 最後 5000 人原動作時間 | 38.650 秒 | 42.417 秒 |
| 最後 200 人，15 秒 ABBA 每路徑 FPS 中位數 | 74.938771 | 74.658314 |

原合併版四輪約 +7.73%；最終守衛版單次 A/B 約 +10.37%，仍屬有限樣本，不能宣稱穩定通用的 10% 增益。**5000 人仍不可玩，未達 30 FPS 或即時模擬。** 最終主畫面前後 5000 人均可見、地圖外 0、4998 批次＋兩位原女性，Draw Calls 仍 5766；原主畫面捕捉已檢視，地形與九個追蹤實作／組件雜湊一致。

200 人曾測到約 -1.70%，未隱藏；補上工作量守衛後再跑四輪為 -0.374%，兩路數值範圍重疊。不能宣稱小隊伍已有明確改善，也不以短測保證完全無退步。

逐步驗證覆蓋 200／5000 人完整原人物列、欄位型別與插入順序、格位／預約、RNG、HOLD／MOVE／ATTACK、硬直／KO／舉盾及缺少可選欄位；另有原 V0.41R、包圍／批次／遠程／疲勞／動畫規則回歸。GPU 181 組完整 RGBA、30,480 筆凍結取樣及原 format3 指揮存讀資料測試通過；這不等同完整 5000 人保存、匯出包、全武器大量射手或所有移動生命週期驗收。

本輪有一次 C# 探測腳本驗證失敗並逾時：兩個 void append 誤放在條件運算式。保留完整失敗結果，改成 if／else 後同一嚴格 bytes 驗收通過；未繞過 verifier 或削弱規則驗收。最終只保留原 Army 的兩處改動及其測試／文件，無新 C# 產品變更；HEAD 仍為 `51adc2f V0.41R`，沒有另行 commit／push。

## 第六階段：待機比較與呈現查詢的小幅精簡（2026-09-14）

使用者要求「還有能改的嗎，再試」。本輪採 ponytail 最小既有擁有者切面，產品程式仍只改原 Army 兩處，沒有再改 Lab／Controller／MultiMesh 呈現器／Shader／C#／SDK。完整原始資料、基準方法與失敗紀錄見 [第六階段](output/site_army_scale_phase6_20260914/README.md)。

- 原待機资格以原数值直接比較零，免去七次 `float` 及姿態 `str`；寫回時仍使用原 float 正規化，不改 age／think、HP／KO 判斷或六次清零。原保存准入只接受有限 int／float 的守衛不變。
- 原批次呈現的既有 idle 條件成立時，直接讀既有 `HELD_LOCOMOTION.idle` 及原人物 age，省去結果固定的 clip 解析和未使用的 clock 查詢。獨立地面／時間／方向／外觀、原女性、染色准入及 Sprite 回退不變，沒有新快取或第二份狀態。
- 第五階段兩個完整方法凍結作同一主畫面的 A/B；沒有更換主場景或另造邏輯。200／5000 人逐步原人物完整 bytes／鍵型別／格位／預約／RNG 與第四、第五階段均相同。另有 4608 組原 Army 批次輸入 bytes 對照，涵蓋三武器／12 姿態／四方向／四時間／四種視覺狀態及移動。

### 實測結果

相同原主畫面、PLAINS seed 581、2500 對 2500、原地形／裝備／鏡頭／解析度、三秒靜態暖機、Godot 4.7.2 Mono／D3D12，無 FPS 上限。開發短測的逐兵準備 CPU 約少 3～4%，普通兵完整呈現 CPU 約少 12～16%，不將 CPU 段收益說成整體 FPS。

未開分段計時的四輪 60 秒、舊→新→新→舊：

| 指標 | 第五階段，兩輪 | 第六階段，兩輪 |
| --- | ---: | ---: |
| FPS | 5.363762、5.401121 | 5.589438、5.488823 |
| 每路徑 FPS 中位數 | 5.382442 | 5.539131 |
| P95 幀時間 | 250.479、248.596 ms | 243.932、244.393 ms |
| P99 幀時間 | 264.322、270.281 ms | 289.613、261.771 ms |
| 原動作時間 | 42.215、42.606 秒 | 44.001、43.272 秒 |

中位數約 **+2.91%**，小幅收益在反向順序可重現，但每路徑兩輪不是充分統計樣本；P99 沒有一致改善。**5000 人仍不可玩，未達 30 FPS／即時模擬。** 四輪前後 5000 人均可見、地圖外 0、4998 批次＋兩位原女性、8 KO／0 死亡；首組四張主畫面已檢視。自然交戰進度不同，末幀 Draw Calls 為 5766／5759／5766／5766，不能當固定步畫面比較；同一步狀態与固定輸入呈現的嚴格比較另驗證。

九項最後 headless 回歸通過：hotpaths、scale_rules、scale_spatial、exchange_readiness_render、exchange_animation_gameplay_parity、exchange_batch、ranged_army、fatigue_zero_fast、encirclement。原資料與保存守衛不變，未把 5000 人壓測放入正式存檔。

新增呈現測試曾誤用不存在的 `attack_sword`：雖 exit 0 且腳本印出 PASS，原 verifier 仍正確判 FAIL。已從既有武器表取真實 `walk_slash`，並讓每個案例清空上一筆輸出、要求實際提交，避免殘留資料假通過；失敗版本／完整結果保留，修正後 4608 組精確比較通過。沒有修改產品動作表、弱化驗收或改 canonical verifier。

測量追蹤的九個源碼／編譯組件 hash 與本輪新長測相同，原 4.6.2 SDK 不變。無本輪 C# 變更、建置或全資產 Editor 匯入；實作 GDScript 已由上述 runtime 載入。全部效能改動仍未另行 stage／commit／push。

### 第六階段收尾回歸

- GPU 181 組完整 RGBA 相同及 30,480 筆凍結取樣對照通過，最新長劍／混合重疊捕捉已檢視；原軍隊指揮／format3 存讀也通過，未改動其斷言或正式 200 NPC 保存限制。
- 200 人同主畫面四輪各 30 秒 ABBA：舊 72.278236、75.359009 FPS，新 74.740259、74.595950 FPS；中位數 73.818622 → 74.668104（+1.15%）。兩路範圍重疊、舊版波動較大，不能宣稱穩定收益；各輪原動作前進約 30 秒且前後 200 人可見。
- 本輪 26 次 bounded Godot helper 為 25 PASS、1 FAIL，失敗已定位修正且保留完整證據。全部本任務測試程序均已結束，未留下背景工作；完整 5000 人保存、全武器大量射手／地形／移動矩陣及匯出包仍未驗收。

## 第七階段：原擁有者同步原生批次（2026-09-14）

本輪直接針對逐兵狀態更新：Army 待機準備、Lab 無工作扣時的靜止零疲勞，以原生 C ABI 處理連續合格列。原人物 Dictionary 的身分、鍵與數值仍在原 Army；首個不合格人物完全未修改地返回原 GDScript，完成其救助／移動／命令後才繼續。沒有平行 SoA 人物快取、代理隊伍、另造入口或降低規則頻率。

本機實際 TerrainLab、5000 人、每輪 60 秒 ABBA：FPS 中位數 **5.5610 → 6.5681（+18.11%）**。另行短測 profile 的準備段 283.85 → 158.00 ms／模擬秒，疲勞段 124.09 → 73.88 ms／模擬秒。仍未達可玩或即時模擬；剩餘成本包含人物例外處理、命令、接敵查詢／結算及呈現提交。不是全部生命更新搬到原生，也不是 ECS／多核心成果。

200／5000 人完整逐步原狀態、原生邊界、真實救助前後順位／移動完成後改令、供餐／訓練／資源工作、缺檔及關閉加速 fallback 均有定向驗證。原 200 NPC 保存守衛與圖像路徑不改；完整數字、逐項證據、失敗及未驗收的平台／匯出界線見 [第七階段紀錄](output/site_army_scale_phase7_20260914/README.md)。本輪未 commit／push。

第七階段收尾：200 人四輪 30 秒 ABBA 中位數 76.66 → 75.69 FPS（-1.27%，範圍重疊），未證明小名冊改善或完全無退步。12 項最終 headless、181 組 GPU 完整 RGBA／30,480 取樣、原軍隊指揮與 format3 存讀均 PASS；34 次 canonical helper 結果已保留，初次原生建置缺 include 的失敗也另行記錄。新原生庫僅完成本機驗證，所有本輪背景工作已結束。

## 第八階段：原人物呈現準備原生批次（2026-09-14）

初始原主畫面探測：普通兵完整呈現路徑約 23.6 ms，外觀查詢約 8.2 ms；不是 GPU 繪製時間。只將既有 Site 外觀發布可精確驗證、已准入且靜止待機的普通兵同步投影為新的呈現 PackedArray。原人物 Dictionary 完全不寫入，外觀變更／新配裝／移動／短招／女隊長仍走原路徑。自訂外觀回呼不會被繞過，原地面列與索引順序、材質／染色、CPU 取樣精度及模擬／保存規則均保留；沒有第二份人物權威資料、另造入口或調低画質。

原主畫面 5000 人四輪各 60 秒 ABBA：**6.5883 → 7.8230 FPS（+18.74%）**，P95 中位數約 230.01 → 187.44 ms。呈現提交短測 29.08 → 14.24 ms／rendered frame；200 人四輪各 30 秒 **73.53 → 76.58 FPS（+4.14%）**，本機這組未觀察到退步。5000 人 60 秒牆鐘仍只前進約 54.6 秒原動作時間，未達可玩或完整即時模擬。

30,000 筆原圖集呈現欄位、輸入不變／COW／回呼 fallback、兩組凍結原主畫面完整 RGBA／人物占格 RNG bytes、200／5000 人原規則、既有 181 組裝備像素／30,480 筆取樣與原存讀回歸通過。新增測試最初漏設原視覺準備呼叫者負責的 ready 旗標，assert 後等到有界時限；已修正初始化與測試失敗退出，原始失敗完整保留，沒有降低斷言或修改 canonical verifier。詳見 [第八階段紀錄](output/site_army_scale_phase8_20260914/README.md)。本輪未 commit／push，匯出／跨平台及完整大量射手／移動／地形矩陣未驗收。

## 第九階段：原人物接敵／指揮名冊整理（2026-09-15）

原主畫面定向探測把接敵與指揮名冊整理分離，分別約 81.62／40.60 ms 每模擬秒。延伸既有 ArmyIdleKernel，同步借用原 Army Dictionary 與 cells，產生當次原 Snapshot 欄位或原 `_exchange_people(false)` Dictionary 介面；不保留人物指標、不跨幀快取、不改原擁有者／30 Hz／10 Hz／保存守衛。原 can-act 順序、不篩除 member=false、64-bit ID／陣營、負格位、owner 與鍵順序均保留。子類別、特殊型別、缺 DLL／方法走原 GD；readiness 與近戰 signal 後的遠程裝備／彈藥讀取維持原時點。

相同實際 TerrainLab 四輪 60 秒 ABBA，5000 人 **7.4994 → 8.1675 FPS（+8.91%）**，P95 約 196.51 → 182.76 ms；200 人四輪 30 秒 **72.82 → 72.93 FPS（約持平）**，尾端幀時間沒有改善。各輪全員在畫面內、源碼／地形 hash 不變，沒有降畫質或更新頻率。新版 5000 人仍僅在 60 秒牆鐘推進 55.3–56.4 秒原動作，未達可玩或即時模擬。探測中的名冊整理降至 38.12／26.30 ms 每模擬秒；原指揮在場判斷與方向場仍約 86／68 ms，留待下一個定向切片。

新查詢逐欄／引用／短路／型別／COW 與原 GD 比對；原 200／5000 人加四名 Actor 的完整逐步狀態、占位、RNG、近遠程事件，以及原 native idle／render、救助、供養、像素與 format3 存讀回歸已通過。初次建置的 const 指標型別錯誤、初次測試誤用原 GD 不接受的 bool(Array) 都已修正且保留失敗。詳見 [第九階段完整證據與限制](output/site_army_scale_phase9_20260914/README.md)。本輪未 stage／commit／push；未驗證正式匯出、跨平台或完整大量射手／地形／武器矩陣。

## 第十階段：前線起點純幾何批次（2026-09-15）

拆分原主畫面 CPU 成本後，方向場主要花在前線起點搜尋，真正傳播僅約 0.87 ms／模擬秒。先以保守範圍略過不可能近的敌格，再讓既有原生模組同步比較原敵格與接觸點的八格 Manhattan 距離，返回新 byte mask。起點段短測約 66.00 → 47.44 ms／模擬秒；不是整個方向場、GPU 或全部 CPU 成本。原指揮在場判斷本輪只探測未改。

原敵格順序、所有地形／占位／外部回呼及其順序、512 格／八格遠的局部反向場、每次最多八個新步與原人物預約不變；不另建導航、人物權威資料或跨幀快取。原生比較乘積超過 2,000,000、特殊輸入或缺方法時回到原 GD 空間桶；回退也保留精確距離判斷。原 30 Hz／10 Hz、200 NPC 保存及畫質不變。

實際 TerrainLab 四輪各 60 秒 ABBA，5000 人 **8.5970 → 9.0373 FPS（+5.12%）**；P95 中位數 176.72 → 164.95 ms。200 人四輪各 30 秒 **76.47 → 77.98 FPS（+1.97%）**。各輪前後全員可見／畫面外 0、產品與地形 hash 不變；未達可玩／30 FPS，新版大場景仍只在 60 秒牆鐘推進 57.5–58.0 秒原動作。

200 組 seeded 幾何／極端座標與凍結第九階段 200／5000 人逐步狀態、占格、RNG、外部阻擋回呼順序之 native／fallback 比對通過；原包圍／射手／工作／救助／追擊守衛、完整戰鬥、原生待機／呈現／查詢、疲勞供養、裝備像素與 format3 存讀回歸通過。最初測試注入誤匹配另一個 pending 迴圈的 parse FAIL 已限縮範圍修正，產品未受影響，失敗保留且驗證器未改。詳見 [第十階段完整紀錄](output/site_army_scale_phase10_20260915/README.md)。本輪未 stage／commit／push，正式匯出／跨平台／大量射手全矩陣未驗收。
