# 中式披風重製計畫與建模 Skill 檢討

日期：2026-09-10。狀態：男女重製與正式編輯器接入已完成；以下先列本次交付，再保留原始問題分析。

### 完成內容與驗證邊界

- 正式男女 `.blend/.glb/.json` 已替換，沿用 `cape_chinese_01`；旅行披風、身體、臉、頭髮、裝備與原骨架保留。覆寫前逐一核對六個正式檔案仍與本輪備份一致。
- 可編輯來源：`assets/characters/human/q35/chinese_cloak/chinese_cloak_male.blend`、`chinese_cloak_female.blend`。主布改成連續開襟曲面，配完整對應紅內面、弧形肩披、開口立領、雙扣與長流蘇；四張 2048×2048 貼圖提供黑布、紅暗紋及金色雲紋。
- 每性別披風有 7 個網格、18,616 三角面；主布與肩披各 336 個姿態修形。一般動作取 13 個姿態，轉身斬擊取 25 個；不是即時布料物理。內外布使用相同三角對角線，避免彎折時紅黑碎片交錯。
- `make_chinese_cape.py` 現為可編輯來源的載入入口，男女 builder 在既有動作建立後綁定同名 NLA 軌道。重跑 builder 不會重新生成舊布板。本次驗證實際 loader，未重建無關的整套裝甲。
- 匯出延遲已解決：既有動作使用直接鍵值匯出並停用不必要的 viewport 更新。原包未公開來源動畫另保留原 channel；男 43、女 41 個原 GLB 動畫名稱完整保留。公開動作清單與 UI 別名規則不變。
- `scripts/tests/validate_chinese_cloak_assets.py`：PASS。比對非披風節點變換、網格索引、所有頂點屬性、權重、joint 次序、inverse bind matrices，男女均零差異項目（浮點比較有明示容差）；披風 UV、正規化權重及修形數量通過。
- `scripts/tests/chinese_cloak_loader_test.py`：Blender 4.2.22，男女 `CHINESE_CLOAK_PRODUCTION_LOADER_PASS`、EXIT=0。確認實際入口可載入 7 件、重綁原骨架、保留 UV／權重與動作軌道。
- `human_character_3d_editor_test.gd`：Godot 4.6.2 visual PASS、EXIT=0、9.439 秒；日誌 `.godot-temp/godot_verify/20260910_031800_975/`。涵蓋男女槽位、披風無／旅行／中式切換，以及既有動畫、武器與跳躍路由回歸。
- 正式路徑全動作擷取：男 PASS、EXIT=0、55.599 秒（`20260910_031407_485`）；女 PASS、EXIT=0、56.094 秒（`20260910_031533_104`）。每性別 24 份、每份 16 張原始幀；含既有別名，不把 24 份視為 24 個獨立 UI 動作。別名捕捉依實際 resolved 動作計時，後續已補拍。
- 後續正式補拍亦 PASS：別名／組合 `20260910_031743_931`、`20260910_031826_506`；防禦構圖 `20260910_031944_646`、`20260910_031959_456`；大幅受擊／斧鎚構圖 `20260910_032331_001`、`20260910_032411_441`；單件／五視圖 `20260910_032515_114`、`20260910_032527_939`。
- 最新圖在 `.visual_captures/chinese_cloak_remake/`：男女中性前／後／左右／3⁄4、領部近景、單件完整內面、編輯器選中圖與原投影比例圖。動作提供 48 份 GIF、48 份八關鍵幀圖，以及保留原尺寸的 1024×1280 PNG。GIF 按實際動畫時長取樣，移除重複循環端點；不是 FPS 測量。
- 組合抽查：男女的內衣、完整皮甲、鐵甲、明光鎧，包含相應靴／頭盔與劍盾；每組 idle／run／guard 關鍵姿態。這是指定姿態抽查，不是所有裝備、鏡頭角度與每一個中間幀的碰撞認證。任意新動畫、體型變化、動作混合仍需另驗，不宣稱「100% 零穿模」。
- 建模 Skill 已加入長布流程、來源保留、實際材質路由、內外面對應剖分、插值中間幀與功能／視覺分開驗收；交接手冊同步修正通用零穿模保證。`quick_validate.py` 實跑結果：`Skill is valid!`、EXIT=0。
- 驗證的 `Failed to read the root certificate store` 是單獨列出的環境訊息；只有 EXIT=0 且無 SCRIPT ERROR 的本次結果才列 PASS。過去超時／錯誤日誌未改寫，舊版六檔備份仍在 `.godot-temp/chinese_cape_remake_baseline/`。
- 所有修改未 commit；沒有刪除其他工作樹資料。
- 視覺限制：大幅彎腰／旋身時，部分布褶仍偏硬、局部下襬會保守向外鼓起；目前使用預烘焙修形，不等同自然布料模擬。正式整包 GLB 男約 83.8 MB、女約 88.9 MB，本輪未做大量角色效能測量。
- 最後核對：四個正式 `.blend/.glb` 與已驗證來源／候選 SHA-256 全部相同；兩份 JSON 除中式披風清單外完全相同。男女均 7 件中式披風；原旅行披風保留男 6 件、女 4 件。依當前動畫清單逐檔確認 768 張動作 PNG，舊版額外幀未刪除。
- 單件預覽最後修正角度更新會重新顯示頭髮的截圖順序，並加入非披風網格不可出現的斷言。補拍男 `20260910_033127_053`（11.302 秒）、女 `20260910_033150_899`（11.163 秒），均 visual PASS、EXIT=0；已目視確認只剩披風。

### 後續編輯與重建

手動修改披風專用 Blender 來源後，只執行 `author_chinese_cape.py -- --sex male --stage export`（female 對應），再驗證候選與晉升正式檔案。`finish` 是顯式重建美術入口，會重新生成披風，不能用於保留手工修形的日常匯出。正式整包 builder 透過 loader 使用專用來源。

原始審查與重製計畫如下；其中「現況」與旧行號描述的是重製前的失敗版本，不代表目前資產。

## 1. 結論與上一輪驗收更正

目前 `cape_chinese_01` 只能算「已接入的粗模」，外觀未通過參考圖驗收，動畫穿模未得到足夠證據。上一輪把程式執行成功與截圖產生成功寫成「視覺 PASS、未見明顯穿模或漂浮」，結論過度且不成立。

重製必須處理主披風的連續曲面、肩披、立領、完整內裡與布料形變。只調顏色、細分面數、增加金線或縮放現有布板，無法解決目前問題。

沿用 `cape_chinese_01`／`Cape_Chinese_01` 作為替換入口。保留旅行披風、男女素體、臉、頭髮、既有骨架與動作 ID、編輯器槽位及地圖投影契約。舊失敗畫面與日誌保留作比較證據。

## 2. 參考圖真正要求的造型

參考：`assets/characters/human/q35/reference/chinese_cloak_reference_20260910.png`。文字標示、面數與「Game Ready」不作為已達標證據。

| 部位 | 重製必須保留的視覺特徵 | 現況差距 |
| --- | --- | --- |
| 主輪廓 | 從肩頸垂落、接近腳踝、逐漸展開的鐘形長披風；前方開襟；側背形成環身曲面 | 偏短、狹窄，前側後像幾片直板，缺少環身垂墜量 |
| 肩披 | 環繞肩背的短層披肩，弧形下緣覆蓋上臂根部，與主披風層次清楚 | 左右矩形板翻起，肩頸和胸前有不連貫的缺口 |
| 立領 | 可辨識的立起高度、前開口、紅色內面、薄金包邊 | 完整橢圓環；穿戴截圖看不出設定圖的開襟立領 |
| 布料 | 從肩部受力點向下展開的主褶、重力垂墜、局部翻邊 | 細小而規律的正弦波，整體仍像硬板 |
| 內裡 | 與外殼對應的完整暗紅內面，從開襟和翻起的下襬自然露出 | 胸前兩條紅帶與背部窄紅下緣，沒有完整背／側內裡 |
| 裝飾 | 下襬與肩披的連續雲紋帶、雙扣、長流蘇、紅面暗紋 | 小多邊形線圈、細管、壓扁球與短錐體，覆蓋範圍與辨識度不足 |

整體仍適配目前動畫風素體；參考圖的輪廓、層次與黑／暗紅／古金關係是主要目標，不因角色風格不同而省掉結構。初始灰模以接近踝部且離地的長度、沿肩線披覆的短肩披為目標，實際比例由男女素體量測及四視圖決定。

## 3. 已確認的技術原因

### A. 幾何與結構

- `make_chinese_cape.py:146` 起的 `_front_panel`、`:188` 的 `_back_panel`、`:227` 的 `_side_panel` 各自建立網格；前後深度與接邊也各自估算，沒有共同裁片邊界或連續環身輪廓。
- `:265` 的 `_shoulder_mantle` 是稍有拱度的矩形曲面；`:545` 起把整片肩披剛性綁到左右 `UpperArm`。畫面中肩片跟手臂翻起，原因與此綁定方式一致。
- `:301` 的 `_ring` 用封閉周圈建立領子，沒有參考圖的前開口。`:604` 起上下緣又各加一圈。
- `:404–427` 只用男女兩組固定尺寸，函式未接收身體或內甲表面；無法把這些常數當成實際貼合量測。
- `:442–453` 的背內裡只有約 0.078 公尺高的下襬窄帶。`:471–483` 的前內裡是另設寬度／深度的紅色板，並非由完整外殼內面推導。

### B. 布料形變

- `_create_skinned`（`:98–126`）只依頂點 Z 值，在四個軀幹骨骼間線性分配權重。
- `:429–434` 的最後錨點是 Hips；男 Z≤0.970、女 Z≤0.870 的布料頂點都給 Hips 100%。這不能表現下襬獨立擺動，也不會自動避開邁步的腿或馬背。
- 有 Armature 權重只證明可跟骨骼變換；目前沒有披風專用的形變校正或動態垂墜機制。完整動畫是否穿模，現有小圖不足以判定。

### C. 材質和描邊

- 本輪直接讀取男女 GLB：各有 22 個 `Cape_Chinese_01` 網格節點、8,068 個三角面；全部披風 primitive 都沒有 `TEXCOORD_0`，沒有 base-color texture。面數已不少，細節投入卻沒有形成參考圖中的布面與紋樣。
- 編輯器 `EQUIPMENT_TOON_SHADER`（`human_character_3d_editor.gd:142`）與 `_apply_toon_surfaces`（`:1822`）只接顏色、金屬度、粗糙度。紋理與法線貼圖沒有沿這條路傳入。
- 固定描邊寬 0.004，金線管半徑約 0.0026。`Raised_Embroidery_Trim` 沒有命中細節排除字串，會套用描邊；描邊尺度大於金線半徑，容易吞掉細線的顏色與形狀。
- `_is_equipment_node` 的細節排除同時跳過描邊和 Toon 轉換；名稱含 `_Lining` 的內裡走原材質，而外布走 Toon。這是需要明確處理的材質路由差異，不應繼續靠改名字碰運氣。
- 截圖確實偏藍灰且缺少布感；光照、材質數值和描邊各自的貢獻尚未做 A/B 比較，不能把顏色問題全部歸因於單一 shader。

### D. 驗證畫面失效

- `capture_chinese_cape_showcase.gd:89` 先設相機，`:103` 才切動畫；切動畫進入 `_play_selected_animation`，在 `human_character_3d_editor.gd:1354` 又呼叫 `_update_preview_framing`。
- 相機因此從 capture 的 size=2.05 被重設為 `CharacterRenderContract.FOOT_PROFILE` 的 7.856338。這可解釋靜態圖與動作圖中人物大小突然改變。
- 截圖取整個 1600×900 編輯器畫面，角色在中央窄欄；動作圖中的人物只有約數十像素高，无法檢查肩縫、膝腿、下襬接觸。
- 每個動作只取一個固定秒數。`attack_jump_heavy` 的 0.36 秒沒有證明已取到最高點、最大屈膝或落地；最後 UI 圖也未 seek 到有效 idle 姿態。
- 靜態四視圖仍使用帶扭身的 idle，yaw=0 不能直接當成嚴格正面量測圖。
- 原功能測試確實檢查網格存在和選項切換；這些斷言沒有評分參考相似度，也沒有量測穿模。

## 4. 重製順序與各階段出口

每一階段產出可檢查的圖或實際 GLB，未過標準就修該階段。這是執行者的品質檢查，不表示每一步都要停下來要求使用者批准。

### P0：建立可信的檢視與比較基線

1. 保存此次失敗版本的目標檔案清單、雜湊與參考圖；重製迭代放獨立資料夾，避免在建模迭代中反覆覆寫正式男女整包。
2. 改造現有專用 capture：切模型／裝備／動畫與 seek 完成後，再設美術檢視相機並等候渲染。直接輸出 preview viewport 的完整解析度；整個 UI 圖另外保留一張。
3. 固定中性站姿與水平相機取得前／後／左右／3⁄4 圖，另拍帶俯視角的實際遊戲尺度。美術檢查相機只作用於驗證場景，保留 `CharacterRenderContract` 的地圖比例。
4. 計画標準：美術全身圖至少 1024×1024，角色高度佔 70–85%，完整下襬入鏡；局部圖須能辨認領緣和布片接縫。角色太小、被裁切、姿態未生效，標為「不可判讀」。

出口：同一版模型的靜態和動作畫面尺度一致；參考、灰模與最終材質圖能直接比較，且每張標示 body、動作 ID、時間、角度與模型版本。

### P1：只重建主要布面與肩領

1. 在男女原生 Rest Pose 量測肩峰、頸部、背部、胸前、髖與內甲包絡。保存量測結果與採用的穿戴間隙。
2. 主體改用圍繞軀幹的連續開襟曲面，或有明確縫合／覆疊邊界的裁片。先做肩部收束、背部主褶、側面展開量、前襟曲線與弧形下襬。
3. 肩披重做成環繞肩背的短披肩；它是柔性衣片，不能直接沿用硬護肩的整片上臂剛性掛接。
4. 領子依頸部截面建立前開口、側高與後高；從領座接到肩披，避免懸空邊緣。
5. 紅內裡由外殼同一套曲面衍生完整內面，邊界與變形一致。可分材質面，不必把每段紅邊都建成另一件厚實物件。
6. 建模來源以可編輯的披風專用 `.blend` 為準，腳本負責載入、適配、命名及匯出。可以程序化建立起始網格，但需保留可編輯的輪廓與褶皺；重跑 builder 不能沖掉已調整的大形。

出口：無金線、無貼圖的中性灰模，在穿戴及單件四視圖已能辨認設定图的長披風、短肩披與開襟立領；肩頸無破口，下襬不呈筆直板框。大形未過，先不做花紋與流蘇。

### P2：在灰模階段驗證布料動作

1. 沿用目前 Q35 男女原生骨架，不套用另一條 HumanRigV1 基準。現有 GLB skin joint 數分別為男 91、女 119，須保留既有綁定及動作映射，不硬套「22 骨」舊資料。
2. 肩領受力區按肩／胸的實際變形配置權重；長下襬不再只用高度自動分配到骨盆，也不把整張背布直接轉印成左右腿的權重。
3. 預定採用既有骨骼蒙皮加少量披風 shape-key 校正，處理抬腿、前傾、跳躍和騎乘展襬。先用一個短動作驗證 morph 匯出、動畫合併、Godot 播放、切換／循環恢復與材質貼合，通過後才展開到其餘動作。
4. Blender 的布料模擬可用於肩部固定後求自然垂墜與製作參考姿態；最終進遊戲的變形需要明確轉成可匯出的資料，不能把模擬快取直接當成已完成 GLB 動畫。
5. 男女 builder 的 action／morph 匯出設定及 Godot 匯入路由必須實測。若既有骨架加校正形態仍無法達到要求，先提交具體失敗姿態和需要的披風附加骨方案；不能私自重建人體骨架或降標驗收。

出口：灰模在 walk、run、跳躍起跳／最高點／落地、格擋抬臂與騎乘時，肩披不翻成板、腿不大面積穿過布面、布料不黏成腿套；校正形態與原動畫在 Godot 同步。

技術依據：Blender 官方文件列出 [glTF 的 shape-key／skinning 動畫支援](https://docs.blender.org/manual/en/3.0/addons/import_export/scene_gltf2.html?highlight=gltf) 與 [Cloth Pin Group](https://docs.blender.org/manual/en/2.90/physics/cloth/settings/shape.html)。這些能力不等於目前專案已完成相容性驗證；本階段的一個短動作試作負責證明可行性。

### P3：打通布料材質，再製作裝飾

1. 先用一小片有 UV、辨識色紋與法線的樣片，驗證 Blender → GLB → Godot 的披風材質路由；確認外布、內裡、金屬和描邊如何分別處理。
2. 針對披風增加最小必要的 albedo／normal／roughness 支援，維持既有角色風格；把「是否描邊」與「材質轉換」分開判斷。金線、刺繡、薄包邊避免套用大於自身尺度的粗描邊。
3. 展開主布、肩披與內裡 UV；建立黑色粗布、暗紅織紋、古金繡線。先完成整體色塊及主褶陰影，再處理微紋理。
4. 雲紋按參考的下襬／肩披分布製作完整圖案帶，主要用貼圖，必要的包邊與浮雕才用幾何。扣具須有相互配對的輪廓，流蘇須有懸掛長度與束狀外形。
5. 材質圖和灰模圖都要檢查。不能靠大範圍發光、雙面渲染或粗黑邊掩蓋法線、漏面與布片錯位。

出口：正常遊戲尺度能讀到黑／紅／金和主要紋樣；近景可辨識布料、內裡、刺繡和扣具；Godot 中已確認可見，沒有只在 Blender 看得到的細節。

### P4：男女適配、組合與正式接入

1. 男版大形與動作可行後再完整適配女版，按胸背包絡、肩寬、頸圍及身高調整，分別檢查；前期量測仍包含兩種素體。
2. 替換原 `cape_chinese_01` 的幾何及材質。builder 讀取專用來源並加入現有匯出清單；保留旅行披風與其他部件。以部件識別和實際畫面驗證，避免用 22 件物件數鎖死舊錯誤拓撲。
3. 單件、搭內衣、搭皮甲、搭明光甲各自檢查。盾牌、長武器、背負武器、長髮及騎乘屬接觸高風險組合，拍到接觸部位；未測到的組合明確列出。
4. 採用本來就存在的測試及 capture，補上必要的目標選取和動畫路由檢查。`-ExpectedOutput` 僅列檔案；PASS 文字在 stdout 檢查。
5. 保存獨立來源、男女正式 GLB／JSON、前後比較圖、動畫與驗證紀錄，記錄三角面／UV／材質／surface 數及影響範圍。參考圖宣稱的 8,412 tris 不作硬性指標；現有 8,068 tris 也不證明分配合理。

出口：男女新選項可用，Travel／Chinese／None 切換不殘留，原有動作正常，人物其餘部件無回歸；畫面與模型版本可對應。

## 5. 最終驗收矩陣

| 檢查 | 最低證據 | 不通過條件 |
| --- | --- | --- |
| 輪廓 | 男女穿戴與披風單件，正／後／左右／3⁄4，同尺度並排參考 | 紙板形、肩披翻翼、短披風比例、領口消失、前後結構不連續 |
| 內外層 | 開襟、下襬翻折、側背內面近景 | 紅帶冒充完整內裡、漏面、交疊閃爍、兩層不同步 |
| 材質 | Blender 與 Godot 相同角度；實際遊戲尺度另列 | 貼圖遺失、金線被描邊吞掉、黑布變無層次平板 |
| 步行／跑步 | 男女完整短循環影片或可播放逐幀序列，含左右最大跨步的關鍵幀 | 只截單幀、人物過小、腿穿出大塊布面、接縫張開 |
| 大幅動作 | 實際動作時長內的起跳、最高點、落地、抬臂、扭身；完整過程可回看 | 未取到極值、回到 idle 後校正形態未重置、肩領脫離 |
| 騎乘與裝備 | 男女騎乘、皮甲／明光甲、盾牌／背負武器的高風險部位 | 馬背穿過整片布料、武器與布料互相截斷卻仍宣稱支援 |
| 功能與來源 | 目標切換測試、GLB／圖像版本記錄、原模型對照 | 只有下拉文字、來源不可重建、預覽對應不到本次模型 |

每項只記「通過／失敗／未測／不可判讀」及具體證據。執行 exit 0、圖像輸出成功、功能測試通過與美術驗收結果各自記錄。對動畫的結論限於已檢查的動作、組合及幀範圍，不泛稱全部零穿模。

## 6. 建模 Skill 檢討

审查對象：`.agents/skills/worldgoing-3d-equipment/SKILL.md` 及其 `references/pipeline.md`，連同它要求讀取的 `HUMANBASE_V1_3D_PIPELINE_HANDOVER.md`。

### 6.1 原本就有規則，上一輪未落實

| 現有規則 | 上一輪失誤 |
| --- | --- |
| SKILL.md:21：粗模先展示正側背，大形未像參考先修 | 尚無合格的肩披／長披風輪廓就開始生成金線、扣具與流蘇 |
| SKILL.md:28：量測 Rest Pose 與內層表面 | 主生成函式只有 armature、男女旗標與 helper，主要外形依固定常數 |
| SKILL.md:47：動畫多個關鍵幀 | 每個動作只取一個固定時間，且 capture 被相機重設縮成小圖 |
| SKILL.md:48：單件、內層、完整裝備，男女分開 | 新展示只固定皮甲搭配，沒有完整的組合證據 |
| SKILL.md:52：看見粗模形狀不符就修；不得把測試 PASS 寫成無穿模 | 截圖已有肩片和布板問題，仍直接宣稱視覺通過 |

第一責任是執行和判讀，不能全推给 skill 缺內容。Ponytail 的最少代碼原則也不能被解讀成減少美術完成度或省略必要驗證。

### 6.2 Skill 本身需要補強的地方

1. **目前更像裝備接入手冊，長布料方法不足。** 合身衣物的表面提取、硬甲的骨骼掛接、長披風的裁片／垂墜／形變需要不同路線。增加長布料分支，說明肩部受力、整體裁片、完整內裡與離身下襬。
2. **「看過圖」缺少可判讀標準。** 新增主體佔比、最低解析度、正交中性姿態、接觸部位近景、極值幀與完整動作檢查。圖太小必須直接標不可判讀。
3. **階段順序沒有具體出口。** 把 reference 特徵清單、灰模對照圖、短動作匯出實測、材質樣片、正式整合設為必要產物；後續執行須能找到前一階段的證據。
4. **「軟部件保留或傳遞原生權重」過於籠統。** 合身衣物可從原表面開始，長披風不能直接借全身／雙腿權重；需要驗證獨立擺動与碰撞空間，明確說明骨骼、形態校正與模擬烘焙的分工。
5. **材質路由警告應提前成為小樣驗證。** pipeline.md:29 已知道 Toon 不自動傳貼圖，但沒有把材質樣片放在細節製作之前；新增 UV、紋理、法線及細描邊的實際引擎檢查。
6. **模組化容易被誤讀成每個細節一個物件。** 模組化的驗收重點是槽位切換與部件來源；單一披風內的連續布面、材質面和刺繡不必拆成大量互不相接的網格。
7. **歷史手冊夾帶未經保證的絕對敘述。** handover 的「100% 精確權重」「從根源徹底消滅」「任意組合零穿模」不能作為技術事實或通過證據。修改時應改成目標、來源條件與實際測試範圍，固定尺寸保留為歷史例子。
8. **缺少失败範例與證據分級。** 將本次肩板、紅條假內裡、小人物動作圖作反例；區分匯出／功能／輪廓／材質／動作／相容性，禁止用單一 PASS 合併全部結論。

### 6.3 建議的 Skill 修訂範圍

- `SKILL.md`：保留專案約束及接入入口，加入「按裝備類型選方法」「灰模與動作出口」「可判讀證據」「失敗處理」。
- 新增 `references/cloth-cape.md`：集中放長布料裁片、肩領、內裡、權重／形態校正、擺動與騎乘的具體做法及本次反例。避免把主 skill 膨脹成另一份歷史長文。
- `references/pipeline.md`：補上目前相機重設路徑、單獨的美術 capture、描邊與材質排除差異、GLB UV／morph 檢查，以及 `ExpectedOutput` 的檔案語意。
- `HUMANBASE_V1_3D_PIPELINE_HANDOVER.md`：修訂絕對品質保證，標註固定數值／動作數的歷史性；以當前生成器與 GLB 為事實來源。

本輪提供上述修訂方案，尚未改動 skill；重製執行時先落實這些具體出口，不以再增加通用警語代替模型重建。

## 7. 本輪交付與執行起點

已完成：參考對照、幾何／蒙皮／材質／capture 路徑審查、男女 GLB 唯讀檢查、skill 與歷史手冊檢討、重製順序和驗收標準。

下一個具體工作包是 P0＋P1：可靠的美術檢視畫面，以及只有主布、短肩披、立領和完整內裡的灰模。交付灰模四視圖時就應看得出已解決紙板輪廓；裝飾與正式男女整包接入接在其後。
