# Pack 02 staging

這個目錄只接收依照 `PAPER_DOLL_V2_COMPONENT_PACK_02_SPEC.md` 產生、尚未通過 Validator 的標準化 PNG。

規則：

- 不得把未驗證素材直接放進 `assets/paper_doll/v2/parts/`。
- 每個檔名、狀態、性別策略與內容框必須符合 `assets/paper_doll/v2/pack_02_spec.json`。
- 完成一個槽位後，必須同時提供來源板、標準化 PNG、contact sheet、mask overlay 與 JSON 報告。
- `README` 只保留 staging 目錄；空目錄不代表素材已通過。
