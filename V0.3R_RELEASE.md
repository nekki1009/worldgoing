# V0.3R

## 內容

只保留目前 TerrainLab 入口的新 Site 地圖系統，沿用現有地形、角色、騎乘、
武器碰撞與百人隊伍。保留男女 3D 模組化角色、各八款髮型、染色與修正後的頭盔遮髮。

刪除舊 2D 紙娃娃各版本及專用素材／製作工具／測試，刪除舊 World／Region／
ContinuousWorldMap／SiteMap／Battle 入口及已無現行呼叫者的舊生成、旅行、編隊與存檔流程。
現行共用岩壁貼圖、3D 角色參考圖及烘焙士兵圖集保留。

刪除前的本機檔案移至 `.godot-temp/v03r_cleanup/removed/`，包括未提交和未追蹤的舊工作；
改寫文件的原稿保留於同層 `originals/`。這些備份不隨版本上傳。
目前版本沒有舊世界旅行或舊存檔相容功能。

## 版本庫範圍

正式程式、必要資產、可編輯來源與檢查保留；二進位模型使用 Git LFS。
Clone 後須安裝 Git LFS 並執行 `git lfs pull`，再用 Godot 匯入專案。
本機工具安裝、候選匯出、Blender 自動備份、快取及擷取圖不納入版本。
Blender 製作仍需依 3D 管線文件準備 Blender／VRM addon；版本庫不封裝工具安裝。

## 驗證

2026-09-11，從 Git 暫存內容建立不含既有 `.godot` 快取的乾淨副本後驗證。
使用 `C:/Users/Nekki/Godot_v4.6.2-stable_mono_win64/Godot_v4.6.2-stable_mono_win64_console.exe`
與 `godot-runtime-verify` 的限時執行器；下列五項皆退出 0、無腳本／斷言錯誤。

| 檢查 | 時間 | 結果記錄 |
| --- | --- | --- |
| 完整冷匯入 `--headless --path <project> --import` | 27.699 秒 | `20260911_185508_023` |
| 清理回歸 `--headless --path <project> --script res://scripts/tests/v03r_cleanup_test.gd` | 1.338 秒 | `20260911_185546_399` |
| GPU 清理回歸（同上但不帶 `--headless`） | 19.682 秒 | `20260911_185557_478` |
| 地形回歸 `--headless --path <project> --script res://scripts/tests/terrain_lab_test.gd` | 11.782 秒 | `20260911_185657_878` |
| 隊伍回歸 `--headless --path <project> --script res://scripts/tests/terrain_lab_army_test.gd` | 23.707 秒 | `20260911_185724_570` |

記錄位於本機 `.godot-temp/v03r_cleanup/clean_project/.godot-temp/godot_verify/<記錄>/result.json`。
清理回歸確認正式入口、舊入口不存在、必要資源、100×100 地形、兩名角色與百人合法獨立佔格。
GPU 輸出 `.visual_captures/v03r_release/01_site.png`、`02_army.png`（乾淨副本內）已實際檢視；
地表／岩壁、角色與士兵圖集正常顯示。地形回歸涵蓋 8 預設 × 3 seeds 與 845,552 次通行指令檢查。
Git LFS 物件及 pointer 完整性檢查通過。

首次短掃描使用 `--quit-after 2`，未完成冷匯入，後續測試因缺少貼圖快取而逾時，不能算通過。
確認原因後改用上列 `--import` 等待全部資產完成，才逐項重驗成功；首次開啟也必須完成資產匯入。
成功執行仍有 Windows root certificate store 環境訊息，沒有伴隨其他錯誤。

本次是刪除／打包回歸，不宣稱全部裝備動畫、萬人效能、跨地圖旅行或存讀檔已通過。
